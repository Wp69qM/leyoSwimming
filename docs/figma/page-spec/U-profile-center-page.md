# 页面规格：U-个人中心页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-052（用户退出登录）/ US-005（用户完善个人资料，资料入口）/ US-009（隐私协议授权，设置入口）
> **对应 PRD**：§3.2 身份状态机、§5.2 注册登录、§13.3 MVP 页面
> **版本**：v1.1 | **创建日期**：2026-08-06 | **更新日期**：2026-08-08

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 个人中心页 |
| 页面名（Frame） | `U-个人中心页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 标准小程序页面尺寸 |
| 页面背景 | 按全局规范页面背景色 |
| 是否含 TabBar | **是**（首页 / 教练 / 预约 / 我的），当前选中「我的」 |
| 是否含 NavBar | **是**（白色背景，标题「我的」） |
| 返回按钮 | 否（Tab 首页） |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 说明 | Calicat 中未找到独立 U-个人中心页 frame | - |

---

## 2. 用户身份与业务状态维度

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | ✅ | 可进入，顶部显示登录/注册入口；底部「退出登录」按钮隐藏 |
| 注册用户 | ✅ | 完整入口与信息展示；底部显示「退出登录」按钮 |
| 学员 | ✅ | 与注册用户一致 |
| 教练 | ❌ | 用户端不展示教练身份；教练使用教练端小程序 |
| 管理员 | ❌ | 管理端使用 Web 后台 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| **已登录** | 本地存在有效 access_token / refresh_token | 展示用户头像、昵称、身份状态与完整功能入口；底部显示「退出登录」按钮 |
| **未登录（游客）** | 无有效 token | 顶部区域显示默认头像与「请登录/注册」提示；功能入口点击后引导登录；隐藏「退出登录」按钮 |
| **退出登录中** | 用户点击「退出登录」并确认后 | 按钮进入 loading，调用 `/api/user/auth/logout` |
| **退出登录成功** | 后端/前端清除 token 后 | 页面刷新为游客态，不跳转其他页面 |

---

## 3. 元素清单（自上而下）

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 状态栏 + 导航栏 | 为系统状态栏预留区域并展示页面标题「我的」 | 全部身份 | 无 | 全局设计规范 §2.5.6 |
| 用户信息头图卡片 | 展示用户头像、昵称/姓名、身份状态，并作为资料完善入口 | 全部身份 | 整卡点击 → 已登录用户跳转 `U-完善个人资料页`；游客引导登录 | US-005 §4.1 / §4.2；CROSS-BATCH-PRINCIPLES §1.2 |
| 用户头像 | 展示用户头像或默认占位 | 全部身份 | 同整卡跳转 | US-005 |
| 功能入口列表 | 聚合 MVP 个人中心功能入口 | 全部身份；游客点击引导登录 | 按各入口对应页面跳转 | PRD §13.3；US-009 §4.3 |
| 退出登录按钮 | 提供退出当前账号能力 | 仅已登录用户（注册用户 / 学员） | 点击 → 二次确认弹窗 → 确认后调用 `/api/user/auth/logout`，清除本地 token，页面刷新为游客态 | US-052 §4.1 / §6.1；PRD §5.2.1 |

### 3.1 功能入口（MVP）

| 入口 | 跳转目标 | 说明 |
|------|---------|------|
| 我的套餐 | `U-我的套餐页`（page-spec 待补充） | 展示已购套餐列表 |
| 我的订单 | `U-订单列表页` | 展示历史订单 |
| 我的预约 | `U-我的预约页` | 展示课程预约记录 |
| 隐私协议 | `U-隐私协议页` | US-009 授权后随时查看 |
| 设置 | `U-设置页` | 账号安全、消息通知等 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 |
|------|:---:|:---:|:---:|
| 状态栏 + NavBar | ✅ | ✅ | ✅ |
| 用户信息头图卡片 | ✅ | ✅ | ✅ |
| 用户头像 | ✅ | ✅ | ✅ |
| 功能入口列表 | ✅ | ✅ | ✅ |
| 我的套餐入口 | ✅（引导登录） | ✅ | ✅ |
| 我的订单入口 | ✅（引导登录） | ✅ | ✅ |
| 我的预约入口 | ✅（引导登录） | ✅ | ✅ |
| 隐私协议入口 | ✅（引导登录） | ✅ | ✅ |
| 设置入口 | ✅（引导登录） | ✅ | ✅ |
| 退出登录按钮 | ❌ | ✅ | ✅ |

---

## 5. 跳转动线矩阵

| 触发元素 | 游客 | 注册用户 | 学员 |
|---------|:----:|:-------:|:----:|
| 用户信息头图 | → `U-微信授权页` / `U-手机号登录页` | → `U-完善个人资料页` | → `U-完善个人资料页` |
| 我的套餐 | 引导登录 | → `U-我的套餐页` | → `U-我的套餐页` |
| 我的订单 | 引导登录 | → `U-订单列表页` | → `U-订单列表页` |
| 我的预约 | 引导登录 | → `U-我的预约页` | → `U-我的预约页` |
| 隐私协议 | 引导登录 | → `U-隐私协议页` | → `U-隐私协议页` |
| 设置 | 引导登录 | → `U-设置页` | → `U-设置页` |
| 退出登录 | 隐藏 | 二次确认 → 退出登录 → 刷新本页为游客态 | 二次确认 → 退出登录 → 刷新本页为游客态 |

> **登录引导**：当游客点击受登录限制的功能入口时，优先唤起登录页（`U-微信授权页` 或 `U-手机号登录页`），而不是直接提示错误。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 页面首次加载，数据尚未返回 | 展示占位头像与默认昵称；功能入口正常显示 |
| **加载中** | 请求用户信息或提交退出登录 | 头像/昵称区域显示骨架屏；退出登录按钮显示「退出中…」 |
| **错误状态** | 网络/服务错误 | 顶部展示错误提示条，支持重试 |
| **成功状态** | 数据正常或退出成功 | 按身份展示完整内容；退出成功后切换为游客态 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 用户可通过「我的」页面退出当前登录态 | PRD §5.2.1；US-052 §4.1 / §6.1 | §3 退出登录按钮 |
| 退出登录后清除会话/token | PRD §5.2.1；US-052 §4.1 | §3 点击行为、§5 跳转矩阵 |
| 用户可完善/编辑个人资料 | US-005 §4.1 / §4.2 | §3 用户信息头图卡片 |
| 用户可在个人中心查看隐私协议 | US-009 §4.3 | §3.1 功能入口列表 |
| 游客访问受登录限制功能需引导登录 | CROSS-BATCH-PRINCIPLES §1.2 | §5 跳转矩阵 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 页面背景使用全局页面背景色
- [ ] 页面含 TabBar，当前选中「我的」
- [ ] 页面含 NavBar，标题「我的」，无返回按钮
- [ ] 顶部用户信息卡片为按全局卡片规范渲染的容器，含头像、昵称、身份状态
- [ ] 游客态顶部显示默认头像与「请登录/注册」提示
- [ ] 功能入口列表为按全局卡片规范渲染的容器，含 5 个 MVP 入口
- [ ] 退出登录按钮仅对已登录用户显示，位于页面底部
- [ ] 退出登录二次确认弹窗文案为「确定要退出登录吗？」
- [ ] 退出成功后页面刷新为游客态，不跳转其他页面
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位：`U-个人中心页-empty`、`U-个人中心页-loading`、`U-个人中心页-error`、`U-个人中心页-success`

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-06 | PM | 初版：定义个人中心页基础结构、元素清单、跳转矩阵 |
| v1.1 | 2026-08-08 | PM | 补充身份维度、显示规则总表、四态设计、业务规则引用、AI 检查清单与变更日志；修正退出登录后停留在本页并刷新为游客态 |
