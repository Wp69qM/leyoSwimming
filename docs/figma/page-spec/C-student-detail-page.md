# 页面规格：C-学员详情编辑页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-037
> **对应 PRD**：§5.4.4 学员管理、§11.1 数据安全
> **版本**：v1.0 | **创建日期**：2026-08-02

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 学员详情编辑页 |
| 页面名（Frame） | `C-学员详情编辑页` |
| 平台 | 微信小程序 - 教练端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA` |
| 是否含 TabBar | 否 |
| 是否含 NavBar | **是**（白色背景，标题 "学员详情"） |
| 返回按钮 | 是 |

---

## 2. 教练状态维度

### 2.1 适用状态

| 状态 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 已通过（1） | 是 | 完整编辑功能 |
| 申请离职中（4） | 是 | 可查看/编辑 |
| 其他状态 | 否 | 重定向 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| 成人学员 | is_minor = false | 监护人表单项隐藏 |
| 未成年学员 | is_minor = true | 监护人表单项必填 |
| 学员已注销 | user.status = 1 | 姓名/手机号脱敏，编辑功能禁用 |
| 非关联学员 | 无 booking/package | 显示"无权查看"空状态 |

---

## 3. 元素清单（自上而下）

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，#FFFFFF，y=0 |
| NavBar | 375×44，#FFFFFF，y=44，底部 1px `#F0F0F0` |
| 标题 | "学员详情"，17px Medium `#262626`，居中 |
| 左侧返回 | 24×24 `#262626` 图标，x=16 |
| 右侧保存 | 文字按钮 "保存"，14px Medium `#1890FF` |

### 3.2 学员基础信息区（y=88）

| 字段 | 值 |
|------|-----|
| 容器 | 375×120，`gradient-water` 背景 |
| 头像 | 64×64 圆形，居中，白边 3px |
| 姓名 | 20px Bold 白色，居中 |
| 手机号 | 14px 白色 80%，居中（已注销时脱敏） |
| 未成年人徽章 | 条件显示，pill，`#FF4D4F` 浅色 |

### 3.3 关联数据摘要（y=224）

| 字段 | 值 |
|------|-----|
| 容器 | 343×72，x=16，白底圆角 12，shadow-card |
| 3 列数据 | 累计课时 / 剩余课时 / 最近上课日期 |
| 数字 | 24px Bold `#262626` |
| 标签 | 12px `#8C8C8C` |

### 3.4 信息切片编辑区（y=312）

| 字段 | 值 |
|------|-----|
| 容器 | 343×动态，x=16，白底圆角 12，shadow-card |
| 学习泳姿 | 单选/输入，如自由泳/蛙泳/仰泳/蝶泳 |
| 游泳等级 | 单选，如初级/进阶/高级 |
| 基础情况 | 多行文本，最多 200 字 |
| 沟通备注 | 多行文本，最多 500 字 |
| 是否未成年人 | Switch 开关 |
| 监护人姓名 | 输入框，开关为 true 时必填 |
| 监护人手机号 | 输入框，开关为 true 时必填，带格式校验 |

### 3.5 底部保存按钮

| 字段 | 值 |
|------|-----|
| 按钮 | 343×48，`gradient-primary` 渐变，圆角 8，白色文字 "保存" |
| 位置 | 底部 32px |
| 禁用态 | 学员已注销或校验不通过时透明度 50% |

---

## 4. 显示规则总表

| 元素 | 已通过（1） | 申请离职中（4） | 学员已注销 | 非关联学员 |
|------|:-----------:|:----------------:|:----------:|:----------:|
| 基础信息 | ✅ | ✅ | 脱敏 | 空状态 |
| 数据摘要 | ✅ | ✅ | ✅（只读） | ❌ |
| 信息切片表单 | ✅ 可编辑 | ✅ 可编辑 | ❌ 禁用 | ❌ |
| 监护人字段 | 开关控制 | 开关控制 | 隐藏 | ❌ |
| 保存按钮 | ✅ | ✅ | ❌ | ❌ |

---

## 5. 跳转动线

| 起点 | 行为 |
|------|------|
| 保存成功 | 返回 C-我的学员列表页，Toast "保存成功" |
| 校验失败 | 停留在本页，字段标红 |
| 返回 | → C-我的学员列表页 |

---

## 6. 四态设计

| 状态 | 表现 |
|------|------|
| 空状态 | 非关联学员显示"无权查看该学员"插画 + 返回按钮 |
| 加载中 | 表单区骨架屏 |
| 错误状态 | 字段校验错误 / 网络错误 |
| 成功状态 | 保存成功 Toast |

---

## 7. 业务规则引用

| 规则 | 来源 | 本页应用 |
|------|------|---------|
| 教练仅维护关联学员的信息切片 | US-037 §4.1 / §5 规则 1 | §2.2 / §4 |
| 未成年人需填写监护人信息 | US-037 §4.2 / §5 规则 2 | §3.4 监护人字段 |
| 监护人手机号需 AES 加密存储 | US-037 §12 | §3.4 输入框 |
| 学员注销后资料脱敏展示 | US-037 §8.3 | §2.2 / §4 |
| 申请离职中仍可使用功能 | US-039 / CROSS-BATCH §1.2 | §2.1 / §4 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档与 CROSS-BATCH-PRINCIPLES-COACH.md
- [ ] 已按教练/学员状态决定显示矩阵
- [ ] 顶部头图使用 `gradient-water` 深水渐变
- [ ] 未成年人开关控制监护人表单项显隐
- [ ] 学员已注销时编辑功能禁用并脱敏
- [ ] 非关联学员显示无权查看空状态
- [ ] 四态完整

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：定义教练端学员详情编辑页结构 |
