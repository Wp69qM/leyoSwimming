# 页面规格：C-教练收入页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-012
> **对应 PRD**：§5.4.6 收入结算
> **版本**：v1.0 | **创建日期**：2026-08-02

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 教练收入页 |
| 页面名（Frame） | `C-教练收入页` |
| 平台 | 微信小程序 - 教练端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | 否 |
| 是否含 NavBar | **是**（白色背景，标题「我的收入」） |
| 返回按钮 | 是（返回 `C-教练中心页`） |

---

## 2. 教练状态维度

### 2.1 适用状态

| 状态 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 待审核（0） | 否 | 无收入入口，重定向至 `C-入驻提交成功页` |
| 已通过（1） | 是 | 完整收入概览与明细 |
| 已驳回（2） | 否 | 重定向至 `C-入驻资料填写页` |
| 已离职（3） | 否 | 功能锁定，重定向至 `C-教练中心页` |
| 申请离职中（4） | 是 | 可查看收入，但隐藏提现操作 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| 有收入数据 | 存在结算/待结算/提现记录 | 展示明细列表 |
| 无收入数据 | 无任何收入记录 | 显示空状态 |
| 可提现金额 > 0 | 账户余额可提现 | 提现按钮高亮可用 |
| 可提现金额 = 0 | 无可提现余额 | 提现按钮禁用 |

---

## 3. 元素清单（自上而下）

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 名称 | 状态栏 + 导航栏 |
| 尺寸 | 状态栏 375×44；NavBar 375×44 |
| 位置 | y=0 / y=44 |
| 背景/颜色 | `#FFFFFF`，底部 1px `#F0F0F0` |
| 圆角 | 无 |
| 字体 | 标题「我的收入」17px Medium `#262626`；左侧返回 24×24 `#262626` |
| 显示条件 | 全部可见状态 |
| 点击行为 | 返回 → `C-教练中心页` |

### 3.2 累计收入概览卡

| 字段 | 值 |
|------|-----|
| 名称 | 累计收入概览卡 |
| 尺寸 | 343×156 |
| 位置 | x=16，y=96 |
| 背景/颜色 | `gradient-primary`（#1890FF → #0050B3），`shadow-primary` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 标签「累计收入（元）」12px Regular 白色 80%；金额 32px Bold `#FFFFFF`；周期标签 12px Medium |
| 显示条件 | status=1 / status=4 |
| 点击行为 | 周期标签（本月/本年/全部）切换统计维度 |

#### 3.2.1 周期切换标签

| 字段 | 值 |
|------|-----|
| 名称 | 周期切换标签 |
| 尺寸 | 每个标签 64×28 |
| 位置 | 卡片底部居中，y=112（相对于卡片） |
| 背景/颜色 | 选中 `#FFFFFF` 文字 `#1890FF`；未选中 透明 文字 `#FFFFFF` 80% |
| 圆角 | `radius-pill`（999px） |
| 字体 | 12px Medium |
| 显示条件 | status=1 / status=4 |
| 点击行为 | 切换「本月 / 本年 / 全部」统计周期 |

### 3.3 收入统计行

| 字段 | 值 |
|------|-----|
| 名称 | 收入统计行卡片 |
| 尺寸 | 343×80 |
| 位置 | x=16，y=268 |
| 背景/颜色 | `#FFFFFF`，`shadow-card` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 数值 20px Bold `#262626`；标签 12px Regular `#8C8C8C` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | 无 |

#### 3.3.1 已结算 / 待结算 / 可提现

| 字段 | 值 |
|------|-----|
| 名称 | 已结算 / 待结算 / 可提现 |
| 尺寸 | 每列约 114×48 |
| 位置 | 卡片内三等分 |
| 背景/颜色 | 透明 |
| 圆角 | 无 |
| 字体 | 数值 20px Bold `#262626`；标签 12px Regular `#8C8C8C` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | 无 |

### 3.4 明细筛选标签

| 字段 | 值 |
|------|-----|
| 名称 | 明细筛选标签 |
| 尺寸 | 343×40 |
| 位置 | x=16，y=364 |
| 背景/颜色 | 透明；选中 pill `#1890FF` 白色文字；未选中 `#F5F5F5` `#262626` |
| 圆角 | `radius-pill`（999px） |
| 字体 | 14px Medium |
| 显示条件 | status=1 / status=4 |
| 点击行为 | 切换「全部 / 课程收入 / 提现 / 到账」列表过滤 |

### 3.5 收入明细列表

| 字段 | 值 |
|------|-----|
| 名称 | 收入明细卡片 |
| 尺寸 | 343×80 |
| 位置 | x=16，y=420 起，垂直间距 12px |
| 背景/颜色 | `#FFFFFF`，`shadow-card` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 标题 16px Bold `#262626`；时间 12px Regular `#8C8C8C`；金额 16px Bold |
| 显示条件 | 存在收入明细记录 |
| 点击行为 | 点击展开明细详情（本 MVP 为只读展示，无跳转） |

#### 3.5.1 明细类型图标

| 字段 | 值 |
|------|-----|
| 名称 | 明细类型图标 |
| 尺寸 | 40×40 |
| 位置 | x=16，y=20（相对于卡片） |
| 背景/颜色 | 课程收入 `#E6F7FF` 图标 `#1890FF`；提现 `#FFF1F0` 图标 `#FF4D4F`；到账 `#F6FFED` 图标 `#52C41A` |
| 圆角 | 50% |
| 字体 | 图标占位，标注 `Icon: income/withdraw/arrival` |
| 显示条件 | 每条明细 |
| 点击行为 | 无 |

#### 3.5.2 金额颜色

| 字段 | 值 |
|------|-----|
| 名称 | 金额文本 |
| 尺寸 | 自适应 |
| 位置 | 卡片右侧 |
| 背景/颜色 | 收入/到账 `#52C41A`；提现 `-xxx` `#FF4D4F` |
| 圆角 | 无 |
| 字体 | 16px Bold |
| 显示条件 | 每条明细 |
| 点击行为 | 无 |

### 3.6 底部提现按钮

| 字段 | 值 |
|------|-----|
| 名称 | 提现按钮 |
| 尺寸 | 343×48 |
| 位置 | 固定底部，距底 32px |
| 背景/颜色 | `gradient-accent`（#FF6B35 → #FA541C），`shadow-accent`；禁用态 `#F5F5F5` `#BFBFBF` |
| 圆角 | `radius-md`（8px） |
| 字体 | 16px Medium `#FFFFFF` |
| 显示条件 | status=1 显示；status=4 隐藏；可提现 ≤0 时禁用 |
| 点击行为 | status=1 时弹出提现说明（本 MVP 仅展示，实际走线下结算） |

---

## 4. 显示规则总表

| 元素 | 待审核（0） | 已通过（1） | 已驳回（2） | 已离职（3） | 申请离职中（4） |
|------|:-----------:|:-----------:|:-----------:|:-----------:|:----------------:|
| 累计收入概览卡 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 收入统计行 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 明细筛选标签 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 收入明细列表 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 提现按钮 | ❌ | ✅ | ❌ | ❌ | ❌ |
| 空状态插画 | ❌ | ✅ | ❌ | ❌ | ✅ |

---

## 5. 跳转动线矩阵

| 起点元素 | 目标页面 |
|---------|----------|
| 返回按钮 | → `C-教练中心页` |
| 周期切换标签 | 停留在本页，切换金额统计周期 |
| 明细筛选标签 | 停留在本页，过滤列表 |
| 收入明细卡片 | 展开/收起明细（无独立页面） |
| 提现按钮 | 弹出提现说明弹窗（无独立页面） |

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| 空状态 | 无收入明细记录 | 中央游泳主题插画 + 「暂无收入明细」+ 「收入将在课程确认后结算」 |
| 加载中 | 进入页面拉取收入数据 | 概览卡、统计行、列表显示骨架屏 |
| 错误状态 | 网络/服务异常 | 中央错误插画 + 「加载失败，点击重试」 |
| 成功状态 | 数据正常返回 | 按 §4 矩阵完整显示 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 教练可在个人中心查看收入明细、提现申请 | PRD §5.4.6 | §3.2 / §3.5 / §3.6 |
| 收入结算模式由平台配置 | PRD §5.4.6 第 1 条 | §3.3 已结算/待结算拆分 |
| 教练可管理个人主页与资料 | US-012 §3 | §1 入口归属 |
| 申请离职中功能仍可用但限制资金操作 | `CROSS-BATCH-PRINCIPLES-COACH.md` §1.2 | §2.1 / §3.6 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档与 `CROSS-BATCH-PRINCIPLES-COACH.md`
- [ ] 已读取 v1.4 全局设计规范（`docs/figma/README.md`）
- [ ] `C-教练收入页` Frame 名与页面名一致
- [ ] 已按 5 种教练状态定义 §4 显示矩阵
- [ ] 概览卡使用主色渐变，金额使用 `font-size-display`
- [ ] 明细列表按「课程收入 / 提现 / 到账」使用不同图标色
- [ ] 提现按钮使用强调色渐变，无余额时禁用
- [ ] 申请离职中状态隐藏提现按钮
- [ ] 已按 §6 预留 4 态视觉占位
- [ ] 跳转动线只引用已存在的 C- 页面

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：定义教练收入页结构、收入概览/明细/提现入口与状态差异 |
