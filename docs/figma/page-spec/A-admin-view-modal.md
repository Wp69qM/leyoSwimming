# 页面规格：A-管理员账号查看弹窗

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时必须按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../../docs/prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：[US-057 管理员管理管理员账号](../../../docs/stories/US-057-管理员-管理管理员账号/user-story.md)
> **对应 PRD**：§10.1 管理员后台登录与账号管理
> **版本**：v1.0 | **创建日期**：2026-08-08

***

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 管理员账号查看弹窗 |
| 页面名（Frame） | `A-管理员账号查看弹窗` |
| 平台 | Web 后台管理系统 |
| 页面类型 | 弹窗（Modal） |
| 弹窗宽度 | 按全局规范 |
| 弹窗最大高度 | 自适应 |
| 遮罩层 | — |
| 圆角 | — |
| 顶部标题栏 | 左侧标题「管理员详情」，右侧关闭 icon |

***


---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | G-管理员详情弹窗 | https://www.calicat.cn/design/2083742072257646592?node-id=627b4d6c-a708-4477-9494-e92d0fbae0aa |

---
## 2. 状态维度

| 维度 | 状态枚举 | 弹窗影响 |
|------|---------|---------|
| 当前登录管理员角色 | `super_admin` / `admin` | 底部「编辑」按钮显隐 |
| 目标管理员角色 | `super_admin` / `admin` | 角色 Tag 显示 |
| 目标账号状态 | 启用 / 禁用 | 状态 Tag 显示 |
| 最近登录时间 | 有 / 无 | 显示具体时间或「从未登录」 |

***

## 3. 元素清单（自上而下）

### 3.1 标题栏

| 字段 | 值 |
|------|-----|
| 容器 | 卡片 |
| 标题 | 文字「管理员详情」 |
| 关闭按钮 | 关闭 icon |

### 3.2 信息卡

| 字段 | 值 |
|------|-----|
| 容器 | 卡片 |
| 管理员姓名 | — |
| 管理员 ID | — |
| 角色标签 | Tag：超级管理员 / 普通管理员 |
| 状态标签 | Tag：启用 / 禁用 |

### 3.3 基础信息分组

| 字段 | 说明 |
|------|------|
| 管理员 ID | 系统分配 |
| 姓名 | — |
| 登录账号 | `username` |
| 角色 | 超级管理员 / 普通管理员 |
| 状态 | 启用 / 禁用 |
| 最近登录时间 | YYYY-MM-DD HH:mm:ss 或「从未登录」 |
| 创建时间 | YYYY-MM-DD HH:mm:ss |
| 更新时间 | YYYY-MM-DD HH:mm:ss |

### 3.4 操作日志分组（最近 5 条）

| 字段 | 说明 |
|------|------|
| 操作时间 | YYYY-MM-DD HH:mm:ss |
| 操作人 | 操作者姓名 / 系统 |
| 操作类型 | 创建 / 编辑 / 禁用 / 启用 / 删除 / 重置密码 |
| 变更摘要 | 简短描述，如「角色由 admin 变更为 super_admin」 |

### 3.5 底部操作栏

| 字段 | 值 |
|------|-----|
| 容器 | 卡片 |
| 左侧按钮 | 关闭（默认按钮） |
| 右侧按钮 | 编辑资料（主按钮），仅 `super_admin` 查看他人账号时显示；查看自身账号或 `admin` 登录时不显示 |

***

## 4. 显示规则总表

| 元素/分组 | super_admin 登录 | admin 登录 |
|----------|:----------------:|:----------:|
| 基础信息分组 | ✅ | ✅ |
| 操作日志分组 | ✅ | ✅（仅展示与自身相关的日志） |
| 编辑资料按钮 | ✅ | ❌ |

> 具体以接口返回的 `allowed_actions` 为准。
