# 页面规格：U-用户须知页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时必须按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../../docs/prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-009（隐私协议与用户须知授权）
> **对应 PRD**：§11.3.3《用户须知》、§5.2.2 账号安全
> **版本**：v1.0 | **创建日期**：2026-08-08

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 用户须知页 |
| 页面名（Frame） | `U-用户须知页` |
| 平台 | 微信小程序 - 用户端 / 教练端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **否**（设置二级页） |
| 是否含 NavBar | **是**，标题「用户须知」 |
| 返回按钮 | 是（返回「我的 → 设置」） |

> **特别说明**：本页为纯内容查看页，展示当前生效的《用户须知》版本内容；页面不提供「撤回授权」按钮，也不提供编辑能力。

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 说明 | Calicat 中未找到独立 U-用户须知页 frame，目前复用 U-协议与须知弹窗浮层-用户须知（55c3468a-a193-4aba-bbda-567b8c3a3b0e） | https://www.calicat.cn/design/2083742072257646592?node-id=55c3468a-a193-4aba-bbda-567b8c3a3b0e |

---

## 2. 用户身份与业务状态维度

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | ❌ | 必须登录后通过「我的 → 设置」进入 |
| 注册用户 | ✅ | 可从「我的 → 设置 → 用户须知」进入 |
| 学员 | ✅ | 与注册用户一致 |
| 教练 | ✅ | 从教练端「我的 → 设置 → 用户须知」进入，内容一致 |
| 管理员 | ❌ | 管理端通过 Web 后台管理协议内容 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| **内容加载中** | 调用 `/api/terms-policy/current` 未返回 | 内容区展示骨架屏或 loading 指示器 |
| **内容加载成功** | 返回当前生效《用户须知》 | 展示版本号与完整内容 |
| **内容加载失败** | 网络异常或接口报错 | 展示错误提示与「重试」按钮 |
| **内容为空** | 系统未配置当前生效版本 | 展示空态提示「暂无内容」 |

---

## 3. 元素清单（自上而下）

### 3.1 状态栏占位

| 字段 | 值 |
|------|-----|
| 名称 | 状态栏占位 |
| 尺寸 | 375 × 44 px |
| 位置 | x=0, y=0 |
| 背景/颜色 | `#FFFFFF`，无额外内容 |
| 显示条件 | 始终显示 |

### 3.2 NavBar

| 字段 | 值 |
|------|-----|
| 名称 | 顶部导航栏 |
| 尺寸 | 375 × 44 px |
| 位置 | 状态栏下方 |
| 背景 | `#FFFFFF` |
| 标题 | 16px Medium `color-text-primary` #262626，居中，文字「用户须知」 |
| 返回按钮 | 左侧 24×24 px 返回 icon，#262626，距左 16 px |
| 显示条件 | 始终显示 |
| 点击行为 | 返回按钮 → 返回「我的 → 设置」页 |

### 3.3 版本号标签

| 字段 | 值 |
|------|-----|
| 名称 | 版本号标签 |
| 尺寸 | 自适应宽度，高度 32 px |
| 位置 | 内容区顶部，距 NavBar 12 px，水平居中 |
| 背景 | `rgba(24, 144, 255, 0.1)` |
| 圆角 | `radius-pill` 999px |
| 内边距 | 8 px 16 px |
| 文案 | 12px Regular `color-primary` #1890FF，文字「版本：v2.0」 |
| 显示条件 | 内容加载成功后显示 |

### 3.4 协议内容区

| 字段 | 值 |
|------|-----|
| 名称 | 协议内容区 |
| 尺寸 | 宽度 343 px（左右边距 16 px），高度自适应 |
| 位置 | 版本号标签下方，间距 16 px，水平居中 |
| 背景 | `#FFFFFF` |
| 圆角 | `radius-md` 8px |
| 内边距 | 20 px |
| 主标题样式 | 20px Bold `color-text-primary` #262626，居中，段后间距 16 px |
| 章节标题样式 | 16px Medium `color-text-primary` #262626，段前间距 24 px，段后间距 8 px |
| 正文样式 | 14px Regular `color-text-secondary` #595959，行高 1.8 |
| 列表样式 | 14px Regular `color-text-secondary`，项目符号圆点 6px #595959，左侧缩进 16 px |
| 显示条件 | 内容加载成功后显示 |
| 滚动行为 | 页面整体垂直滚动 |

### 3.5 加载状态

| 字段 | 值 |
|------|-----|
| 名称 | 内容加载中 |
| 尺寸 | 宽度填满，高度 400 px |
| 位置 | 内容区居中 |
| 显示条件 | 协议内容 API 请求中 |
| 表现 | 骨架屏（3-4 行灰色占位块）或 32×32 px loading 指示器 + 14px #8C8C8C 文案「加载中…」 |

### 3.6 错误状态

| 字段 | 值 |
|------|-----|
| 名称 | 内容加载失败 |
| 尺寸 | 宽度填满，高度 400 px |
| 位置 | 内容区居中 |
| 显示条件 | 协议内容 API 失败 |
| 表现 | 64×64 px 错误插画占位 + 14px #8C8C8C 文案「内容加载失败，请重试」+ 下方主色「重试」按钮（343×48 px） |
| 点击行为 | 点击「重试」重新请求 `/api/terms-policy/current` |

### 3.7 空状态

| 字段 | 值 |
|------|-----|
| 名称 | 暂无内容 |
| 尺寸 | 宽度填满，高度 400 px |
| 位置 | 内容区居中 |
| 显示条件 | 系统未配置当前生效《用户须知》 |
| 表现 | 64×64 px 空状态插画占位 + 14px #8C8C8C 文案「暂无用户须知内容」 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 | 教练 | 管理员 |
|------|:---:|:---:|:---:|:---:|:---:|
| 状态栏占位 | ❌ | ✅ | ✅ | ✅ | ❌ |
| NavBar | ❌ | ✅ | ✅ | ✅ | ❌ |
| 版本号标签 | ❌ | ✅（加载成功后）| ✅ | ✅ | ❌ |
| 协议内容区 | ❌ | ✅（加载成功后）| ✅ | ✅ | ❌ |
| 加载状态 | ❌ | ✅（加载时）| ✅ | ✅ | ❌ |
| 错误状态 | ❌ | ✅（失败时）| ✅ | ✅ | ❌ |
| 空状态 | ❌ | ✅（无内容时）| ✅ | ✅ | ❌ |

---

## 5. 跳转动线矩阵

| 触发条件 | 注册用户 | 学员 | 教练 |
|---------|---------|------|------|
| 进入本页 | 从「我的 → 设置 → 用户须知」进入 | 从「我的 → 设置 → 用户须知」进入 | 从教练端「我的 → 设置 → 用户须知」进入 |
| 点击返回按钮 | → 「我的 → 设置」页 | → 「我的 → 设置」页 | → 教练端「我的 → 设置」页 |

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 系统未配置当前生效版本 | 展示空状态插画 + 「暂无用户须知内容」 |
| **加载中** | 页面首次进入或点击重试 | 内容区展示骨架屏或 loading 指示器 |
| **错误状态** | 网络异常或接口报错 | 展示错误插画 + 文案 + 「重试」按钮 |
| **成功状态** | 协议内容加载完成 | 展示版本号标签与完整协议内容 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 用户可在个人中心随时查看最新版《用户须知》 | PRD §11.3.3；US-009 §4.3 | §2.1 适用身份、§5 跳转动线矩阵 |
| 页面仅支持查看，不提供撤回授权 | US-009 §4.3 步骤 3 | 页面不含「撤回授权」按钮 |
| 协议内容由 US-047 后台管理 | US-009 §3 前置条件 | 本页只读，调用 `/api/terms-policy/current` |
| 协议内容加载失败支持重试 | US-009 §8.2 | §3.6 错误状态 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 页面背景使用 `#F5F7FA`
- [ ] 页面含 NavBar，标题「用户须知」，左侧返回按钮
- [ ] 页面**不含 TabBar**
- [ ] 内容区为白底卡片，圆角 8px，左右边距 16px
- [ ] 版本号标签使用主色浅底 + 主色文字，胶囊样式
- [ ] 协议标题 20px Bold 居中，章节标题 16px Medium，正文 14px Regular，行高 1.8
- [ ] 加载状态使用骨架屏或 loading 指示器
- [ ] 错误状态含错误插画、失败文案与「重试」按钮
- [ ] 空状态含空插画与「暂无用户须知内容」文案
- [ ] 已预留 4 态视觉占位：`U-用户须知页-empty`、`U-用户须知页-loading`、`U-用户须知页-error`、`U-用户须知页-success`
- [ ] 元素命名包含状态/身份后缀

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-08 | PM | 初版：定义用户须知查看页的基础结构、身份维度、元素清单、显示规则、四态设计与业务规则引用 |
