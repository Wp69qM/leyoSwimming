# 页面规格：C-微信授权页（教练端）

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-051（教练微信授权登录并进入教练端）/ US-054（教练手机号验证码登录）
> **对应 PRD**：§3.2 身份状态机、§5.2.1 注册登录、§6.2 教练状态机、§11.2 隐私合规、§13.3 MVP 页面
> **版本**：v1.0 | **创建日期**：2026-08-03

---

## 1. 页面基础

| 字段 | 值 |
| ------ | ----- |
| 页面名（中文） | 微信授权页（教练端） |
| 页面名（Frame） | `C-微信授权页` |
| 平台 | 微信小程序 - 教练端 |
| 是否含 TabBar | **否**（登录流程特殊页） |
| 是否含 NavBar | **否**（登录流程特殊页，无系统返回） |
| 返回按钮 | 否（iOS 侧滑返回由系统处理，页面内不显示返回按钮） |

> **特别说明**：教练端微信授权页为无登录态教练进入小程序的统一拦截页，属于登录流程特殊页面，不展示 NavBar/TabBar；页面以品牌展示为主，登录按钮。

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
| ---------- | --------------- | ------ |
| 主页面 | C-微信授权页 | https://www.calicat.cn/design/ea7ec847-47bc-492e-a4b0-d86d9190d702?node-id=c0a365bc-432b-4604-9781-4de3344f6da4 |

---

## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
| ------ | :-------: | --------- |
| 游客（未登录教练） | ✅ | 当前页面目标用户；展示完整授权引导与一键登录按钮 |
| 注册用户（无教练资质） | ✅ | 同游客处理，登录后按无 coach 记录跳转入驻资料页 |
| 已入驻教练 | ❌ | 已登录教练不应进入此页；若通过旧链接/分享进入，自动跳转 `C-教练端首页` |
| 学员 | ❌ | 学员使用用户端小程序登录流程 |
| 管理员 | ❌ | 管理端使用 Web 后台登录流程 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
| ------ | ------ | ----------- |
| **未发起授权** | 教练刚进入页面，尚未点击登录 | 展示品牌区 + 登录按钮，无 loading |
| **授权中** | 教练已点击「微信一键登录」，正在调用 `wx.login()` 或后端 `code2session` | 按钮进入 loading，显示「登录中…」文案，防止重复点击 |
| **首次登录（无 coach 记录）** | 微信返回新用户或仅有 user 记录，无 coach 记录 | 登录成功后跳转 `C-入驻资料页`（US-010） |
| **待审核（coach.status=0）** | 已提交入驻资料，等待管理员审核 | 登录成功后跳转 `C-等待审核页` |
| **已通过（coach.status=1）** | 入驻审核通过 | 登录成功后跳转 `C-教练端首页` |
| **已驳回（coach.status=2）** | 审核被拒绝，需重新提交 | 登录成功后跳转 `C-入驻资料页`（US-040，重新入驻状态），显示驳回原因 |
| **已离职（coach.status=3）** | 离职审批通过 | 登录成功后跳转 `C-入驻资料页`（US-040，重新入驻状态） |
| **申请离职中（coach.status=4）** | 已提交离职申请，待管理员审批 | 登录成功后跳转 `C-教练端首页`；「我的」页面提供「查看离职申请」入口，教练可主动进入 `C-离职处理中页` |
| **教练拒绝授权** | 教练在授权弹窗中点击「拒绝」 | 停留在本页，展示错误提示「需要微信授权才能登录」+ 「重新授权」按钮 |
| **微信接口失败** | `wx.login()` 失败或后端调用微信 `code2session` 失败 | 停留在本页，展示错误提示「微信服务暂时不可用，请稍后重试」 |
| **登录凭证失效** | 用户提交的 `code` 已过期或已被使用 | 停留在本页，展示错误提示「登录凭证已失效，请重新点击登录」 |
| **未同意隐私协议** | 教练完成微信授权但未同意当前隐私协议 | 跳转 `U-隐私协议页`（US-009），同意后再按 redirect_page 跳转 |

---

## 3. 元素清单（自上而下）

> **设计原则**
> 1. 每个元素必须说明：名称、显示条件、交互行为
> 2. 登录流程页不使用 TabBar/NavBar，保持全屏沉浸
> 3. 所有元素按 Calicat 设计稿摆放

### 3.1 状态栏占位

| 字段 | 值 |
| ------ | ----- |
| 名称 | 状态栏占位 |
| 显示条件 | 始终显示（系统状态栏区域，Figma 中仅作占位） |
| 点击行为 | 无 |

### 3.2 品牌 Logo 区

| 字段 | 值 |
| ------ | ----- |
| 名称 | 品牌 Logo |
| 显示条件 | 始终显示 |
| 点击行为 | 无 |

> **AI 占位说明**：Logo 实际图形由设计师后续替换；AI 生成时绘制游泳/水波图标占位，并在旁边标注 `Icon: logo-swimming`。
>

### 3.3 品牌名称

| 字段 | 值 |
| ------ | ----- |
| 名称 | 品牌名称 |
| 显示条件 | 始终显示 |
| 点击行为 | 无 |
| 文案 | **leyoSwimming** |

### 3.4 品牌 Slogan

| 字段 | 值 |
| ------ | ----- |
| 名称 | 品牌 Slogan |
| 显示条件 | 始终显示 |
| 点击行为 | 无 |
| 文案 | **专业教练，轻松开课** |
| 字数限制 | 建议不超过 14 个汉字，确保一行展示 |
| 换行规则 | slogan 较长时允许两行 |

### 3.5 登录引导文案

| 字段 | 值 |
| ------ | ----- |
| 名称 | 登录引导文案 |
| 显示条件 | 始终显示 |
| 点击行为 | 无 |
| 文案 | **微信一键登录，开启教练工作台** |

### 3.6 底部固定登录区

> **来源**：US-051 §14.1；PRD §5.2.1 / §6.2。
> 固定区承载主登录按钮、隐私协议勾选。

| 字段 | 值 |
| ------ | ----- |
| 名称 | 登录区 |

#### 3.6.1 隐私协议勾选区

| 字段 | 值 |
| ------ | ----- |
| 名称 | 隐私协议勾选区 |
| 复选框 | 未选中时显示未勾选框，选中时显示已勾选框 |
| 文案 | 「已阅读并同意《隐私协议》《用户须知》」 |
| 显示条件 | 始终显示；未勾选时提交登录需提示「请先阅读并同意协议」 |
| 点击行为 | 勾选框切换选中态；点击协议名 → `U-登录页协议浮层`（US-009） |

#### 3.6.2 微信一键登录按钮

| 字段 | 值 |
| ------ | ----- |
| 名称 | 微信一键登录按钮 |
| 文案 | **微信一键登录** |
| 显示条件 | 始终显示 |
| 点击行为 | 点击后按钮 loading，调用 `wx.login()`；未勾选协议时弹窗提示 |
| loading 态 | 按钮，文案改为「登录中…」，显示 loading 指示器 |
| 禁用态 | 未勾选协议时按钮禁用，点击提示「请先阅读并同意协议」 |

### 3.7 错误提示区（错误状态时显示）

| 字段 | 值 |
| ------ | ----- |
| 名称 | 错误提示区 |
| 文案 | 动态错误文案 |
| 显示条件 | 仅错误状态显示（拒绝授权 / 微信接口失败 / 凭证失效 / 网络超时） |
| 点击行为 | 无 |

### 3.8 重新授权按钮（拒绝授权错误状态时显示）

| 字段 | 值 |
| ------ | ----- |
| 名称 | 重新授权按钮 |
| 显示条件 | 仅「用户拒绝授权」错误态显示 |
| 点击行为 | 调用 `wx.openSetting()` 引导用户开启权限 |
| 文案 | **重新授权** |

---

## 4. 显示规则总表

| 元素 | 游客/无 coach 注册用户 | 已登录教练 |
| ------ | :---: | :---: |
| 状态栏占位 | ✅ | ❌ |
| 品牌 Logo | ✅ | ❌ |
| 品牌名称 | ✅ | ❌ |
| 品牌 Slogan | ✅ | ❌ |
| 登录引导文案 | ✅ | ❌ |
| 隐私协议勾选区 | ✅ | ❌ |
| 微信一键登录按钮 | ✅ | ❌ |
| 错误提示区 | ✅（错误时） | ❌ |
| 重新授权按钮 | ✅（拒绝授权时） | ❌ |

> **说明**：本页仅对「未登录或无教练资质的微信用户」展示；已登录教练若进入此页应自动跳转首页，不在本页停留。

---

## 5. 跳转动线矩阵

> 本页所有跳转由用户主动点击或登录成功后自动触发。教练端登录后的分流以 `coach_status` 为核心。

| 触发条件 | 目标页面 | 条件/说明 |
| --------- | --------- | ---------- |
| 点击「微信一键登录」且成功 + 无 coach 记录 | → `C-入驻资料页` | US-010 |
| 点击「微信一键登录」且成功 + `coach_status=0` | → `C-等待审核页` | 本 Page Spec 定义 |
| 点击「微信一键登录」且成功 + `coach_status=1` | → `C-教练端首页` | US-012 起 |
| 点击「微信一键登录」且成功 + `coach_status=2` | → `C-入驻资料页` | US-040，重新入驻状态，显示驳回原因 |
| 点击「微信一键登录」且成功 + `coach_status=3` | → `C-入驻资料页` | US-040，重新入驻状态 |
| 点击「微信一键登录」且成功 + `coach_status=4` | → `C-教练端首页` | 不强制跳转离职处理中页；「我的」页面提供「查看离职申请」入口，教练主动查看 |
| 点击《隐私协议》/《用户须知》 | → `U-登录页协议浮层` | US-009 |
| 用户拒绝授权后点击「重新授权」 | 调用 `wx.openSetting()` | — |
| 已登录教练通过旧链接进入 | — | 自动 → `C-教练端首页` |

> **登录成功后的回跳逻辑**：若教练从受登录限制的功能被引导至本页，登录成功后应返回原页面并刷新身份状态；若直接打开小程序进入本页，登录成功后按 `coach_status` 跳转对应页面。

### 5.1 跳转参数说明

| 参数 | 类型 | 来源 | 用途 |
| ------ | ------ | ------ | ------ |
| source_page | string | 拦截来源页 | 记录用户从哪个页面被引导至授权页，用于登录成功后回跳 |
| target_action | string | 拦截来源操作 | 如 `book_slot`，登录成功后继续该操作 |
| code | string | 微信 `wx.login()` | 用于后端换取 openid/session_key |
| coach_status | int/null | 后端返回 | 决定登录后跳转目标页 |
| redirect_page | string | 后端返回 | 枚举：coach_onboarding / coach_pending / coach_home / coach_rejected / coach_resigned；coach_resigning 不再作为登录后自动跳转目标 |

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
| ------ | ------ | ------ |
| **空状态** | 页面首次加载，未触发任何操作 | 与成功态一致，展示品牌元素 + 登录区；无错误提示 |
| **加载中** | 用户点击「微信一键登录」后，`wx.login()` 或后端接口处理中 | 品牌元素保留；登录按钮进入 loading 态，文案变为「登录中…」；隐私协议勾选区保持可见但不可交互 |
| **错误状态** | 用户拒绝授权 / 微信接口失败 / 登录凭证失效 / 网络超时 | 品牌元素保留；登录区显示错误提示区；拒绝授权时额外显示「重新授权」按钮；登录按钮恢复可点击 |
| **成功状态** | 微信授权成功且后端返回登录结果 | 页面保持品牌元素完整显示后离开并跳转目标页 |
| **协议未勾选状态** | 用户点击登录按钮但未勾选隐私协议 | 登录区显示「请先阅读并同意协议」提示，按钮不进入 loading |

### 6.1 页面滚动与留白规则

| 字段 | 值 |
| ------ | ----- |
| 名称 | 页面滚动与留白 |
| 品牌区 | — |
| 底部登录区 | — |
| 错误提示区 | — |
| 水平边距 | — |
| 滚动行为 | 页面内容不超过一屏，原则上不滚动；极小屏设备允许品牌区轻微上移 |

### 6.2 跨页面一致性说明

| 字段 | 值 |
| ------ | ------ |
| 与用户端登录页一致性 | 品牌区、登录区、按钮、隐私协议勾选区与用户端 `U-微信授权页` 保持一致 |
| 与入驻资料页一致性 | 登录成功后无 coach 记录跳转 `C-入驻资料页`，需保持一致 |
| 与等待审核页一致性 | `coach_status=0` 跳转等待审核页，该页同样使用品牌 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
| ------ | -------------- | ------------- |
| 支持微信授权登录，教练端独立入口 | PRD §5.2.1；US-051 §4.1 | §3.6.2 微信一键登录按钮 |
| 教练状态机：0 待审核 / 1 已通过 / 2 已驳回 / 3 已离职 / 4 申请离职中 | PRD §6.2；US-051 §5 | §2.2 关键业务状态、§5 跳转矩阵 |
| 登录成功后按 coach_status 自动分流 | US-051 §4.1 步骤 10 | §5 跳转矩阵 |
| 教练拒绝授权后停留在登录页并提示 | US-051 §4.2 分支 1 / §6.4 | §3.7 错误提示区、§3.8 重新授权按钮 |
| 微信 `code2session` 失败提示「微信服务暂时不可用」 | US-051 §4.2 分支 2 / §6.4 | §3.7 错误提示区 |
| 登录凭证失效提示重新点击登录 | US-051 §4.2 分支 3 / §6.4 | §3.7 错误提示区 |
| 首次注册/登录需用户同意隐私协议 | PRD §11.2；US-009 §4.1 | §3.6.1 隐私协议勾选区 |
| 同一微信账号用户端与教练端共用 user 记录 | US-051 §4.2 分支 5 | §2.1 适用身份 |
| 登录页属于 MVP 核心页面 | PRD §13.3 | §1 页面基础 |

---

## 8. 关联页面：C-等待审核页

> 本页为 US-051 登录分流的目标页之一，建议与 `C-微信授权页` 同批次生成。

### 8.1 页面基础

| 字段 | 值 |
| ------ | ----- |
| 页面名（中文） | 等待审核页 |
| 页面名（Frame） | `C-等待审核页` |
| 平台 | 微信小程序 - 教练端 |
| 是否含 TabBar | **否** |
| 是否含 NavBar | **否** |

### 8.2 元素清单

| 元素 | 文案 |
| ------ | ------ |
| 状态栏占位 | — |
| 品牌 Logo | — |
| 主标题 | **资料审核中** |
| 副标题 | **我们会在 1-3 个工作日内完成审核，请耐心等待** |
| 提示卡片 | 包含「审核期间无法接单」「审核结果将通过微信服务通知推送」两条提示 |
| 返回首页/重新登录按钮 | **退出登录** |

### 8.3 四态

- **空状态**：不适用，本页为结果页，主状态即空状态（无额外操作）。
- **加载状态**：页面加载审核状态时使用骨架屏或全屏 loading。
- **错误状态**：获取审核状态失败时显示「网络异常，请重试」+ 重试按钮。
- **成功状态**：审核通过时自动跳转 `C-教练端首页`。

---

## 9. 关联页面：C-离职处理中页

> 本页由 US-039 定义，用于 `coach_status=4`（申请离职中）教练主动查看离职进度；不再是 US-051 登录后的自动跳转目标，登录后统一进入 `C-教练端首页`。

### 9.1 页面基础

| 字段 | 值 |
| ------ | ----- |
| 页面名（中文） | 离职处理中页 |
| 页面名（Frame） | `C-离职处理中页` |
| 平台 | 微信小程序 - 教练端 |
| 是否含 TabBar | **否** |
| 是否含 NavBar | **否** |

### 9.2 元素清单

| 元素 | 文案 |
| ------ | ------ |
| 状态栏占位 | — |
| 品牌 Logo | — |
| 主标题 | **离职申请处理中** |
| 副标题 | **部分功能受限，管理员完成审批后将通知你** |
| 提示卡片 | 包含「仍可继续上课」「不可提交新请假」「不可修改主页信息」等提示 |
| 工单入口 | **查看离职进度** → `C-离职工单处理页` |
| 返回首页/重新登录按钮 | **退出登录** |

### 9.3 四态

- **空状态**：不适用，本页为结果页。
- **加载状态**：获取离职工单状态时使用骨架屏。
- **错误状态**：获取失败时显示「网络异常，请重试」+ 重试按钮。
- **成功状态**：管理员审批通过后自动跳转 `C-入驻资料页`（status=3，重新入驻状态）。

---

## 10. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已读取教练端跨批次全局设计原则（`CROSS-BATCH-PRINCIPLES-COACH.md`）
- [ ] 已按 §2 身份维度确认本页仅对「未登录/无教练资质用户」展示完整元素

- [ ] 页面**不含 NavBar 与 TabBar**

- [ ] 品牌 Logo 为游泳/水波图标占位
- [ ] 品牌名称为 "leyoSwimming"
- [ ] Slogan 文案正确展示
- [ ] 登录区为卡片

- [ ] 隐私协议勾选区包含《隐私协议》《用户须知》可点击协议名
- [ ] 未勾选协议时点击登录按钮需提示「请先阅读并同意协议」
- [ ] 错误提示区覆盖常见错误状态
- [ ] 拒绝授权错误态额外显示「重新授权」按钮
- [ ] 已按 §5 跳转动线矩阵实现登录成功后的 coach_status 分流（6 种目标页）
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位：`C-微信授权页-empty`、`C-微信授权页-loading`、`C-微信授权页-error`、`C-微信授权页-success`
- [ ] 元素命名包含状态/身份后缀（如 `Button-微信一键登录-始终显示`、`ErrorTip-拒绝授权-仅错误态`）

- [ ] 已生成关联页面 `C-等待审核页` 和 `C-离职处理中页` 及其四态副本
- [ ] 未遗漏任何跨页面联动规则（登录后回跳原页面、按 coach_status 跳转）
- [ ] 品牌 Logo 占位已标注 `Icon: logo-swimming`，便于设计师替换
- [ ] 微信一键登录按钮 loading 态文案为「登录中…」
- [ ] 错误提示区动态文案覆盖：拒绝授权 / 微信服务不可用 / 凭证失效 / 网络超时

---

## 11. 变更日志

| 版本 | 日期 | 作者 | 变更 |
| ------ | ------ | ------ | ------ |
| v1.0 | 2026-08-03 | PM | 初版：覆盖教练端微信授权页、等待审核页、离职处理中页的页面基础、身份维度、元素清单、显示规则、登录成功分流、跳转参数、四态设计、业务规则引用与 AI 检查清单 |

> **AI 生成备注**：授权页为小程序教练旅程第一站，必须保证品牌感强、登录路径单一、错误状态明确、教练状态分流清晰。
>
>
> **验收红线**：若 AI 生成的授权页出现 NavBar/TabBar、或已登录教练仍可看到完整登录按钮、或未按 coach_status 分流，视为不合格。
>
> **关联页面**：`C-入驻资料页`、`C-等待审核页`、`C-教练端首页`、`C-离职处理中页`、`C-离职工单处理页`、`U-登录页协议浮层`。
