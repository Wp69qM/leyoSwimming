# 页面规格：C-教练中心页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-012 / US-039 / US-040
> **对应 PRD**：§5.4.1 入驻与资料、§5.4.7 申请离职、§5.4.8 重新入驻
> **版本**：v1.0 | **创建日期**：2026-08-02

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 教练中心页 |
| 页面名（Frame） | `C-教练中心页` |
| 平台 | 微信小程序 - 教练端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **是**（当前选中「我的」） |
| 是否含 NavBar | **是**（渐变背景，标题「我的」） |
| 返回按钮 | 否（Tab 首页） |

---

## 2. 教练状态维度

### 2.1 适用状态

| 状态 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 待审核（0） | 是 | 仅显示入驻状态提示与入驻资料入口 |
| 已通过（1） | 是 | 完整功能入口 |
| 已驳回（2） | 是 | 顶部显示红色驳回原因条，入口去入驻资料页 |
| 已离职（3） | 是 | **唯一入口**，仅显示已离职状态与「重新入驻」入口 |
| 申请离职中（4） | 是 | 完整入口，顶部增加「离职申请中」提示条 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| 资料审核中（0） | 已提交入驻资料 | 隐藏收入/排班等功能入口 |
| 入驻被驳回（2） | 审核未通过 | 显示驳回原因，提供修改入口 |
| 在职（1） | 审核通过 | 展示全部功能入口 |
| 申请离职中（4） | 已提交离职申请 | 显示离职进度入口，功能仍可用 |
| 已离职（3） | 离职审批通过 | 功能锁定，仅保留重新入驻 |

---

## 3. 元素清单（自上而下）

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 名称 | 状态栏 + 导航栏 |
| 尺寸 | 状态栏 375×44；NavBar 375×44 |
| 位置 | y=0 / y=44 |
| 背景/颜色 | `gradient-primary`（#1890FF → #0050B3） |
| 圆角 | 无 |
| 字体 | 标题「我的」17px Medium `#FFFFFF` |
| 显示条件 | 全部教练状态 |
| 点击行为 | 右侧设置图标：弹出通用设置菜单（本 MVP 无独立设置页） |

### 3.2 教练资料头图卡片

| 字段 | 值 |
|------|-----|
| 名称 | 教练资料头图 |
| 尺寸 | 343×120 |
| 位置 | x=16，y=96 |
| 背景/颜色 | `#FFFFFF`，`shadow-card` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 姓名 20px Medium `#262626`；状态标签 12px Regular |
| 显示条件 | 全部教练状态 |
| 点击行为 | 整卡点击 → `C-个人主页编辑页` |

#### 3.2.1 个人形象照

| 字段 | 值 |
|------|-----|
| 名称 | 个人形象照 |
| 尺寸 | 64×64 |
| 位置 | x=16，y=28（相对于卡片） |
| 背景/颜色 | `#E6F7FF`（primary 浅色） |
| 圆角 | 50% |
| 字体 | 无图时首字符 24px Bold `#FFFFFF` |
| 显示条件 | 全部教练状态 |
| 点击行为 | 同整卡跳转 |

#### 3.2.2 状态标签

| 字段 | 值 |
|------|-----|
| 名称 | 教练状态标签 |
| 尺寸 | 自适应，高 22px |
| 位置 | x=92，y=64（相对于卡片） |
| 背景/颜色 | 待审核/申请离职中 `#FFFBE6` 文字 `#FAAD14`；在职 `#F6FFED` 文字 `#52C41A`；已驳回/已离职 `#FFF1F0` 文字 `#FF4D4F` |
| 圆角 | `radius-pill`（999px） |
| 字体 | 12px Regular |
| 显示条件 | 按当前 coach.status |
| 点击行为 | 无 |

### 3.3 状态提示卡（按状态条件显示）

| 字段 | 值 |
|------|-----|
| 名称 | 状态提示卡 |
| 尺寸 | 343×80 |
| 位置 | x=16，y=224 |
| 背景/颜色 | status=0 `#E6F7FF`；status=2 `#FFF1F0`；status=3 `#FFF1F0`；status=4 `#FFFBE6` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 标题 14px Bold；正文 12px Regular `#262626` |
| 显示条件 | status=0/2/3/4 显示，status=1 不显示 |
| 点击行为 | status=4 时「查看进度」文字链 → `C-离职工单处理页` |

#### 状态提示卡文案

| 状态 | 标题 | 正文 |
|------|------|------|
| 0 待审核 | 资料审核中 | 审核通常需 1-3 个工作日 |
| 2 已驳回 | 入驻申请被驳回 | 原因：{rejection_reason} |
| 3 已离职 | 已离职 | 教学资格已冻结，仅可进行重新入驻 |
| 4 申请离职中 | 离职申请中 | 学员端购买入口已隐藏，可继续上课 |

### 3.4 功能入口列表

| 字段 | 值 |
|------|-----|
| 名称 | 功能入口列表卡片 |
| 尺寸 | 343×动态（每项 56px 高） |
| 位置 | x=16，y=状态提示卡下方 16px；无提示卡时 y=224 |
| 背景/颜色 | `#FFFFFF`，`shadow-card` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 入口标题 16px Regular `#262626`；右侧值 14px Regular `#8C8C8C` |
| 显示条件 | 按 §4 显示矩阵 |
| 点击行为 | 各项分别跳转，见 §5 |

#### 3.4.1 个人主页编辑入口

| 字段 | 值 |
|------|-----|
| 名称 | 个人主页编辑入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第一项 |
| 背景/颜色 | 透明；左侧图标 24×24 `#8C8C8C`；右侧箭头 16×16 `#BFBFBF` |
| 圆角 | 无 |
| 字体 | 16px Regular `#262626` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | → `C-个人主页编辑页` |

#### 3.4.2 参考单价设置入口

| 字段 | 值 |
|------|-----|
| 名称 | 参考单价设置入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第二项 |
| 背景/颜色 | 同 3.4.1 |
| 圆角 | 无 |
| 字体 | 16px Regular `#262626`；右侧值 14px `#8C8C8C` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | → `C-参考单价设置页` |

#### 3.4.3 我的收入入口

| 字段 | 值 |
|------|-----|
| 名称 | 我的收入入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第三项 |
| 背景/颜色 | 同 3.4.1 |
| 圆角 | 无 |
| 字体 | 16px Regular `#262626` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | → `C-教练收入页` |

#### 3.4.4 排班管理入口

| 字段 | 值 |
|------|-----|
| 名称 | 排班管理入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第四项 |
| 背景/颜色 | 同 3.4.1 |
| 圆角 | 无 |
| 字体 | 16px Regular `#262626` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | → `C-排班管理页` |

#### 3.4.5 请假申请入口

| 字段 | 值 |
|------|-----|
| 名称 | 请假申请入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第五项 |
| 背景/颜色 | 同 3.4.1 |
| 圆角 | 无 |
| 字体 | 16px Regular `#262626` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | → `C-请假申请页` |

#### 3.4.6 申请离职入口

| 字段 | 值 |
|------|-----|
| 名称 | 申请离职入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第六项 |
| 背景/颜色 | 同 3.4.1；文字色 `#FF4D4F` |
| 圆角 | 无 |
| 字体 | 16px Regular `#FF4D4F` |
| 显示条件 | status=1 / status=4 |
| 点击行为 | → `C-离职申请页` |

#### 3.4.7 重新入驻入口

| 字段 | 值 |
|------|-----|
| 名称 | 重新入驻入口 |
| 尺寸 | 343×56 |
| 位置 | 列表唯一项（status=3） |
| 背景/颜色 | `#FFFFFF`，`shadow-card`；文字/图标 `#1890FF` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 16px Medium `#1890FF` |
| 显示条件 | status=3 |
| 点击行为 | → `C-重新入驻页` |

#### 3.4.8 入驻资料入口

| 字段 | 值 |
|------|-----|
| 名称 | 入驻资料入口 |
| 尺寸 | 343×56 |
| 位置 | 列表第一项（status=0/2） |
| 背景/颜色 | 同 3.4.1 |
| 圆角 | 无 |
| 字体 | 16px Regular `#262626` |
| 显示条件 | status=0 / status=2 |
| 点击行为 | → `C-入驻资料填写页` |

### 3.5 底部 TabBar

| 字段 | 值 |
|------|-----|
| 名称 | 底部 TabBar |
| 尺寸 | 375×83 |
| 位置 | y=729 |
| 背景/颜色 | `#FFFFFF` + 顶部 1px `#F0F0F0` + `shadow-float` |
| 圆角 | 无 |
| 字体 | 10px Regular；选中 `#1890FF`，未选中 `#8C8C8C` |
| 显示条件 | 全部教练状态 |
| 点击行为 | 首页 → `C-教练端首页`；预约 → `C-预约管理页`；学员 → `C-我的学员列表页`；我的 → 本页 |

---

## 4. 显示规则总表

| 元素 | 待审核（0） | 已通过（1） | 已驳回（2） | 已离职（3） | 申请离职中（4） |
|------|:-----------:|:-----------:|:-----------:|:-----------:|:----------------:|
| 资料头图卡片 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 状态提示卡 | ✅ 审核中 | ❌ | ✅ 驳回 | ✅ 已离职 | ✅ 离职中 |
| 个人主页编辑入口 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 参考单价入口 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 我的收入入口 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 排班管理入口 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 请假申请入口 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 申请离职入口 | ❌ | ✅ | ❌ | ❌ | ✅ |
| 重新入驻入口 | ❌ | ❌ | ❌ | ✅ | ❌ |
| 入驻资料入口 | ✅ | ❌ | ✅ | ❌ | ❌ |
| 底部 TabBar | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 5. 跳转动线矩阵

| 起点元素 | 目标页面 |
|---------|----------|
| 资料头图卡片 | → `C-个人主页编辑页` |
| 个人主页编辑入口 | → `C-个人主页编辑页` |
| 参考单价入口 | → `C-参考单价设置页` |
| 我的收入入口 | → `C-教练收入页` |
| 排班管理入口 | → `C-排班管理页` |
| 请假申请入口 | → `C-请假申请页` |
| 申请离职入口 | → `C-离职申请页` |
| 重新入驻入口 | → `C-重新入驻页` |
| 入驻资料入口 | → `C-入驻资料填写页` |
| status=4 提示卡「查看进度」 | → `C-离职工单处理页` |
| Tab「首页」 | → `C-教练端首页` |
| Tab「预约」 | → `C-预约管理页` |
| Tab「学员」 | → `C-我的学员列表页` |

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| 空状态 | 教练资料字段为空 | 头图卡片显示默认个人形象照占位 + 「未设置昵称」 |
| 加载中 | 进入页面时拉取 coach 信息 | 头图卡片与功能列表显示骨架屏 |
| 错误状态 | 网络/服务异常 | 中央错误插画 + 「加载失败，点击重试」 |
| 成功状态 | 数据正常返回 | 按 §4 矩阵完整显示 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 教练端 TabBar 固定为 4 个 Tab | `CROSS-BATCH-PRINCIPLES-COACH.md` §7.1 | §3.5 |
| 不同教练状态决定页面可见功能 | `CROSS-BATCH-PRINCIPLES-COACH.md` §1.2 | §2 / §4 |
| 已离职教练仅保留重新入驻入口 | `CROSS-BATCH-PRINCIPLES-COACH.md` §6.2 | §3.4.7 / §4 |
| 申请离职中功能仍可用但显示进度入口 | `CROSS-BATCH-PRINCIPLES-COACH.md` §1.2 / US-039 | §3.3 / §3.4.6 |
| 教练可管理个人主页与参考单价 | US-012 §3 / §5 | §3.4.1 / §3.4.2 |
| 已驳回教练可修改资料重新提交 | US-010 §4.2 | §3.3 / §3.4.8 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档与 `CROSS-BATCH-PRINCIPLES-COACH.md`
- [ ] 已读取 v1.4 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 5 种教练状态定义 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/阴影严格引用 Token
- [ ] `C-教练中心页` Frame 名与页面名一致
- [ ] 已离职状态仅显示重新入驻入口
- [ ] 申请离职中状态顶部显示提示条与进度入口
- [ ] 跳转动线只引用已存在的 C- 页面
- [ ] 已按 §6 预留 4 态视觉占位
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：定义教练中心页（Tab「我的」）结构、5 种状态差异与跳转 |
