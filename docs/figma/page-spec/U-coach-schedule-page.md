# 页面规格：U-教练排班页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-003（释放倒计时）/ US-014（教练管理可约时段，数据依赖）/ US-018（体验课学员预约与取消）/ US-020（购买正价套餐时选时段）/ US-023（学员候补与关注时段）/ US-029（学员预约正价课程）
> **对应 PRD**：§5.3.2 预约功能、§5.4.2 教练预约管理、§6.1 预约状态机、§6.3.1 预占规则、§13.3 MVP 核心页面清单（页面 8 / 13）
> **版本**：v1.0 | **创建日期**：2026-08-02
> **特别说明**：本页是用户端最核心的约课入口，必须严格使用 2D 周×时段网格，禁止做成纵向卡片列表。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 教练排班页 |
| 页面名（Frame） | `U-教练排班页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 标准小程序页面尺寸 |
| 页面背景 | 按全局规范页面背景色 |
| 是否含 TabBar | **否**（二级页面，仅含 NavBar 返回） |
| 是否含 NavBar | **是**（白色背景，标题居中） |
| 返回按钮 | **是** |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | U-教练排班页 2 | https://www.calicat.cn/design/2083742072257646592?node-id=1e176c8a-b72e-4ab2-8b6e-3db9cea03f00 |

---
## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [US-018](../../../docs/stories/US-018-体验课学员-预约与取消/user-story.md) / [US-029](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| **游客** | 是 | 仅浏览，不可预约；点击格子/按钮引导登录 |
| **注册用户（无套餐）** | 是 | 仅浏览，不可预约；点击可约格子提示购买套餐 |
| **体验课学员** | 是 | 可预约标记为「体验课」的可约时段（`course_type = 0`） |
| **正价课学员（已绑定本教练）** | 是 | 可预约本教练正价时段（`course_type = 1`），是主预约路径 |
| **正价课学员（绑定其他教练）** | 是 | 可浏览但不可预约本教练；底部常驻提示条 + 禁用确认按钮 |
| **教练/管理员（以用户身份进入）** | 是 | 按其实际用户身份（游客/注册用户/学员）处理，不显示管理态 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `slot_status = available` | 教练已释放且无人预约 | 格子显示「可约」态，可点击 |
| `slot_status = booked_by_me` | 当前用户已预约该时段 | 格子显示「我的」态，高亮 |
| `slot_status = full` | 已被他人预约或锁定 | 格子显示「已满」态；若允许候补则叠加「候补」入口 |
| `slot_status = closed` | 教练请假 / 闭馆 / 未释放 | 格子显示「休息」态，不可点击 |
| `waitlist_open = true` | 满员但可排队候补 | 满员格显示橙色「候补 N」而非纯灰「已满」 |
| `release_countdown` | 处于释放倒计时窗口 | 顶部显示倒计时条，提示下周释放时间 |
| `coach_bound_mismatch` | 正价学员已绑定其他教练 | 底部提示条 + 确认按钮禁用 |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. **元素清单只描述「有什么元素」以及「元素之间的交互与状态」，不描述具体样式细节。** 颜色、宽度、高度、边距、背景、圆角、字体、字号、阴影等视觉参数**禁止**出现在 page-spec 中。
> 2. 视觉样式以 Calicat 设计稿为唯一最高优先级来源；page-spec 仅对交互逻辑、业务规则、状态流转负责。
> 3. 每个元素必须说明：名称、作用、显示条件、交互行为、关联业务规则。
> 4. 开发使用 rpx 适配设计稿基准宽度（仅作为工程说明，不作为视觉规格）。

### 3.1 状态栏 + NavBar

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 状态栏占位 | 为系统状态栏预留区域 | 始终显示 | 无 | 全局设计规范 |
| 导航栏 | 展示页面标题与返回入口 | 所有身份统一显示 | 点击返回按钮 → 上一页（教练详情 / 套餐详情 / 订单确认 / 我的预约等） | — |
| 页面标题 | 标识当前页面为“选择时段” | 始终显示 | 无 | — |
| 返回按钮 | 返回上一页 | 始终显示 | 点击返回上一页 | — |
| 右侧占位 | 保持视觉平衡 | 始终显示 | 无 | — |

### 3.2 教练信息卡（Coach Info Card）

> **来源**：[PRD §5.3.1 第 1 条](../../../docs/prd/prd.md) 教练详情页需展示头像、姓名、实时状态；本页作为约课入口需复用核心信息。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则/数据 |
|----------|------|----------|----------|-------------------|
| 教练信息卡容器 | 展示目标教练摘要 | 本页默认展示目标教练信息；从「预约」Tab 进入且已绑定教练时也展示该教练 | 点击卡片 → `U-教练详情页` | PRD §5.3.1 |
| 教练头像 | 展示教练个人形象照 | 始终显示 | 无 | `coach.avatar` |
| 教练姓名 | 展示教练姓名 | 始终显示 | 无 | `coach.name` |
| 状态徽标 | 展示教练实时状态：空闲中 / 上课中 / 休息中 / 已下班 / 请假中 | 始终显示 | 无 | PRD §5.3.1 |
| 副信息 | 展示教龄、评分等，如 "任教 X 年 · 评分 4.8" | 始终显示 | 无 | `coach` 相关字段 |
| 右侧箭头 | 提示可跳转教练详情 | 始终显示 | 无 | — |

### 3.3 释放倒计时条（Release Countdown Banner）

> **来源**：[US-003 §4.1](../../../docs/stories/US-003-游客-查看预约释放倒计时/user-story.md) / [PRD §5.3.2 第 3-4 条](../../../docs/prd/prd.md)。
> **规则**：仅在倒计时窗口内显示（释放前 24h 至释放当天 10:00 前）；非窗口期完全隐藏。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 倒计时条容器 | 提示预约释放窗口 | 游客/注册用户/学员均按 `release_rule` 与当前时间计算；非窗口期整栏隐藏 | 点击整条可展开释放规则说明弹窗（MVP 可选） | US-003 / PRD §5.3.2 |
| 倒计时图标 | 闹钟/时钟语义图标 | 窗口期显示 | 无 | — |
| 倒计时文案 | 窗口期文案二选一：①"距离下周预约开放还有 XX 小时"；②"今日 10:00 开放下周预约" | 窗口期显示 | 无 | PRD §5.3.2 |
| 倒计时数字 | 展示剩余小时数 | 文案①出现时显示 | 无 | — |
| 关闭按钮 | 允许用户临时关闭倒计时条 | 窗口期显示 | 点击后本次会话内不显示 | — |

### 3.4 周选择器（Week Selector）

> **来源**：[PRD §5.3.2 第 2 条](../../../docs/prd/prd.md) 可预约未来 7 天内课程；释放规则决定可选周。
> **设计决策**：默认展示「本周」与「下周」两个 Tab；若未来 7 天跨周，则下周 Tab 高亮可约日期。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 周选择器容器 | 切换本周/下周 | 所有身份统一显示 | 点击切换本周/下周，2D 网格内容同步切换 | PRD §5.3.2 第 2 条 |
| 本周 Tab | 展示本周可约日期 | 始终显示 | 点击切换到本周 | — |
| 下周 Tab | 展示下周可约日期 | 始终显示 | 点击切换到下周 | — |
| 当前周提示 | 展示选中周日期范围 | 选中周时显示 | 无 | — |

### 3.5 二维排班网格（2D Schedule Grid）

> **核心元素**。Y 轴为时段，X 轴为周一~周日，每个格子展示该日该时段的预约状态。
> **来源**：[PRD §5.3.2 第 8 条](../../../docs/prd/prd.md) 学员端预约卡片展示规则 / [US-029 §14.2](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。
> **硬性约束**：必须使用二维表格，禁止做成「日期 Tab + 纵向时段卡片列表」。

#### A. 网格整体结构

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 网格视口 | 承载 2D 周×时段表格 | 所有身份统一显示 | 水平/垂直滚动；日期列左右滑动时左侧时段列保持 sticky；上下滚动时表头保持 sticky | PRD §5.3.2 |
| 表头行 | 展示星期与日期 | 始终显示 | 可点击列头快速滚动到该列（可选） | — |
| 左侧时段列 | 展示起始时段 | 始终显示 | 保持 sticky 左侧 | — |
| 网格单元格 | 展示单一时段预约状态 | 每个时段对应一个单元格 | 点击触发对应操作 | 见 §3.5.E |

#### B. 表头行（Sticky Header Row）

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 左上角占位 | 与左侧时段列对齐 | 始终显示 | 无 | — |
| 日期列头 | 展示 "周一" ~ "周日" 与对应日期数字 | 7 列，始终显示 | 可选：点击快速滚动到该列 | — |
| 今天高亮 | 标识当天列 | 当天所在列 | 无 | — |
| 选中日期 | 当前查看周无额外选中态，周选择器已表达 | 始终显示 | 无 | — |

#### C. 左侧时段列（Sticky First Column）

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 时段行 | 展示起始时间，如 "07:00" | 15 个时段，后台可配置 | 无 | PRD §5.3.2 |

#### D. 网格单元格（Grid Cell）

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 单元格基础 | 展示单一时段状态文案/图标 | 每个时段一个 | 点击触发对应操作 | §3.5.E |

#### E. 单元格状态定义（6 种状态）

| 状态 | 视觉语义 | 点击行为 | 来源 |
|------|----------|---------|------|
| **可约 available** | 显示 "可约" | 点击选中，底部栏出现「确认预约」 | PRD §5.3.2 第 8 条 |
| **我的 my-booking** | 显示 "我的" | 点击可查看预约详情 / 取消入口 | US-029 §4.1 |
| **已满 full** | 显示 "已满" | 不可预约；若不允许候补则 toast「该时段已约满」 | PRD §5.3.2 第 8 条 |
| **休息 closed** | 显示 "休息" | 不可点击 | US-014 / PRD §5.4.5 |
| **候补 waitlist-available** | 显示 "候补 N" | 点击选中，底部栏出现「加入候补」 | US-023 §4.1 |
| **体验课体验可约 trial-available** | 显示 "体验" | 体验课学员可点击预约；正价学员点击提示"请购买体验课套餐" | US-018 §4.1 |

> **状态优先级**：当多个状态可能同时存在时，按以下优先级显示（高优先级覆盖低优先级）：
> 1. 我的预约（my-booking）
> 2. 休息（closed）
> 3. 候补（waitlist-available）
> 4. 已满（full）
> 5. 体验课可约（trial-available）
> 6. 可约（available）

#### F. 候补人数显示

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则/数据 |
|----------|------|----------|----------|-------------------|
| 候补人数 | 展示当前候补人数 | 仅 `waitlist_open = true` 且 N > 0 时显示；N = 0 时显示 "候补" 无数字 | 无 | `waitlist.count` |

#### G. 选中态（Selection State）

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 选中态指示 | 标识当前选中格子 | 用户点击可约/候补格时显示 | 同一时刻仅允许选中 1 个格子；切换格子时底部栏同步更新 | — |

### 3.6 图例说明（Legend）

> **来源**：提升用户识别效率，是 2D 网格的必要辅助。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 图例容器 | 说明网格各状态含义 | 所有身份统一显示；空状态时隐藏 | 无 | — |
| 图例项 | 每项包含色块占位 + 文字说明 | 始终显示（空状态隐藏） | 无 | — |
| 图例内容 | 可约 / 我的 / 已满 / 休息 / 候补 / 体验 | 始终显示 | 无 | — |

### 3.7 底部固定操作栏（Bottom Action Bar）

> **来源**：[US-018 §4.1](../../../docs/stories/US-018-体验课学员-预约与取消/user-story.md) / [US-029 §4.1](../../../docs/stories/US-029-学员-预约正价课程/user-story.md) / [PRD §5.3.2 第 5-6 条](../../../docs/prd/prd.md)。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 操作栏容器 | 承载底部信息与操作按钮 | 始终显示 | 无 | — |
| 选中时段信息 | 展示已选时段、教练/套餐提示 | 选中格子后显示 | 无 | — |
| 默认提示 | "请选择一个时段" | 未选中格子时显示 | 无 | — |
| 确认预约按钮 | 提交预约 | 选中可约格且课时充足/身份匹配时显示 | 点击 → 调用预约 API → 成功跳转 `U-预约成功页` | PRD §5.3.2 第 6 条 |
| 加入候补按钮 | 提交候补 | 选中候补格时显示 | 点击 → 调用候补 API → Toast "已加入候补" | US-023 |
| 禁用按钮 | 提示不可预约原因 | 课时不足/身份不匹配/已绑定其他教练时显示 | 无 | PRD §4.4 / §6.3.4 |
| 登录按钮 | 引导游客登录后预约 | 游客身份 | 点击 → 登录授权弹窗 / `U-微信授权页` | PRD §5.2.1 |

### 3.8 跨教练绑定警告条（Coach Mismatch Banner）

> **来源**：[PRD §4.4](../../../docs/prd/prd.md) 单教练约束 / [US-029 §4.2](../../../docs/stories/US-029-学员-预约正价课程/user-story.md) 异常分支 4。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 警告条容器 | 提示已绑定其他教练 | 仅当正价学员已绑定其他教练时显示 | 无 | PRD §4.4 |
| 警告图标 | 警告语义图标 | 显示警告条时 | 无 | — |
| 提示文案 | "你已绑定教练李教练，暂不可预约其他教练" | 显示警告条时 | 无 | US-029 §4.2 |
| 更换教练按钮 | 跳转换教练流程 | 显示警告条时 | 点击 → `U-换教练流程页`（US-022） | US-022 |

### 3.9 空状态 / 加载 / 错误占位

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 空状态 | 暂无可约时段 | 该教练当前周无可约时段 | 展示释放倒计时提示 | US-003 |
| 加载状态 | 网格加载中 | 首次进入 / 切换周 / 下拉刷新 | 无 | — |
| 错误状态 | 加载失败 | 网络断开 / 接口 5xx / 业务 4xx | 点击「重新加载」按钮重试 | — |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户（无套餐） | 体验课学员 | 正价学员（绑定本教练） | 正价学员（绑定其他教练） |
|------|:---:|:---:|:---:|:---:|:---:|
| NavBar 返回 + 标题 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 教练信息卡 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 释放倒计时条 | ✅（按窗口） | ✅（按窗口） | ✅（按窗口） | ✅（按窗口） | ✅（按窗口） |
| 周选择器 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 2D 排班网格 | ✅ 只读 | ✅ 只读 | ✅ 可预约体验课 | ✅ 可预约正价课 | ✅ 只读 |
| 图例 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 底部操作栏 | ✅（登录按钮） | ✅（购买提示） | ✅（确认预约/候补） | ✅（确认预约/候补） | ✅（禁用 + 换教练） |
| 跨教练警告条 | ❌ | ❌ | ❌ | ❌ | ✅ |
| 空/加载/错误占位 | ✅ | ✅ | ✅ | ✅ | ✅ |

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户（无套餐） | 体验课学员 | 正价学员（绑定本教练） | 正价学员（绑定其他教练） |
|---------|------|---------|---------|---------|---------|
| 返回按钮 | → 上一页 | → 上一页 | → 上一页 | → 上一页 | → 上一页 |
| 教练信息卡 | → `U-教练详情页` | → `U-教练详情页` | → `U-教练详情页` | → `U-教练详情页` | → `U-教练详情页` |
| 可约格子 | 引导登录 | → `U-套餐列表页`（提示购买） | → 选中态 + 确认预约 | → 选中态 + 确认预约 | Toast「已绑定其他教练」 |
| 我的预约格子 | 引导登录 | → `U-套餐列表页`（提示购买） | → `U-我的预约页` / `U-预约详情页` | → `U-我的预约页` / `U-预约详情页` | → `U-我的预约页` |
| 候补格子 | 引导登录 | 引导购买套餐 | → 选中态 + 加入候补 | → 选中态 + 加入候补 | Toast「已绑定其他教练」 |
| 确认预约按钮 | 引导登录 | → `U-套餐列表页` | → `U-预约成功页` | → `U-预约成功页` | 禁用 |
| 加入候补按钮 | 引导登录 | 引导购买套餐 | → `U-候补/关注页`（可选） | → `U-候补/关注页`（可选） | 禁用 |
| 更换教练文字按钮 | — | — | — | — | → `U-换教练流程页` |

> **"引导登录"**：当游客点击受登录限制的功能时，弹出登录授权弹窗或跳转 `U-微信授权页`，而不是直接提示错误。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 该教练当前周无可约时段（未释放或全部关闭） | 网格区域替换为 Empty 组件：游泳主题插画 + "暂无可约时段" + 释放倒计时提示（若处于窗口期） |
| **加载中** | 首次进入 / 切换周 / 下拉刷新 | 网格区域显示骨架屏占位，底部栏显示默认占位文字 |
| **错误状态** | 网络断开 / 接口 5xx / 业务 4xx | 中央错误插画 + "网络异常，请重试" 或 "服务繁忙，请稍后重试" + Primary「重新加载」按钮 |
| **成功状态** | 数据正常返回 | 按 §4 显示规则矩阵完整展示 2D 网格、图例、底部栏；选中态按用户操作实时更新 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 学员进入预约页默认显示已绑定教练的可约时间段 | PRD §5.3.2 第 1 条 | 教练信息卡默认展示目标教练 |
| 可预约未来 7 天内课程；每周三 10:00 释放下周时段 | PRD §5.3.2 第 2-3 条 | 周选择器本周/下周；倒计时条显示 |
| 同一时段 (coach_id, start_time) 唯一；多人并发仅第一个成功 | PRD §5.3.2 第 5 条 | 提交预约后锁单，失败时格子刷新为「已满」 |
| 正价套餐学员提交预约即成功并预占课时 | PRD §5.3.2 第 6 条 / §6.3.1 | 确认预约按钮直接创建 booking，不进入支付 |
| 已约满/锁定时段显示「已满」；支持候补/关注 | PRD §5.3.2 第 8 条 / 第 11-13 条 | 已满格与候补格的视觉区分 |
| 取消预约：24h 外直接释放，24h 内需教练审批 | PRD §5.3.2 第 9 条 / US-030 | 我的预约格跳转预约详情页处理取消 |
| 教练端预约卡片显示排队候补人数、关注用户数 | PRD §5.4.2 第 2 条 | 用户端候补格显示「N人候补」 |
| 预约状态机：无 → 已预约 / 候补中 | PRD §6.1 | 格子状态对应 booking/waitlist 状态 |
| 课时预占规则：预约 +1 reserved | PRD §6.3.1 | 确认预约后 package.reserved +1 |
| 单教练约束：正价学员同一时期只能绑定一个教练 | PRD §4.4 | 跨教练警告条 + 禁用预约 |
| 体验课学员需持有 active 体验套餐 | US-018 §3 / §4.3 | 无体验套餐时不可预约体验课格 |
| 正价学员需 active 套餐且 coach_id 匹配 | US-029 §3 / §4.2 | 套餐不匹配时禁用预约 |
| 满员时段可加入候补，多个候补中一个转正其他取消 | US-023 §4.1 / §5 | 候补格点击创建 waitlist |
| MVP 页面清单第 8 项「教练排班页」用于选时段 | PRD §13.3 | 本页 Frame 名与功能定位 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] **已创建真正的 2D 网格**：7 列日期 × 15 行时段，禁止做成列表或卡片堆叠
- [ ] 左侧时段列已设置为 sticky first column
- [ ] 顶部日期行已设置为 sticky header row
- [ ] 每个单元格状态文案按 §3.5.E 执行
- [ ] 可约格显示 "可约"
- [ ] 我的预约格显示 "我的"
- [ ] 已满格显示 "已满"
- [ ] 休息格显示 "休息"
- [ ] 候补格显示 "候补 N"
- [ ] 体验课可约格显示 "体验"
- [ ] 底部栏已包含选中时段信息 + 对应操作按钮
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含身份/状态后缀（如 `Cell-可约-体验课学员可用`、`Cell-已满-候补可点`）
- [ ] 未遗漏跨教练绑定警告条
- [ ] 未遗漏释放倒计时条动态显隐
- [ ] 未遗漏图例说明

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：针对用户最不满意的 2D 排班网格进行彻底规格化，明确 7 列日期 × 15 行时段、6 种状态、sticky 行列、身份差异、跳转矩阵、四态设计 |
