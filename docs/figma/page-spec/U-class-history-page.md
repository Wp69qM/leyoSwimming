# 页面规格：U-上课记录页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-032（学员签到课程）/ US-033（教练确认上课记录）/ US-034（管理员查看上课记录）/ US-035（管理员返还课时）
> **对应 PRD**：§5.3.2 预约功能、§5.3.3 课时与上课记录、§5.4.3 上课记录、§6.1 预约状态机、§13.3 MVP 核心页面清单（页面 20）
> **版本**：v1.0 | **创建日期**：2026-08-02
> **特别说明**：本页为二级列表页，聚合学员所有已完成/旷课的上课记录，按月份分组展示，支持查看单条记录详情与课后总结。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 上课记录页 |
| 页面名（Frame） | `U-上课记录页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **否**（二级页面） |
| 是否含 NavBar | **是**（白色背景，标题居中） |
| 返回按钮 | **是**（左侧 24×24px 返回箭头，左 padding 16px） |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | U-上课记录页 | https://www.calicat.cn/design/2083742072257646592?node-id=5e9905ab-9ace-4e92-bd62-88f93c1e4be9 |

---
## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [CROSS-BATCH-PRINCIPLES.md](../CROSS-BATCH-PRINCIPLES.md) §1。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | 否 | 直接访问时跳转微信授权页；从我的预约/个人中心进入时拦截 |
| 注册用户 | 否 | 无 active 套餐，无上课记录；访问时提示购买套餐 |
| 学员 | 是 | 本人所有已完成/旷课记录的聚合查看入口 |
| 教练 | 否 | 教练端使用教练端页面 |
| 管理员 | 否 | 管理端使用 Web 后台 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `booking.status = 已完成` | 教练已确认扣课时 | 显示在列表中，可查看教练记录与学员总结 |
| `booking.status = 旷课` | 学员未到场且教练标记 | 显示在列表中，展示旷课标签与教练备注 |
| `course_record.student_summary_json` 存在 | 学员已填写课后总结 | 记录卡片显示"已总结"标签 |
| `course_record.student_summary_json` 不存在 | 学员未填写课后总结 | 记录卡片显示"待总结"提示 |
| `admin_returned = true` | 管理员返还课时（US-035） | 记录卡片显示"课时已返还"标签 |
| 月份分组 | 按课程结束时间年月分组 | 列表顶部显示月份标题 |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为。
> 2. 所有尺寸以 375px 设计宽度为基准；开发使用 rpx（750rpx 基准）。
> 3. 颜色/字号/圆角/阴影必须引用 `docs/figma/README.md` §1 Token，禁止硬编码。

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，背景 `#FFFFFF`，y = 0 |
| NavBar | 375×44，背景 `#FFFFFF`，底部 1px `#F0F0F0`（`color-divider`），y = 44 |
| 左侧返回按钮 | 24×24 线性返回箭头，`color-text-primary` #262626，左 padding 16px，垂直居中 |
| 标题 | "上课记录"，17px / Medium（`font-size-body` + `font-weight-medium`），`color-text-primary`，水平居中 |
| 右侧占位 | 空（保持视觉平衡） |
| 显示条件 | 所有身份统一显示 |
| 点击行为 | 返回上一页（我的预约页 / 个人中心 / 预约详情页等） |

### 3.2 数据统计摘要区

> **来源**：PRD §5.3.3 查看历史上课记录；提升学员成就感。

| 字段 | 值 |
|------|-----|
| 容器 | 343×96，x = 16，y = 96（NavBar 底部 8px 间距），背景 `gradient-primary`，`radius-lg` 12px，`shadow-primary` |
| 左侧数据 | "累计上课" + "X 节"，上方 12px Regular 白色 80%，下方 28px Bold 白色 |
| 中间数据 | "累计课时" + "X 小时"，同上 |
| 右侧数据 | "本月上课" + "X 节"，同上 |
| 数据间距 | 三等分容器宽度，文字居中 |
| 显示条件 | 学员身份；有 ≥1 条已完成记录时显示 |
| 点击行为 | 无 |

### 3.3 筛选标签栏

> **来源**：PRD §5.3.3 / US-033。

| 字段 | 值 |
|------|-----|
| 容器 | 375×52，背景 `#FFFFFF`，y = 204（摘要区下方 12px），底部 1px `#F0F0F0` |
| Tab 区 | 三个胶囊 Tab："全部"、"已完成"、"旷课"，居中排列，间距 8px |
| 未选中 Tab | 高 32px，水平 padding 16px，背景 `#F5F5F5`，圆角 999px（`radius-pill`），文字 14px / Regular `color-text-secondary` |
| 选中 Tab | 高 32px，水平 padding 16px，背景 `#1890FF`，圆角 999px，文字 14px / Medium `#FFFFFF` |
| 显示条件 | 记录数量 ≥ 1 时显示 |
| 点击行为 | 切换筛选条件，列表按状态过滤 |

### 3.4 月份分组标题

| 字段 | 值 |
|------|-----|
| 容器 | 343×36，x = 16，y = 268（筛选栏下方 12px），背景透明 |
| 月份文字 | "2026年8月"，16px / Bold `color-text-primary`，左侧对齐 |
| 当月统计 | "X 节"，12px / Regular `color-text-secondary`，右侧对齐 |
| 显示条件 | 列表按课程结束时间倒序分组，每月一条标题 |

### 3.5 上课记录卡片（Class Record Card）

> **核心元素**。每个卡片展示一次已完成或旷课的上课记录。
> **来源**：PRD §5.3.3 / US-033 / US-032。

#### A. 卡片整体结构

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应（最小 112px），x = 16，y = 304（首个卡片），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 卡片间距 | 12px（垂直） |
| 内边距 | 16px |
| 可点击区域 | 整张卡片可点击 → 上课记录详情（本页展开或跳转 `U-预约详情页`） |
| hover/按下 | 阴影从 `shadow-card` 变为 `shadow-raised`，过渡 200ms |

#### B. 卡片头部行

| 字段 | 值 |
|------|-----|
| 日期块 | 48×56，背景 `color-primary` 浅色 #E6F7FF，圆角 8px，显示「08/02」14px Bold `color-primary` |
| 星期文字 | 日期块下方，"周五"，10px / Regular `color-text-secondary`，居中对齐 |
| 课程时间 | 日期块右侧 12px，"09:00-10:00"，16px / Medium `color-text-primary` |
| 状态徽标 | 右侧对齐，使用 Tag 组件；已完成用 `color-primary` 浅色底 + `color-primary` 字；旷课用 `color-error` 浅色底 + `color-error` 字 |
| 已总结标签 | 状态徽标左侧 8px，"已总结"，Success Tag 样式 |
| 课时返还标签 | 状态徽标左侧 8px（与已总结互斥），"已返还"，Warning Tag 样式 |

#### C. 教练与内容区

| 字段 | 值 |
|------|-----|
| 教练个人形象照 | 32×32 圆形，背景 `#E6F7FF`，位于卡片左侧 |
| 教练姓名 | "张教练"，14px / Medium `color-text-primary`，位于头像右侧 8px |
| 课程内容 | "自由泳打腿 · 换气练习"，12px / Regular `color-text-secondary`，位于姓名下方 2px；无内容时显示 "教练未填写内容" |
| 教练备注 | 旷课状态显示："备注：{reason}" 12px / Regular `color-error` |
| 显示条件 | 所有记录卡片统一显示 |

#### D. 课后总结区

| 字段 | 值 |
|------|-----|
| 容器 | 卡片底部，顶部 1px `#F0F0F0` 分隔线，padding-top 12px，与上方内容间距 12px |
| 总结标题 | "我的课后总结"，12px / Medium `color-text-secondary` |
| 总结内容 | 14px / Regular `color-text-primary`，最多 2 行；未填写时显示 "点击填写课后总结" `color-primary` |
| 编辑按钮 | 右侧 "编辑" 文字按钮 14px / Medium `color-primary` |
| 显示条件 | 已完成状态显示；旷课状态隐藏 |

#### E. 卡片状态变体

| 变体 | 触发条件 | 视觉差异 |
|------|---------|---------|
| 已完成-有总结 | booking.status = 已完成 且 course_record.student_summary_json 存在 | 显示 "已总结" Success Tag，课后总结区显示总结摘要 |
| 已完成-无总结 | booking.status = 已完成 且未填写总结 | 不显示 "已总结" Tag，课后总结区显示 "点击填写课后总结" |
| 已完成-课时已返还 | admin_returned = true | 额外显示 "已返还" Warning Tag，课时变动显示 "已返还 1 节" |
| 旷课-有备注 | booking.status = 旷课 且教练填写备注 | 显示 "旷课" Error Tag，展示教练备注 |
| 旷课-无备注 | booking.status = 旷课 且教练未填写备注 | 显示 "旷课" Error Tag，备注区显示 "教练未填写备注" |

### 3.6 上课记录详情展开区

> **来源**：US-032 / US-033。

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应，x = 16，y = （列表下方），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "课程详情"，16px / Medium `color-text-primary`，顶部 padding 16px |
| 教练记录 | 标题 "教练记录" 14px / Medium，内容 14px / Regular `color-text-secondary` |
| 重点标签 | 横向滚动，Neutral Tag 样式 |
| 课后作业 | "课后作业：{homework}" 14px / Regular `color-text-secondary` |
| 照片/视频 | 最多 4 个 72×72 圆角 8px 占位图 |
| 学员总结 | 标题 "我的课后总结" 14px / Medium，内容 14px / Regular `color-text-primary` |
| 编辑总结按钮 | Primary 按钮 "编辑课后总结" |
| 显示条件 | 点击卡片展开或跳转新页面；MVP 建议本页展开 |

### 3.7 加载更多与分页

| 字段 | 值 |
|------|-----|
| 分页参数 | 每页 15 条，按课程结束时间倒序 |
| 加载更多提示 | 列表底部 "加载中..." 12px `color-text-secondary`，左侧 16×16 loading 占位 |
| 无更多数据 | "没有更多了" 12px `color-text-secondary`，居中 |
| 触发条件 | 滚动到底部自动加载 |
| 显示条件 | 记录数量 > 15 时启用 |

### 3.8 数据摘要计算规则

| 字段 | 计算规则 | 显示位置 |
|------|---------|---------|
| 累计上课 | booking.status = 已完成 的记录总数 | 统计摘要区左侧 |
| 累计课时 | 已完成记录的课时总和（默认每节 1 小时） | 统计摘要区中间 |
| 本月上课 | 当月 1 日 00:00 至月末 23:59 内已完成的记录数 | 统计摘要区右侧 |
| 当月统计 | 每个月份分组内记录数量 | 月份分组标题右侧 |

#### 统计摘要区视觉细节

| 字段 | 值 |
|------|-----|
| 背景 | `gradient-primary`（#1890FF → #0050B3），`radius-lg` 12px，`shadow-primary` |
| 数据数字 | 28px / Bold `#FFFFFF` |
| 数据标签 | 12px / Regular `#FFFFFF` 80% 不透明度 |
| 分隔线 | 1px `#FFFFFF` 30% 不透明度，垂直分割三个数据区 |
| 装饰元素 | 右上角可选游泳主题水印图标（P3），MVP 不做 |
| 显示条件 | 学员有 ≥1 条已完成记录时显示；仅旷课记录时不显示 |

### 3.9 月份分组与排序规则

| 字段 | 值 |
|------|-----|
| 分组字段 | 按 booking 的课程结束时间所在年月分组 |
| 排序方向 | 月份倒序（最新月份在上），同月份内按课程结束时间倒序 |
| 分组标题 | "YYYY年M月"，16px / Bold `color-text-primary` |
| 当月统计 | 标题右侧 "X 节"，12px / Regular `color-text-secondary` |
| 跨月边界 | 同一月份内不分页，加载更多跨月时自动出现新的月份标题 |

### 3.10 空状态 / 加载 / 错误占位

| 字段 | 值 |
|------|-----|
| 空状态 | 内容区替换为 [README.md §2.5.9](../../../docs/figma/README.md) Empty 组件：游泳主题插画 + "暂无上课记录" 14px + "完成第一节课程后即可查看" + Primary「去预约」按钮 |
| 加载状态 | 列表区显示骨架屏：统计摘要区灰色占位 + 3 个 343×112 灰色矩形（`color-divider` #F0F0F0）脉冲动画，圆角 12px |
| 错误状态 | 中央错误插画 + "网络异常，请重试" + Primary「重新加载」按钮 |
| 显示条件 | 见 §6 四态设计 |

### 3.11 分享与导出（P3 预留）

> **来源**：PRD §5.3.3 / US-038 教练分享。

| 字段 | 值 |
|------|-----|
| 分享按钮 | NavBar 右侧可选 24×24 分享图标 `color-text-primary` |
| 分享内容 | 生成学习周报海报：累计上课数、本月上课数、教练评价摘要 |
| 导出入口 | 页面底部 "导出上课记录" 文字按钮 14px `color-primary` |
| 显示条件 | P3 功能，MVP 隐藏或置灰标注 "[P3]" |
| 点击行为 | P3 调用微信小程序分享 / 生成海报；MVP Toast "功能即将上线" |

### 3.12 页面骨架与 Auto Layout 要求

> **来源**：README.md §2.6.1 / §2.6.2。

| 字段 | 值 |
|------|-----|
| 页面 Frame | 375×812，背景 `#F5F7FA` |
| 状态栏 + NavBar | 88px 总高，背景 `#FFFFFF` |
| 内容区 | 可滚动，padding 16px，底部留白 24px（二级页面无 TabBar） |
| 统计摘要区 | 固定在 NavBar 下方，滚动时保持吸顶 |
| 筛选标签栏 | 吸顶于统计摘要区下方 |
| Auto Layout | 列表使用 VERTICAL 方向 Auto Layout；月份标题与卡片分组使用嵌套 Auto Layout |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 |
|------|:---:|:---:|:---:|
| NavBar 返回 | ✅ | ✅ | ✅ |
| 数据统计摘要区 | ❌ | ❌ | ✅（有记录时） |
| 筛选标签栏 | ❌ | ❌ | ✅（有记录时） |
| 月份分组标题 | ❌ | ❌ | ✅（有记录时） |
| 上课记录卡片 | ❌ | ❌ | ✅ |
| 已完成状态徽标 | ❌ | ❌ | ✅ |
| 旷课状态徽标 | ❌ | ❌ | ✅ |
| 已总结标签 | ❌ | ❌ | ✅（已填写总结） |
| 课时返还标签 | ❌ | ❌ | ✅（管理员返还后） |
| 课后总结区 | ❌ | ❌ | ✅（已完成状态） |
| 课程详情展开区 | ❌ | ❌ | ✅（点击卡片后） |
| 加载更多 | ❌ | ❌ | ✅（记录 > 15 条） |
| 数据统计摘要区 | ❌ | ❌ | ✅（有记录时） |
| 分享与导出 | ❌ | ❌ | ⚠️（P3 预留） |
| 页面骨架/Auto Layout | ❌ | ❌ | ✅ |
| 空/加载/错误占位 | 登录引导 | 购买引导 | ✅ |

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员 |
|---------|------|---------|------|
| 返回按钮 | → 上一页 | → 上一页 | → 上一页 |
| 上课记录卡片 | — | — | → 展开详情区 / `U-预约详情页`（携带 booking_id） |
| 教练姓名/头像 | — | — | → `U-教练详情页` |
| "填写课后总结" | — | — | → 课后总结表单（弹窗或新页面） |
| "编辑课后总结" | — | — | → 课后总结表单（携带现有内容） |
| 空状态「去预约」按钮 | — | — | → `U-教练排班页` |
| 旷课记录「联系客服」 | — | — | → `U-客服工单页`（US-049） |

> **参数说明**：跳转 `U-预约详情页` 携带 `booking_id={id}`、`from=class_history`。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 学员无已完成/旷课记录 | 内容区替换为 Empty 组件：游泳主题插画 + "暂无上课记录" + "完成第一节课程后即可查看" + Primary「去预约」按钮 |
| **加载中** | 首次进入 / 切换筛选 / 下拉刷新 | 列表区显示骨架屏（统计摘要占位 + 3 个记录卡片占位） |
| **错误状态** | 网络断开 / 接口 5xx / 业务 4xx | 中央错误插画 + "网络异常，请重试" 或 "服务繁忙，请稍后重试" + Primary「重新加载」按钮 |
| **成功状态** | 数据正常返回 | 按 §4 显示规则矩阵完整展示统计摘要、筛选标签、月份分组、记录卡片；支持展开详情 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 学员可查看历史上课记录：时间、教练、内容 | PRD §5.3.3 第 3 条 | §3.5 上课记录卡片 |
| 教练记录每节课时间、内容、学员学习情况 | PRD §5.4.3 / US-033 §4.1 | §3.6 课程详情展开区 |
| 每节课后填写自我总结：身体感受、学习效果、问题反馈 | US-032 §4.1 / PRD §5.3.3 | §3.5.D 课后总结区 / §3.6 |
| 教练标记旷课后系统自动扣课时 | US-033 §4.2 / PRD §5.3.2 第 10 条 | §3.5.B 旷课状态徽标 / §3.5.C 教练备注 |
| 管理员可手动返还课时 | US-035 / PRD §5.3.2 第 10 条 | §3.5.B 课时返还标签 |
| 预约状态机：已完成 / 旷课为终态 | PRD §6.1 | §2.2 状态定义 |
| 上课记录页为 MVP 页面清单第 20 项 | PRD §13.3 | §1 页面基础 |
| 游客/注册用户查看记录类页面需登录/购买 | CROSS-BATCH-PRINCIPLES §1.2 | §2.1 适用身份 / §5 跳转矩阵 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/边框严格按 §3 规格
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含显示条件后缀（如 `Card-已完成-有总结`、`Card-旷课-有备注`）
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] 统计摘要区使用 `gradient-primary`，数据三等分居中
- [ ] 月份分组标题按课程结束时间倒序排列
- [ ] 筛选标签「全部」「已完成」「旷课」切换正确过滤列表
- [ ] 记录卡片左侧日期块使用 `color-primary` 浅色背景
- [ ] 已完成与旷课状态徽标颜色正确区分
- [ ] 课后总结区仅在已完成卡片显示
- [ ] 课程详情展开区包含教练记录、重点标签、课后作业、学员总结
- [ ] 数据摘要计算规则正确（累计上课/课时/本月上课）
- [ ] 月份分组按课程结束时间倒序，同月份内倒序
- [ ] 加载更多分页每页 15 条
- [ ] 分享与导出按 P3 预留处理
- [ ] 页面骨架与 Auto Layout 符合 §3.12 规格
- [ ] 空状态包含「去预约」CTA 按钮
- [ ] 未遗漏跨页面联动规则（预约详情、教练详情、课后总结表单）

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：完整定义上课记录页，覆盖学员视角的已完成/旷课记录按月分组、统计摘要、筛选标签、课后总结与详情展开 |
