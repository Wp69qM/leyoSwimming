# 页面规格：U-预约成功页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-018 / US-029
> **对应 PRD**：§5.3.2 预约功能、§6.1 预约状态机、§6.3.1 预占规则、§7.2 通知场景、§13.3 MVP 页面清单第 14 项
> **版本**：v1.0 | **创建日期**：2026-08-02
> **特别说明**：预约成功页是约课闭环的终点，必须明确告知用户预约结果、展示预约卡片、提供日历同步入口，并以「完成」按钮结束流程；本页无返回按钮。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 预约成功页 |
| 页面名（Frame） | `U-预约成功页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 标准小程序页面尺寸 |
| 页面背景 | 按全局规范页面背景色 |
| 是否含 TabBar | **否** |
| 是否含 NavBar | **否**（结果页，无返回按钮） |
| 返回按钮 | **否**（预约流程终点，防止返回上一页造成误操作） |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | U-预约成功页 | https://www.calicat.cn/design/2083742072257646592?node-id=26395696-71b0-4c4f-8470-792a39d76772 |

---
## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [US-018](../../../docs/stories/US-018-体验课学员-预约与取消/user-story.md) / [US-029](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| **游客** | 否 | 预约成功后不会进入本页；游客前置被拦截 |
| **注册用户（无套餐）** | 否 | 无套餐无法完成预约，不会进入本页 |
| **体验课学员** | 是 | 展示体验课预约成功信息 |
| **正价课学员（已绑定本教练）** | 是 | 展示正价课预约成功信息 |
| **教练/管理员（以用户身份进入）** | 是 | 按其实际用户身份处理，不显示管理态 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `booking.status = 2` 已预约 | 预约提交成功 | 正常展示成功页 |
| `booking.course_type = 0` | 体验课 | 预约卡片显示「体验课」标签 |
| `booking.course_type = 1` | 正价课 | 预约卡片显示「正价课」标签 |
| `calendar_sync = true` | 用户已授权日历同步 | 「添加到日历」按钮显示为「已添加」或隐藏 |
| `calendar_sync = false` | 未同步日历 | 显示「添加到日历」按钮 |
| `first_booking` | 用户首次预约 | 展示额外欢迎文案 "恭喜您完成首次预约" |
| `release_countdown` | 处于释放倒计时窗口 | 显示"距离上课还有 X 天"提示 |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. **元素清单只描述「有什么元素」以及「元素之间的交互与状态」，不描述具体样式细节。** 颜色、宽度、高度、边距、背景、圆角、字体、字号、阴影等视觉参数**禁止**出现在 page-spec 中。
> 2. 视觉样式以 Calicat 设计稿为唯一最高优先级来源；page-spec 仅对交互逻辑、业务规则、状态流转负责。
> 3. 每个元素必须说明：名称、作用、显示条件、交互行为、关联业务规则。
> 4. 开发使用 rpx 适配设计稿基准宽度（仅作为工程说明，不作为视觉规格）。

### 3.1 成功状态区（Success Header）

> **来源**：[US-018 §4.1](../../../docs/stories/US-018-体验课学员-预约与取消/user-story.md) / [US-029 §4.1](../../../docs/stories/US-029-学员-预约正价课程/user-story.md) / [PRD §5.3.2 第 8 条](../../../docs/prd/prd.md)。
> 成功状态区是本页视觉焦点，使用大尺寸成功图标与明确文案。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 成功状态区容器 | 展示预约成功视觉焦点 | 预约成功状态统一显示 | 无 | US-018 / US-029 |
| 成功图标 | 对勾/成功语义图标 | 预约成功状态统一显示 | 无 | — |
| 主文案 | "预约成功" | 预约成功状态统一显示 | 无 | — |
| 副文案 | 展示预约时段与教练，如 "周三 08:00-09:00 · 张教练" | 预约成功状态统一显示 | 无 | `booking` / `coach` |
| 首次预约欢迎文案 | "恭喜您完成首次预约" | 仅 `first_booking = true` 时显示 | 无 | — |

### 3.2 预约信息卡（Booking Info Card）

> **来源**：[PRD §5.3.2 第 8 条](../../../docs/prd/prd.md) 学员端预约卡片展示规则 / [US-029 §4.1](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则/数据 |
|----------|------|----------|----------|-------------------|
| 预约信息卡容器 | 承载预约核心信息 | 所有成功状态统一显示 | 无 | — |
| 课程类型标签 | 体验课 = "体验课" 标签；正价课 = "正价课" 标签 | 所有成功状态统一显示 | 无 | `booking.course_type` |
| 状态徽标 | "已预约" 状态标签 | 所有成功状态统一显示 | 无 | PRD §6.1 |
| 时间大标题 | "08:00 - 09:00" | 所有成功状态统一显示 | 无 | `booking.start_time` / `booking.end_time` |
| 日期行 | "2026-08-05 周三" | 所有成功状态统一显示 | 无 | `booking.start_time` |
| 教练信息行 | 教练头像 + 姓名 + 任教年限 | 所有成功状态统一显示 | 无 | `coach` |
| 地点行 | 上课地点，如 "Leyo 游泳中心 · 1 号泳池" | 所有成功状态统一显示 | 无 | `venue` |
| 剩余课时行 | "预约后剩余 X 节课" | 所有成功状态统一显示 | 无 | `package.available_count` |
| 温馨提示 | "课前 24 小时外可随时取消" | 所有成功状态统一显示 | 无 | PRD §5.3.2 第 9 条 |

### 3.3 日历同步区（Calendar Sync Section）

> **来源**：[PRD §5.3.2 第 8 条](../../../docs/prd/prd.md) 支持日程提醒（可同步至手机日历）。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 日历同步区容器 | 提供课程添加到手机日历入口 | 所有成功状态显示 | 无 | PRD §5.3.2 第 8 条 |
| 日历图标 | 日历语义图标 | 所有成功状态显示 | 无 | — |
| 标题 | "添加到手机日历" | 所有成功状态显示 | 无 | — |
| 副标题 | "开课前一天提醒您" | 所有成功状态显示 | 无 | — |
| 开关 | 日历同步开关 | 所有成功状态显示 | 开启 → 调起系统日历授权 → 成功后显示"已添加"；失败时保持关闭并提示 | — |
| 已同步状态 | "已添加" | `calendar_sync = true` 时显示 | 无 | — |

### 3.4 下一步操作区（Next Actions）

> **来源**：预约闭环后的用户分流；跳转至「我的预约」或「首页」。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 下一步操作区容器 | 承载后续操作入口 | 所有成功状态统一显示 | 无 | — |
| 区标题 | "下一步" | 所有成功状态统一显示 | 无 | — |
| 查看预约入口 | 跳转我的预约页 | 所有成功状态统一显示 | 点击 → `U-我的预约页` | US-018 / US-029 |
| 再次预约入口 | 跳转预约页继续约课 | 所有成功状态统一显示 | 点击 → `U-预约页`（同教练） | US-018 / US-029 |

### 3.5 客服提示区（Support Notice）

> **来源**：[PRD §5.3.2 第 9 条](../../../docs/prd/prd.md) 取消预约规则 / [PRD §6.5.1](../../../docs/prd/prd.md) 取消时间窗。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 客服提示区容器 | 提示取消规则 | 所有成功状态显示 | 点击 → `U-预约详情页`（带 booking_id） | PRD §5.3.2 第 9 条 |
| 提示文案 | "如需取消，请在课前 24 小时外操作" | 所有成功状态显示 | 无 | PRD §6.5.1 |

### 3.6 底部固定完成栏（Bottom Complete Bar）

> **来源**：预约流程终点；全局规则要求结果页显示「完成」按钮。

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 完成栏容器 | 承载完成按钮 | 所有成功状态统一显示 | 无 | — |
| 完成按钮 | 结束预约流程 | 所有成功状态统一显示 | 点击 → `U-我的预约页`（默认）或返回首页（根据业务配置） | — |

### 3.7 空状态 / 加载 / 错误占位

| 元素名称 | 作用 | 显示条件 | 交互行为 | 关联业务规则 |
|----------|------|----------|----------|--------------|
| 空状态 | 无 booking_id 传入或预约不存在 | 无 booking 或不存在 | 点击 "返回首页" → `U-首页` | — |
| 加载状态 | 查询预约详情中 | 查询预约详情 / 等待预约创建回调 | 无 | — |
| 错误状态 | 网络/接口异常 | 网络断开 / 接口 5xx / 预约不存在 | 点击「重新加载」按钮重试 | — |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户（无套餐） | 体验课学员 | 正价课学员（绑定本教练） |
|------|:---:|:---:|:---:|:---:|
| 成功状态区 | — | — | ✅ | ✅ |
| 预约信息卡 | — | — | ✅ | ✅ |
| 日历同步区 | — | — | ✅ | ✅ |
| 下一步操作区 | — | — | ✅ | ✅ |
| 客服提示区 | — | — | ✅ | ✅ |
| 底部完成栏 | — | — | ✅ | ✅ |
| 空/加载/错误占位 | — | — | ✅ | ✅ |

> 图例：✅ = 显示/可操作；— = 不适用（游客/无套餐用户不会进入本页）。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户（无套餐） | 体验课学员 | 正价课学员（绑定本教练） |
|---------|------|---------|---------|---------|
| 系统返回手势 | — | — | 拦截，弹二次确认 "是否返回首页？" | 同左 |
| 日历同步开关 | — | — | 调起系统日历授权 | 同左 |
| "查看我的预约" 入口 | — | — | → `U-我的预约页` | 同左 |
| "继续预约其他时段" 入口 | — | — | → `U-预约页`（同教练） | 同左 |
| 客服提示区 | — | — | → `U-预约详情页`（booking_id） | 同左 |
| "完成" 按钮 | — | — | → `U-我的预约页`（默认） | 同左 |

> **前置拦截**：游客/无套餐用户尝试进入本页时，前置跳转授权页或套餐列表页。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 无 booking_id 传入或预约不存在 | 中央 Empty 组件：插画 + "暂无预约信息" + "返回首页" 按钮（→ `U-首页`） |
| **加载中** | 查询预约详情 / 等待预约创建回调 | 成功区显示渐变占位；卡片区域显示骨架屏占位 |
| **错误状态** | 网络断开 / 接口 5xx / 预约不存在 | 中央错误插画 + "加载失败，点击重试" + Primary「重新加载」按钮 |
| **成功状态** | 预约创建成功且数据正常返回 | 按 §4 显示规则矩阵完整展示成功页；底部固定「完成」按钮 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 预约成功后 booking.status = 已预约 | PRD §6.1 / US-018 §4.1 / US-029 §4.1 | §3.1 成功状态区 / §3.2 状态徽标 |
| 预约成功后预占 1 课时（reserved +1） | PRD §6.3.1 / US-029 §4.1 | §3.2 剩余课时行 |
| 学员端预约卡片展示时间、教练、状态 | PRD §5.3.2 第 8 条 | §3.2 预约信息卡 |
| 支持日程提醒同步至手机日历 | PRD §5.3.2 第 8 条 | §3.3 日历同步区 |
| 开课前 24 小时外可自由取消 | PRD §5.3.2 第 9 条 / §6.5.1 | §3.5 客服提示区 |
| 预约成功发送微信订阅消息 | PRD §7.2 通知场景 1 | 本页不直接展示，但业务背景一致 |
| MVP 页面清单第 14 项「预约成功页」提示 + 日历 | PRD §13.3 | 本页 Frame 名与功能定位 |
| 体验课与正价课在预约卡片中区分 | US-018 §4.1 / US-029 §4.1 | §3.2 课程类型标签 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 已按 §5 跳转动线矩阵处理系统返回拦截
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含状态后缀（如 `SuccessHeader-预约成功`、`BookingCard-体验课`）
- [ ] **无 NavBar、无返回按钮**
- [ ] **底部固定栏仅包含「完成」按钮**
- [ ] 成功区使用成功主题视觉 + 大对勾
- [ ] 预约信息卡包含时间、日期、教练、地点、剩余课时、取消提示
- [ ] 日历同步区包含开关与"已添加"状态
- [ ] "查看我的预约" 与 "继续预约" 入口已定义
- [ ] 首次预约欢迎文案已按状态显隐

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：无 NavBar/无返回按钮，成功头部，预约信息卡，日历同步，下一步入口，底部「完成」按钮，覆盖体验课/正价课两种预约类型 |
