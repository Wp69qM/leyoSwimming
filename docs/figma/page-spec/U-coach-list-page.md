# 页面规格：U-教练列表页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-001（游客浏览教练列表与详情）
> **对应 PRD**：§3.1 身份体系、§5.1 游客功能、§5.3.1 教练与套餐浏览、§5.4.7 教练离职/可见性、§13.3 MVP 页面 6
> **版本**：v1.0 | **创建日期**：2026-08-02

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 教练列表页 |
| 页面名（Frame） | `U-教练列表页` |
| 平台 | 微信小程序 - 用户端 |
| 是否含 TabBar | **是**（首页 / 教练 / 预约 / 我的），当前选中"教练" |
| 是否含 NavBar | **是** |
| 返回按钮 | 否（Tab 首页，无返回） |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | U-教练列表页 | https://www.calicat.cn/design/2083742072257646592?node-id=7551564f-8485-4ac9-af0c-ae6edd7a23bd |

---
## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | 是 | 可浏览教练列表、查看详情；不能购买/预约，点击"立即预约"需引导登录 |
| 注册用户 | 是 | 可浏览、查看详情；购课后才能成为学员 |
| 学员 | 是 | 可浏览、查看详情；详情页可直接预约（受实时状态约束） |
| 教练 | 否 | 本页为用户端，教练通过教练端小程序操作 |
| 管理员 | 否 | 本页为用户端，管理员通过 Web 管理后台操作 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| 有公开教练 | `coach.status IN (1, 4)` 的教练存在 | 正常展示卡片列表 |
| 无公开教练 | 系统没有 `status IN (1, 4)` 的教练 | 展示空状态 |
| 筛选/排序生效 | 用户点击排序项或输入搜索词 | 列表按新条件重新拉取 |
| 分页未结束 | 当前页 < 总页数 | 底部展示加载更多提示 |
| 分页已结束 | 当前页 = 总页数 | 底部展示"没有更多了" |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. **元素清单只描述「有什么元素」以及「元素之间的交互与状态」，不描述具体样式细节。** 颜色、宽度、高度、边距、背景、圆角、字体、字号、阴影等视觉参数**禁止**出现在 page-spec 中，避免对后续 Figma 渲染和 Calicat 设计稿还原造成干扰。
> 2. 视觉样式以 Calicat 设计稿为唯一最高优先级来源；page-spec 仅对交互逻辑、业务规则、状态流转负责。
> 3. 每个元素必须说明：名称、作用、显示条件、交互行为、关联业务规则。
> 4. 快捷入口 ≠ TabBar 重复。
> 5. 同一业务入口在不同身份下形态可能不同，必须分身份定义。

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 名称 | 状态栏 + 导航栏 |
| 作用 | 展示页面标题"教练"，提供顶部导航 |
| 显示条件 | 始终显示 |
| 左侧返回 | 无（Tab 首页） |
| 右侧操作 | 无 |
| 关联业务规则 | 全局 NavBar 规范 |

### 3.2 搜索栏

> **来自 PRD §5.1 / US-001 §4.1**：游客可通过搜索快速查找教练。

| 字段 | 值 |
|------|-----|
| 名称 | 教练搜索栏 |
| 作用 | 按教练姓名搜索 |
| 显示条件 | 始终显示 |
| placeholder | "搜索教练姓名" |
| 左侧图标 | 搜索图标 |
| 右侧清除图标 | 输入框有内容时显示，点击清空关键词 |
| 交互 | 输入时 debounce 300ms 触发列表刷新；点击清除清空关键词并刷新 |
| 关联业务规则 | PRD §5.1 / US-001 §4.1 |

### 3.3 筛选/排序栏

> **来自 PRD §13.3 页面 6 / US-001 §5 规则 6**：教练列表页支持筛选/排序。

| 字段 | 值 |
|------|-----|
| 名称 | 排序栏 |
| 作用 | 切换列表排序方式 |
| 显示条件 | 始终显示 |
| 排序项 | "综合评分" / "价格" / "可约时间" |
| 默认选中 | "综合评分"（高 → 低） |
| 价格切换 | 首次点击按价格升序；再次点击切换为降序，文字后显示 ▲/▼ 图标 |
| 可约时间 | 按最近可约时段从早到晚排序 |
| 选中指示 | 选中项下方显示短横线指示 |
| 关联业务规则 | PRD §13.3 页面 6 / US-001 §5 规则 6 |

### 3.4 教练卡片列表

> **来自 US-001 §4.1 / §14.1 / PRD §5.3.1**：列表按综合评分默认降序展示；卡片展示个人形象照、姓名、实时状态、评分、教龄、擅长泳姿。

#### 3.4.1 列表容器

| 字段 | 值 |
|------|-----|
| 名称 | 教练卡片列表 |
| 作用 | 展示教练卡片列表 |
| 显示条件 | 有公开教练时显示 |
| 卡片间距 | 垂直间距 |
| 可点击态 | 卡片可点击 |
| 关联业务规则 | US-001 §4.1 / §14.1 / PRD §5.3.1 |

#### 3.4.2 卡片内部布局

| 元素 | 作用 | 位置 |
|------|------|------|
| 个人形象照 | 展示教练头像 | 卡片左侧，垂直居中 |
| 姓名 | 展示教练姓名 | 头像右侧，与头像顶部对齐 |
| 实时状态标签 | 展示教练实时状态 | 姓名同一行右侧 |
| 评分 | 展示教练综合评分 | 姓名下方 |
| 教龄 | 展示教龄 | 与评分同行 |
| 擅长泳姿 | 展示最多 2 项擅长泳姿 | 评分行下方 |

#### 3.4.3 评分显示

> **来自 US-001 §14.2**：星级比精确小数更直观。

| 字段 | 值 |
|------|-----|
| 名称 | 教练综合评分 |
| 作用 | 展示教练综合评分，辅助用户选择教练 |
| 显示条件 | 始终显示（有评分时显示评分；无评分时显示占位文案） |
| 内容 | 一个星形图标 + 评分数字 |
| 关联业务规则 | US-001 §14.2 |

#### 3.4.4 实时状态徽标

> **来自 PRD §5.3.1 / §5.4.7 / 全局规范 §3.1**：列表展示教练实时状态，不展示入职审核/离职申请等内部状态。

| 实时状态 | 文案 |
|---------|------|
| 空闲中 | 空闲中 |
| 上课中 | 上课中 |
| 休息中 | 休息中 |
| 已下班 | 已下班 |
| 请假中 | 请假中 |

> **注意**：`coach.status = 4`（申请离职中）的教练仍正常展示，**不得**出现"申请离职中"等内部状态标签（PRD §5.4.7）。

### 3.5 列表底部状态

| 字段 | 值 |
|------|-----|
| 名称 | 列表底部状态 |
| 作用 | 提示列表加载/到底状态 |
| 加载更多 | 滚动到底部且还有下一页时显示"加载中..." |
| 无更多数据 | 已到最后一页时显示"— 没有更多了 —" |
| 触发条件 | 滚动到底部且还有下一页时自动触发；失败时展示"点击重试"文字按钮 |

### 3.6 空状态

> **来自 US-001 §6.2 / 全局规范 §4 / §5.2**：无公开教练时展示空状态。

| 字段 | 值 |
|------|-----|
| 名称 | 空状态 |
| 作用 | 无公开教练时提示用户 |
| 触发 | 接口返回空数组 |
| 插画占位 | 运动主题插画（游泳人物/水波） |
| 文案 | "暂无教练入驻，敬请期待" |
| CTA | 无（用户可在 TabBar 返回首页） |
| 关联业务规则 | US-001 §6.2 / 全局规范 §4 / §5.2 |

### 3.7 加载中骨架屏

| 字段 | 值 |
|------|-----|
| 名称 | 加载中骨架屏 |
| 作用 | 数据加载时占位 |
| 触发 | 首次进入 / 下拉刷新 / 切换排序 |
| 样式 | 骨架屏占位，高度与卡片一致，多张卡片垂直排列 |
| 动效 | shimmer 动画 |

### 3.8 错误状态

| 字段 | 值 |
|------|-----|
| 名称 | 错误状态 |
| 作用 | 网络/服务异常时提示并重试 |
| 触发 | 网络断开 / 接口 5xx / 业务 4xx |
| 插画占位 | 错误插画 |
| 文案 | "网络异常，请重试" 或 "服务繁忙，请稍后重试" |
| 重试按钮 | "重新加载" |

### 3.9 TabBar

> **来自全局规范 §2.5.7**：用户端 4 Tab，当前选中第 2 个"教练"。

| 字段 | 值 |
|------|-----|
| 名称 | 底部 TabBar |
| 作用 | 一级导航 |
| 显示条件 | 始终显示 |
| Tab 项 | 首页 / 教练 / 预约 / 我的 |
| 当前选中 | 教练 |
| 关联业务规则 | 全局规范 §2.5.7 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 | 教练 | 管理员 |
|------|:---:|:---:|:---:|:---:|:---:|
| NavBar | ✅ | ✅ | ✅ | ❌ | ❌ |
| 搜索栏 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 筛选/排序栏 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 教练卡片 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 状态徽标（空闲/上课/休息/下班/请假） | ✅ | ✅ | ✅ | ❌ | ❌ |
| 加载更多 / 无更多 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 空状态 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 骨架屏 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 错误重试 | ✅ | ✅ | ✅ | ❌ | ❌ |
| TabBar | ✅ | ✅ | ✅ | ❌ | ❌ |

> **说明**：教练/管理员不可见因为他们使用独立的教练端小程序或 Web 管理后台；本表"❌"表示该身份不会在此用户端页面出现，而非功能权限限制。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员 |
|---------|------|---------|------|
| 教练卡片 | → `U-教练详情页` | → `U-教练详情页` | → `U-教练详情页` |
| Tab"首页" | → `U-首页` | → `U-首页` | → `U-首页` |
| Tab"教练" | —（当前页） | —（当前页） | —（当前页） |
| Tab"预约" | 引导登录 | → `U-我的预约页` | → `U-我的预约页` |
| Tab"我的" | 引导登录 | → `U-个人中心页` | → `U-个人中心页` |

> **登录引导**：游客点击"预约"/"我的" Tab 时，应弹出登录引导或跳转登录页，不能直接提示错误（全局规范 §5 文案）。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| 空状态 | 系统无公开教练 / 搜索/筛选结果为空 | 隐藏卡片列表与加载更多；居中展示空状态插画 + "暂无教练入驻，敬请期待" |
| 加载中 | 首次进入、下拉刷新、切换排序、输入搜索 | 显示骨架屏；NavBar / 搜索栏 / 排序栏保持可见 |
| 错误状态 | 网络/服务异常 | 隐藏骨架屏；居中展示错误插画 + 文案 + "重新加载" 按钮；下拉仍可触发重试 |
| 成功状态 | 数据正常返回 | 按 §3 元素清单完整展示；默认按综合评分降序 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 游客可浏览教练列表与详情 | PRD §5.1 / US-001 §4.1 | 页面不拦截游客访问，列表正常展示 |
| 教练列表卡片字段：个人形象照、姓名、实时状态标签、综合评分、教龄、擅长泳姿 | US-001 §4.1 / §5 规则 1 / §14.1 | 卡片展示个人形象照、姓名、实时状态徽标、综合评分、教龄、擅长泳姿；参考单价、证书、评价、可约时间在详情页展示 |
| 教练实时状态：空闲中/上课中/休息中/已下班/请假中 | PRD §5.3.1 / US-001 §5 规则 2 | 卡片状态徽标按 §3.4.4 展示 |
| 公开可见的教练为 `status IN (1, 4)`；status=0/2/3 不公开 | PRD §5.4.7 / US-001 §5 规则 4 | 列表仅展示已入驻（status=1）或申请离职中（status=4）的教练 |
| status=4 的教练不得展示"申请离职中"标签 | PRD §5.4.7 / US-001 §5 规则 5 | 状态徽标仅显示实时状态，不显示入职流程状态 |
| 教练列表支持筛选/排序 | PRD §13.3 页面 6 / US-001 §5 规则 6 | 顶部提供"综合评分/价格/可约时间"排序栏 |
| 空状态文案 | US-001 §6.2 / 全局规范 §5.2 | "暂无教练入驻，敬请期待" |
| 列表分页 | US-001 §8 边界场景 5 | 滚动到底部自动加载下一页，size=10 |
| 评分可视化规则 | US-001 §14.2 | 星级 + 数字 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 元素显隐、交互行为、状态流转与 §3 一致
- [ ] NavBar 标题为"教练"，无返回按钮
- [ ] 搜索栏包含搜索图标、清除图标与搜索占位文案
- [ ] 排序栏包含"综合评分 / 价格 / 可约时间"三项，默认选中综合评分
- [ ] 教练卡片包含个人形象照、姓名、实时状态徽标、评分、教龄、擅长泳姿
- [ ] 状态徽标仅显示实时状态，**不**显示"申请离职中"等内部状态
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导（Tab"预约"/"我的"）
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位（empty / loading / error / success）
- [ ] 元素命名包含显示条件后缀（如 `Card-教练-公开状态`、`Tag-状态-空闲中`）
- [ ] 未遗漏任何跨页面联动规则
- [ ] 已按全局规范在右侧创建 `U-教练列表页-empty` / `U-教练列表页-loading` / `U-教练列表页-error` 三个副本 Frame

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.1 | 2026-08-13 | PM | 移除教练卡片中的性别元素：§3.4.2、§7 业务规则引用、§8 AI 生成检查清单同步调整 |
| v1.0 | 2026-08-02 | PM | 初版：完整教练列表页规格，覆盖搜索、排序、卡片、空状态、分页、四态、跳转动线 |
