# 页面规格：U-候补关注页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-023（学员候补与关注时段）/ US-024（学员候补自动转正）
> **对应 PRD**：§5.3.2 学员端预约规则、§6.1 预约状态机、§6.3.1 预占规则、§14 C1 取消规则澄清、§13.3 MVP 核心页面清单（页面 15）
> **版本**：v1.0 | **创建日期**：2026-08-02

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 候补关注页 |
| 页面名（Frame） | `U-候补关注页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **否**（二级页面，从「我的」或「预约」Tab 进入） |
| 是否含 NavBar | **是**（白色背景，标题居中） |
| 返回按钮 | **是**（左侧 24×24px 返回箭头，左 padding 16px） |

---

## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 本页为**学员核心页面**，游客访问时统一拦截到登录流程。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | ❌ 不可见 | 整页拦截，跳转 `U-微信授权页`；登录成功后返回本页刷新 |
| 注册用户 | ✅ 可见 | 可查看并管理「关注」列表；若无 active 套餐，「候补」Tab 为空且提示购买套餐 |
| 学员 | ✅ 可见 | 可查看并管理「候补」与「关注」两个列表，是主使用身份 |
| 教练 | ❌ 不可见 | 教练端使用教练端小程序「我的学员」相关页面 |
| 管理员 | ❌ 不可见 | 管理端使用 Web 后台 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `waitlist.status = waiting` | 正在排队候补 | 卡片显示排队位置与预计等待提示，展示「取消候补」按钮 |
| `waitlist.status = converted` | 候补已自动转正为预约 | 卡片显示「已转正」徽标，提供跳转预约详情入口 |
| `waitlist.status = cancelled` | 候补已取消或他处转正被系统取消 | 默认不展示；若展示则置灰并标注「已取消」 |
| `slot_follow.status = active` | 关注有效中 | 卡片显示关注时段信息，展示「取消关注」按钮 |
| `slot_follow.status = invalid` | 时段已删除/过期导致关注失效 | 卡片置灰，标注「该时段已失效」，提供「删除」按钮 |
| 用户持有 active 套餐 | 具备候补资格 | 无 active 套餐时，候补 Tab 空状态提示「购买套餐后可候补」 |
| 候补队列释放 | 他用户取消导致名额释放 | 触发 US-024 自动转正，waiting → converted |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为。
> 2. 所有尺寸以 375px 设计宽度为基准；开发使用 rpx（750rpx 基准）。
> 3. 颜色/字号/圆角/阴影必须引用 `docs/figma/README.md` §1 Token，禁止硬编码。

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，背景 `#FFFFFF`，y = 0 |
| NavBar | 375×44，背景 `#FFFFFF`，底部 1px `#F0F0F0`（`color-divider`），y = 44 |
| 左侧返回按钮 | 24×24 线性返回箭头，`color-text-primary` #262626，左 padding 16px，垂直居中 |
| 标题 | "候补与关注"，17px / Medium（`font-size-body` + `font-weight-medium`），`color-text-primary`，水平居中 |
| 右侧占位 | 空（保持视觉平衡） |
| 显示条件 | 所有身份统一显示（游客被拦截，不进入本页） |
| 点击行为 | 返回上一页（我的预约页 / 个人中心页 / 教练排班页） |

> **来源**：全局设计规范 §2.5.6 NavBar；PRD §13.3 页面 15「候补/关注页」。

### 3.2 顶部 Segment 切换（候补 / 关注）

> **来源**：US-023 §14.2「默认显示候补列表，顶部 tab 切换关注列表」。

| 字段 | 值 |
|------|-----|
| 容器 | 343×40，x = 16，y = 96（NavBar 底部 8px 间距），背景 `#F5F5F5`，圆角 `radius-pill` 999px |
| 选项宽度 | 两选项等宽，每项 171.5px |
| 未选中项 | 背景透明，文字 14px / Regular `color-text-secondary`，垂直居中 |
| 选中项 | 背景 `#FFFFFF`，圆角 `radius-pill`，文字 14px / Medium `color-text-primary`，`shadow-card` |
| 默认选中 | "候补" |
| 切换动效 | 300ms ease-out，选中背景平滑滑动 |
| 显示条件 | 注册用户/学员登录后始终显示 |
| 点击行为 | 切换候补列表与关注列表，列表区域横向滑动切换 |

### 3.3 候补列表区

> **来源**：US-023 §4.1、US-024 §4.1、PRD §5.3.2 第 11-13 条。

#### A. 列表容器

| 字段 | 值 |
|------|-----|
| 容器 | x = 16，宽度 343，顶部 y = 152（Segment 下方 16px），底部预留 24px |
| 卡片间距 | 垂直 12px |
| 可滚动 | 是，纵向滚动 |

#### B. 候补卡片（Waitlist Card）

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应，白底卡片，`radius-lg` 12px，`shadow-card`，内边距 16px |
| 教练个人形象照 | 48×48 圆形（`Avatar-md`），背景 `#E6F7FF`，位于卡片左侧 |
| 教练姓名 | 16px / Medium `color-text-primary`，位于头像右侧 12px，顶部与头像顶部对齐 |
| 状态徽标 | 位于姓名右侧 8px，按 §3.6 候补状态徽标样式 |
| 时段信息 | "周三 08:00-09:00"，14px / Regular `color-text-primary`，位于姓名下方 4px |
| 场馆/地点 | "XX 游泳馆 · 1 号池"，12px / Regular `color-text-secondary`，位于时段下方 4px |
| 排队位置 | "当前排在第 N 位"，14px / Medium `color-accent` #FF6B35，位于卡片右侧垂直居中 |
| 候补人数 | "共 N 人候补"，10px / Regular `color-text-secondary`，位于排队位置下方 2px |
| 预计提示 | "有人取消时将按顺序自动预约"，10px / Regular `color-text-secondary`，位于时段信息下方 16px |
| 取消按钮 | 80×32， Secondary 按钮样式（白底 + 1px `color-primary` 边框），文字 "取消候补" 14px / Medium `color-primary`，位于卡片底部右侧 |
| 已转正入口 | `waitlist.status = converted` 时，取消按钮替换为 Primary 按钮 "查看预约" |
| 删除入口 | `waitlist.status = cancelled/expired` 时，按钮改为 Neutral 样式 "删除记录" |
| 点击行为 | 点击卡片非按钮区域 → `U-教练排班页`（定位到该时段）；点击 "取消候补" → 二次确认弹窗 → 调用 API；点击 "查看预约" → `U-预约详情页` |

#### C. 候补卡片状态变体

| 状态 | 徽标文案 | 徽标颜色 | 卡片表现 |
|------|---------|---------|---------|
| `waiting` | "排队中" | Warning 黄 #FAAD14 / `#FFFBE6` 底 | 显示排队位置，「取消候补」可操作 |
| `converted` | "已转正" | Success 绿 #52C41A / `#F6FFED` 底 | 隐藏排队位置，按钮改为「查看预约」 |
| `cancelled` | "已取消" | Neutral 灰 #8C8C8C / `#F5F5F5` 底 | 整卡置灰，按钮改为「删除记录」 |
| `expired` | "已过期" | Neutral 灰 #8C8C8C / `#F5F5F5` 底 | 整卡置灰，提示"时段已过期" |

### 3.4 关注列表区

> **来源**：US-023 §4.2、US-024 §4.1、PRD §5.3.2 第 13 条。

#### A. 列表容器

| 字段 | 值 |
|------|-----|
| 容器 | 同 §3.3.A，x = 16，宽度 343，y = 152 |
| 卡片间距 | 垂直 12px |

#### B. 关注卡片（Follow Card）

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应，白底卡片，`radius-lg` 12px，`shadow-card`，内边距 16px |
| 教练个人形象照 | 48×48 圆形（`Avatar-md`），背景 `#E6F7FF` |
| 教练姓名 | 16px / Medium `color-text-primary`，头像右侧 12px |
| 状态徽标 | 位于姓名右侧 8px，按关注状态徽标样式 |
| 时段信息 | "周四 19:00-20:00"，14px / Regular `color-text-primary` |
| 提醒说明 | "释放前或有人取消时提醒"，12px / Regular `color-text-secondary` |
| 取消关注按钮 | 80×32，Secondary 按钮样式，文字 "取消关注" 14px / Medium `color-primary` |
| 失效提示 | `slot_follow.status = invalid` 时，底部显示 "该时段已失效" 12px / Regular `color-error` |
| 点击行为 | 点击卡片 → `U-教练排班页`（定位时段）；点击 "取消关注" → 二次确认 → 调用 API |

#### C. 关注卡片状态变体

| 状态 | 徽标文案 | 徽标颜色 | 卡片表现 |
|------|---------|---------|---------|
| `active` | "关注中" | Primary 蓝 #1890FF / `#E6F7FF` 底 | 正常显示，「取消关注」可操作 |
| `invalid` | "已失效" | Error 红 #FF4D4F / `#FFF1F0` 底 | 整卡置灰，显示失效原因，按钮改为「删除」 |

### 3.5 空状态占位

> **来源**：全局规范 §2.5.9 Empty 组件。

| 字段 | 值 |
|------|-----|
| 触发 | 当前 Tab 下无记录 |
| 插画占位 | 120×120 扁平运动主题插画，`color-primary` 浅色 #E6F7FF 底色 |
| 主文案 | 候补 Tab："暂无候补时段"；关注 Tab："暂无关注时段"，14px Regular `color-text-secondary` |
| 副文案 | 候补 Tab（无 active 套餐）："购买套餐后即可排队候补"；其他："去预约页看看可约时段吧"，12px Regular `color-text-disabled` |
| CTA 按钮 | 候补 Tab 无套餐时显示 Primary 按钮 "去购买套餐"；其他情况显示 Text 按钮 "去预约" |
| 垂直位置 | 距屏幕顶部约 35% 高度居中 |

### 3.6 状态徽标样式

> **来源**：全局规范 §2.5.3 Tag 组件。

| 徽标类型 | 文案 | 背景色 | 文字色 |
|---------|------|--------|--------|
| 候补-排队中 | 排队中 | `#FFFBE6` | `#FAAD14` |
| 候补-已转正 | 已转正 | `#F6FFED` | `#52C41A` |
| 候补-已取消 | 已取消 | `#F5F5F5` | `#8C8C8C` |
| 关注-关注中 | 关注中 | `#E6F7FF` | `#1890FF` |
| 关注-已失效 | 已失效 | `#FFF1F0` | `#FF4D4F` |

### 3.7 加载与错误占位

| 字段 | 值 |
|------|-----|
| 加载状态 | 列表区域显示 3 张骨架卡片（343×96，圆角 12px，浅灰 #F5F5F5，shimmer 动画） |
| 错误状态 | 中央错误插画 + "加载失败，点击重试" + Primary「重新加载」按钮 |
| 触发条件 | 见 §6 四态设计 |

### 3.8 设计约束与注意事项

> **来源**：跨批次全局设计原则 §7 / §8。

| # | 约束 | 说明 |
|---|------|------|
| 1 | 候补/关注为两种并列机制 | 不得将两者混为同一队列，UI 上必须通过 Segment 明确区分 |
| 2 | 排队位置仅对 waiting 状态有效 | converted/cancelled/expired 状态不显示排队位置，避免误导 |
| 3 | 操作按钮状态与后端状态严格对应 | 不允许在 waiting 状态外显示「取消候补」按钮 |
| 4 | 二次确认必须携带业务说明 | 取消候补弹窗文案："取消后将不再排队，是否确认？" |
| 5 | 关注提醒不强制预约 | 关注用户仅收到提醒，不会自动创建 booking |
| 6 | 列表项长按/左滑不触发删除 | MVP 仅通过卡片内按钮操作，避免与滚动冲突 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 |
|------|:---:|:---:|:---:|
| NavBar 返回 + 标题 | ❌（拦截登录） | ✅ | ✅ |
| Segment 切换（候补/关注） | ❌ | ✅ | ✅ |
| 候补列表卡片 | ❌ | ✅（仅自己的） | ✅ |
| 候补-排队位置 | ❌ | ✅ | ✅ |
| 候补-取消候补按钮 | ❌ | 仅 `waiting` 状态 | 仅 `waiting` 状态 |
| 候补-查看预约按钮 | ❌ | 仅 `converted` 状态 | 仅 `converted` 状态 |
| 关注列表卡片 | ❌ | ✅ | ✅ |
| 关注-取消关注按钮 | ❌ | 仅 `active` 状态 | 仅 `active` 状态 |
| 关注-删除按钮 | ❌ | 仅 `invalid` 状态 | 仅 `invalid` 状态 |
| 空状态 + CTA | ❌ | ✅ | ✅ |
| 加载/错误占位 | ❌ | ✅ | ✅ |

> 图例：✅ = 显示/可操作；❌ = 隐藏或页面被拦截。候补/关注列表仅展示当前登录用户自己的记录。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员 |
|---------|------|---------|------|
| 整页访问 | → `U-微信授权页` | 正常进入本页 | 正常进入本页 |
| 返回按钮 | — | → 上一页 | → 上一页 |
| Segment「候补」 | — | 切换显示候补列表 | 切换显示候补列表 |
| Segment「关注」 | — | 切换显示关注列表 | 切换显示关注列表 |
| 候补卡片 | — | → `U-教练排班页`（coach_id, slot_id） | → `U-教练排班页` |
| 「取消候补」按钮 | — | 二次确认弹窗 → API → 列表刷新 | 二次确认弹窗 → API → 列表刷新 |
| 「查看预约」按钮 | — | → `U-预约详情页`（booking_id） | → `U-预约详情页` |
| 关注卡片 | — | → `U-教练排班页` | → `U-教练排班页` |
| 「取消关注」按钮 | — | 二次确认 → API → 列表刷新 | 二次确认 → API → 列表刷新 |
| 空状态 CTA「去购买套餐」 | — | → `U-套餐列表页` | → `U-套餐列表页`（过滤当前教练） |
| 空状态 CTA「去预约」 | — | → `U-教练排班页` | → `U-教练排班页` |

> **"引导登录"**：游客访问本页时直接跳转 `U-微信授权页`，不在当前页弹登录框；登录成功后返回本页并刷新数据。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 当前 Tab 下无任何记录 | 隐藏列表，居中展示 Empty 组件：插画 + 主文案 + 副文案 + CTA 按钮 |
| **加载中** | 首次进入 / 下拉刷新 / 切换 Tab | 列表区域显示 3 张骨架屏卡片；NavBar 与 Segment 保持可见 |
| **错误状态** | 网络断开 / 接口 5xx / 业务 4xx | 隐藏骨架屏，中央展示错误插画 + "加载失败，点击重试" + Primary「重新加载」按钮 |
| **成功状态** | 数据正常返回 | 按 §4 显示规则矩阵完整展示 Segment、列表卡片、操作按钮；操作后局部刷新 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 满员时段可排队候补；有人取消时按候补顺序自动转正并通知 | PRD §5.3.2 第 11 条 / US-024 §4.1 | §3.3 候补卡片、§3.3.C converted 状态 |
| 用户可同时加入多个时段候补；若其中一个转正，其他候补自动取消 | PRD §5.3.2 第 12 条 / US-024 §4.1 step 7 | §3.3.C cancelled 状态展示 |
| 关注时段：释放前或有人取消时收到提醒 | PRD §5.3.2 第 13 条 / US-023 §4.2 | §3.4 关注卡片 |
| 候补与关注为两种可选方式 | PRD §5.3.2 第 14 条 / US-023 §5 | §3.2 Segment 切换 |
| 取消预约后课时释放，候补用户按队列顺序收到通知 | PRD §4.2 / §6.3.1 / US-024 | §3.3 排队位置与转正逻辑 |
| 预约状态机包含「候补中」状态 | PRD §6.1.2 状态 7 | §3.3.C 状态徽标 |
| 24h 外取消直接释放课时，24h 内需教练审批 | PRD §14 C1 | 候补转正触发源之一 |
| 用户需登录后才能查看个人候补/关注记录 | PRD §5.1 / US-023 §3 | §2.1 游客拦截 |
| 加入候补需用户持有 active 套餐 | US-023 §3 / §4.3 | §3.5 候补 Tab 空状态副文案 |
| 用户同时加入多个候补，其中一个转正其他自动取消 | PRD §5.3.2 第 12 条 / US-024 §4.1 | §3.3.C cancelled 状态 |
| 关注时段在释放前或有人取消时发送提醒 | PRD §5.3.2 第 13 条 / US-024 §4.1 step 9 | §3.4 关注卡片提醒说明 |
| 候补/关注记录仅展示当前用户自己的数据 | US-023 §7.1 / US-026 §8.1 | §4 显示矩阵 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/边框严格按 §3 规格并使用 Token
- [ ] NavBar 使用白色背景，`color-text-primary` 标题，底部 1px `color-divider`
- [ ] Segment 切换为两选项胶囊，选中项白底 + `shadow-card`，默认选中「候补」
- [ ] 候补卡片包含教练个人形象照、姓名、状态徽标、时段、排队位置、操作按钮
- [ ] 关注卡片包含教练个人形象照、姓名、状态徽标、时段、提醒说明、操作按钮
- [ ] 状态徽标严格使用 §3.6 五种颜色变体
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导（整页拦截）
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含身份/状态后缀（如 `Card-候补-排队中`、`Card-关注-已失效`）
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] 未遗漏候补转正后的「查看预约」入口
- [ ] 未遗漏关注失效后的「删除」入口
- [ ] 候补卡片显示「当前排在第 N 位」+「共 N 人候补」+「有人取消时将按顺序自动预约」
- [ ] 取消候补/取消关注操作均带二次确认弹窗
- [ ] 已取消/已过期候补记录提供「删除记录」按钮
- [ ] 已按 §2.6.9 在右侧创建 `U-候补关注页-empty` / `U-候补关注页-loading` / `U-候补关注页-error` 三个副本 Frame

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：完整候补关注页规格，覆盖候补/关注双列表、waiting/converted/cancelled/active/invalid 五种卡片状态、游客整页拦截、跳转矩阵与四态设计 |
