# 页面规格：U-我的预约页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-018（体验课学员预约与取消）/ US-029（学员预约正价课程）/ US-030（学员取消正价课程）/ US-032（学员签到课程）/ US-033（教练确认上课记录）
> **对应 PRD**：§5.3.2 预约功能、§5.3.3 课时与上课记录、§6.1 预约状态机、§6.5 取消与改约、§14 C1 取消规则澄清、§13.3 MVP 核心页面清单（页面 18）
> **版本**：v1.0 | **创建日期**：2026-08-02
> **特别说明**：本页为 Tab 页第 3 项「预约」，是学员查看全部预约（含体验课/正价课、已完成/待上课/已取消）的统一入口。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 我的预约页 |
| 页面名（Frame） | `U-我的预约页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **是**（第 3 个 Tab：预约，calendar 图标） |
| 是否含 NavBar | **否**（Tab 首页无需 NavBar，顶部为页面标题区） |
| 返回按钮 | **否** |

---

## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [CROSS-BATCH-PRINCIPLES.md](../CROSS-BATCH-PRINCIPLES.md) §1。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | 否 | 点击 TabBar「预约」→ 跳转微信授权页（`U-微信授权页`），不显示本页内容 |
| 注册用户 | 否 | 无 active 套餐，点击 TabBar「预约」→ 跳转 `U-套餐列表页` 提示购买套餐 |
| 学员 | 是 | 持有 active 套餐，是本页唯一目标用户；展示全部预约卡片 |
| 教练 | 否 | 教练端使用教练端页面；以用户身份登录后若同时为学员则按学员身份显示 |
| 管理员 | 否 | 管理端使用 Web 后台 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `booking.status = 已预约` | 预约成功且未开课 | 显示在「 upcoming 」列表；可取消/改约 |
| `booking.status = 待上课` | 距开课 ≤ 10 分钟 | 显示在「 upcoming 」列表；展示「一键签到」入口 |
| `booking.status = 上课中` | 课程进行中 | 显示在「 upcoming 」列表；展示「一键签到」入口（若未签到） |
| `booking.status = 已完成` | 教练已确认扣课时 | 显示在「 past 」列表；可查看上课记录 |
| `booking.status = 已取消` | 用户/教练/平台取消 | 显示在「 past 」列表；展示取消原因 |
| `booking.status = 旷课` | 学员未到场且教练标记 | 显示在「 past 」列表；展示旷课状态与教练备注 |
| `booking.status = 候补中` | 加入候补队列 | 显示在「 upcoming 」列表；展示候补序号 |
| `time_to_start ≥ 24h` | 距离开课 ≥ 24 小时 | 卡片徽标「可取消」，点击直接取消 |
| `2h ≤ time_to_start < 24h` | 距离开课 < 24 小时且 ≥ 2 小时 | 卡片徽标「需审批」，取消需教练审批 |
| `time_to_start < 2h` | 距离开课 < 2 小时 | 卡片徽标「不可取消」，仅可走特殊原因申诉 |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为。
> 2. 所有尺寸以 375px 设计宽度为基准；开发使用 rpx（750rpx 基准）。
> 3. 颜色/字号/圆角/阴影必须引用 `docs/figma/README.md` §1 Token，禁止硬编码。

### 3.1 状态栏 + 页面标题区

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，背景 `#FFFFFF`，y = 0 |
| 标题区 | 375×56，背景 `#FFFFFF`，y = 44，底部 1px `#F0F0F0`（`color-divider`） |
| 页面标题 | "我的预约"，20px / Bold（`font-size-h2`），`color-text-primary`，水平居中，y = 16 |
| 右侧日历图标 | 24×24 `color-text-secondary`，x = 335，y = 16；点击跳转系统日历/日程提醒（P3 预留，MVP 仅展示 Toast） |
| 显示条件 | 学员身份；游客/注册用户点击 TabBar 时被拦截 |

### 3.2 顶部分类标签栏（Segmented Tabs）

> **来源**：PRD §5.3.2 第 8 条「我的预约」需区分 upcoming 与 past；US-030 取消流程。

| 字段 | 值 |
|------|-----|
| 容器 | 375×52，背景 `#FFFFFF`，y = 100，底部 1px `#F0F0F0` |
| Tab 区 | 两个胶囊 Tab：「即将上课」「历史记录」，居中排列，间距 8px |
| 未选中 Tab | 高 32px，水平 padding 20px，背景 `#F5F5F5`，圆角 999px（`radius-pill`），文字 14px / Regular `color-text-secondary` |
| 选中 Tab | 高 32px，水平 padding 20px，背景 `#1890FF`，圆角 999px，文字 14px / Medium `#FFFFFF` |
| 显示条件 | 学员身份；预约数量 ≥ 1 时显示 |
| 点击行为 | 切换「即将上课」「历史记录」两列表；切换时内容区横向滑动（300ms ease-out） |

### 3.3 预约卡片（Appointment Card）

> **核心元素**。每个卡片展示一次预约的完整摘要信息。
> **来源**：PRD §5.3.2 第 8-9 条 / US-029 / US-030 / US-032。

#### A. 卡片整体结构

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应（最小 128px），x = 16，y = 164（首个卡片），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 卡片间距 | 12px（垂直） |
| 内边距 | 16px |
| 可点击区域 | 整张卡片可点击 → `U-预约详情页` |
| hover/按下 | 阴影从 `shadow-card` 变为 `shadow-raised`，过渡 200ms |

#### B. 卡片头部行

| 字段 | 值 |
|------|-----|
| 课程类型标签 | 胶囊标签，高 20px，水平 padding 8px，圆角 999px，字号 12px；体验课为 `color-success` 浅色底 + `color-success` 字；正价课为 `color-primary` 浅色底 + `color-primary` 字 |
| 状态徽标 | 位于类型标签右侧 8px，使用 [README.md §2.5.3](../../../docs/figma/README.md) Tag 组件；具体见 §3.3.C |
| 上课时间 | 右侧对齐，14px / Medium `color-text-primary`，如 "8月2日 周五 09:00-10:00" |

#### C. 状态徽标定义

> **来源**：PRD §6.5.1 取消时间窗 / CROSS-BATCH-PRINCIPLES §4.2。

| 徽标文案 | 背景 | 文字 | 显示条件 |
|----------|------|------|----------|
| 可取消 | `color-success` 浅色 #F6FFED | `color-success` #52C41A | 已预约/待上课/上课中 + 距开课 ≥ 24h |
| 需审批 | `color-warning` 浅色 #FFFBE6 | `color-warning` #FAAD14 | 已预约/待上课/上课中 + 2h ≤ 距开课 < 24h |
| 不可取消 | `color-error` 浅色 #FFF1F0 | `color-error` #FF4D4F | 已预约/待上课/上课中 + 距开课 < 2h |
| 已完成 | `color-primary` 浅色 #E6F7FF | `color-primary` #1890FF | booking.status = 已完成 |
| 已取消 | `color-text-secondary` 浅色 #F5F5F5 | `color-text-secondary` #8C8C8C | booking.status = 已取消 |
| 旷课 | `color-error` 浅色 #FFF1F0 | `color-error` #FF4D4F | booking.status = 旷课 |
| 候补中 | `color-accent` 浅色 #FFF2E8 | `color-accent` #FF6B35 | booking.status = 候补中 |

#### D. 教练信息区

| 字段 | 值 |
|------|-----|
| 教练个人形象照 | 40×40 圆形（`Avatar-sm` 规格），背景 `#E6F7FF`，位于卡片左侧 |
| 教练姓名 | "张教练"，16px / Medium `color-text-primary`，位于头像右侧 12px |
| 课程信息 | "自由泳 · 正价课"，12px / Regular `color-text-secondary`，位于姓名下方 2px |
| 显示条件 | 所有预约卡片统一显示 |

#### E. 操作按钮区

> **来源**：US-032 签到 / US-030 取消 / US-031 改约。

| 字段 | 值 |
|------|-----|
| 容器 | 卡片底部，与上方内容间距 12px，顶部 1px `#F0F0F0` 分隔线，padding-top 12px |
| 一键签到按钮 | 高 32px，水平 padding 12px，背景 `gradient-primary`，圆角 `radius-md` 8px，文字 "一键签到" 14px / Medium `#FFFFFF`，`shadow-primary` |
| 取消预约按钮 | Secondary 按钮样式：白底 + `color-primary` 边框，文字 "取消预约" 14px / Medium `color-primary` |
| 查看记录按钮 | Text 按钮样式：文字 "查看记录" 14px / Medium `color-primary` |
| 再次预约按钮 | Primary 按钮样式：背景 `gradient-primary`，文字 "再次预约" 14px / Medium `#FFFFFF` |
| 显示条件 | 见 §4 显示规则矩阵 |

#### F. 卡片操作按钮状态矩阵

| 预约状态 | 时间窗 | 主操作 | 次操作 | 说明 |
|----------|--------|--------|--------|------|
| 已预约 | ≥24h | 取消预约 | — | 点击直接弹出确认 |
| 已预约 | 2h~24h | 取消预约 | — | 点击进入教练审批流程 |
| 已预约 | <2h | 申诉 | — | 点击跳转预约详情页申诉入口 |
| 待上课 | 任意 | 一键签到 | — | 仅在开课前 10 分钟至课程结束显示 |
| 上课中 | 任意 | 一键签到 | — | 若未签到则显示 |
| 已完成 | 任意 | 查看记录 | — | 跳转上课记录页 |
| 已取消 | 任意 | 再次预约 | — | 跳转教练排班页 |
| 旷课 | 任意 | 查看详情 | — | 跳转预约详情页 |
| 候补中 | 任意 | 取消候补 | — | 释放候补名额 |

#### G. 状态徽标计算规则

> **来源**：PRD §6.5.1 / CROSS-BATCH-PRINCIPLES §4.2。

| 优先级 | 判定条件 | 徽标文案 | 背景/文字色 |
|--------|---------|----------|------------|
| 1 | booking.status = 候补中 | 候补中 | `color-accent` 浅色 / `color-accent` |
| 2 | booking.status = 已完成 | 已完成 | `color-primary` 浅色 / `color-primary` |
| 3 | booking.status = 已取消 | 已取消 | `color-text-secondary` 浅色 / `color-text-secondary` |
| 4 | booking.status = 旷课 | 旷课 | `color-error` 浅色 / `color-error` |
| 5 | booking.status ∈ {已预约, 待上课, 上课中} 且 time_to_start ≥ 24h | 可取消 | `color-success` 浅色 / `color-success` |
| 6 | booking.status ∈ {已预约, 待上课, 上课中} 且 2h ≤ time_to_start < 24h | 需审批 | `color-warning` 浅色 / `color-warning` |
| 7 | booking.status ∈ {已预约, 待上课, 上课中} 且 time_to_start < 2h | 不可取消 | `color-error` 浅色 / `color-error` |

> **计算基准**：`time_to_start = 课程开始时间 - 当前时间`；精确到分钟，不考虑秒级。

### 3.4 快速状态筛选标签（可选）

| 字段 | 值 |
|------|-----|
| 容器 | 375×44，背景 `#FFFFFF`，位于分类标签栏下方（当有多个状态时显示） |
| 标签项 | 横向滚动："全部"、"待上课"、"候补中"、"已完成"、"已取消" |
| 未选中 | 高 28px，水平 padding 12px，背景 `#F5F5F5`，圆角 999px，文字 12px Regular `color-text-secondary` |
| 选中 | 高 28px，水平 padding 12px，背景 `#1890FF`，圆角 999px，文字 12px Medium `#FFFFFF` |
| 显示条件 | MVP 可选；同一状态 ≥ 2 条时建议显示 |

### 3.5 底部 TabBar

> **来源**：CROSS-BATCH-PRINCIPLES §6.1。

| 字段 | 值 |
|------|-----|
| 容器 | 375×83，固定底部 y = 729，背景 #FFFFFF，顶部 1px `#F0F0F0`，`shadow-float` |
| Tab 数量 | 4 个：首页、教练、预约、我的 |
| 预约 Tab | 第 3 个，图标 24×24 calendar，文字 "预约" 10px / Regular；选中态图标与文字均为 `color-primary` #1890FF；未选中态为 `color-text-secondary` #8C8C8C |
| 图标文字间距 | 2px |
| 安全区 | 底部 34px 为 iPhone X+ 安全区，非 X 设备为 49px 总高 |
| 显示条件 | 本页为 Tab 首页，始终显示 TabBar |

### 3.6 列表排序规则

| 分类 | 排序字段 | 排序方向 | 说明 |
|------|---------|---------|------|
| 即将上课 | 课程开始时间 | 升序（最近在上） | 包含已预约/待上课/上课中/候补中 |
| 历史记录 | 课程结束时间 | 降序（最近在上） | 包含已完成/已取消/旷课 |
| 同时间戳 | booking.created_at | 降序 | 用于解决时间完全相同的情况 |
| 置顶规则 | 待上课/上课中状态 | 最顶部 | 距离开课 ≤ 10 分钟的课程置顶展示 |

### 3.7 下拉刷新与加载更多

| 字段 | 值 |
|------|-----|
| 下拉刷新 | 列表顶部下拉显示加载指示器（NutUI PullRefresh），松手后刷新当前分类数据 |
| 加载更多 | 列表底部到达最后一条时显示 "加载中..."；无更多数据时显示 "没有更多了" 12px `color-text-disabled` |
| 分页参数 | 每页 10 条，按课程开始时间倒序 |
| 显示条件 | 学员身份；预约数量 ≥ 1 时启用 |

### 3.8 空状态 / 加载 / 错误占位

| 字段 | 值 |
|------|-----|
| 空状态（即将上课） | 内容区替换为 Empty 组件：插画 120×120 + "暂无待上课程" 14px + "去预约一节课程吧" 提示 + Primary「去预约」按钮 |
| 空状态（历史记录） | 内容区替换为 Empty 组件：插画 120×120 + "暂无历史记录" 14px + "您还没有上完的课程" 提示 |
| 加载状态 | 列表区显示骨架屏：3 个 343×128 灰色矩形（`color-divider` #F0F0F0）脉冲动画，圆角 12px |
| 错误状态 | 中央错误插画 + "网络异常，请重试" + Primary「重新加载」按钮 |
| 显示条件 | 见 §6 四态设计 |

### 3.9 页面骨架与 Auto Layout 要求

> **来源**：README.md §2.6.1 / §2.6.2。

| 字段 | 值 |
|------|-----|
| 页面 Frame | 375×812，背景 `#F5F7FA` |
| 状态栏 | 44px 高，背景 `#FFFFFF` |
| 页面标题区 | 56px 高，背景 `#FFFFFF`，位于状态栏下方 |
| 内容区 | 可滚动，padding 16px，底部留白 83px（避免被 TabBar 遮挡） |
| 分类标签栏 | 固定于标题区下方，滚动时保持吸顶（sticky top = 100px） |
| TabBar | 固定底部，高度 83px |
| Auto Layout | 所有列表项使用 VERTICAL 方向 Auto Layout，卡片间距 12px；空状态容器使用 CENTER 对齐 |

### 3.10 消息提醒与红点规则

> **来源**：PRD §5.3.2 / US-030 / US-033。

| 字段 | 值 |
|------|-----|
| TabBar 红点 | 当存在 booking.status = 待上课 / 上课中 且未签到，或 cancellation.status = 已拒绝 时，预约 Tab 图标右上角显示 8px 红色圆点 |
| 卡片新状态标记 | 教练确认后首次查看的已完成卡片，左上角显示 "New" 小标签，查看后消失 |
| 取消审批通知 | 教练审批结果通过消息推送触达，进入本页后刷新卡片状态 |
| 显示条件 | 学员身份；红点由后端未读消息数驱动 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 |
|------|:---:|:---:|:---:|
| TabBar「预约」Tab 入口 | ✅（点击拦截） | ✅（点击拦截） | ✅ |
| 页面标题区 | ❌ | ❌ | ✅ |
| 分类标签栏 | ❌ | ❌ | ✅ |
| 预约卡片 | ❌ | ❌ | ✅ |
| 卡片状态徽标 | ❌ | ❌ | ✅ |
| 一键签到按钮 | ❌ | ❌ | ✅（待上课/上课中且未签到） |
| 取消预约按钮 | ❌ | ❌ | ✅（可取消/需审批时段） |
| 查看记录按钮 | ❌ | ❌ | ✅（已完成卡片） |
| 再次预约按钮 | ❌ | ❌ | ✅（已取消/旷课卡片，P3 可选） |
| 快速筛选标签 | ❌ | ❌ | ⚠️（MVP 可选） |
| 下拉刷新 | ❌ | ❌ | ✅（有记录时） |
| 加载更多 | ❌ | ❌ | ✅（有记录时） |
| 列表排序规则 | ❌ | ❌ | ✅ |
| 页面骨架/Auto Layout | ❌ | ❌ | ✅ |
| 消息提醒与红点 | ❌ | ❌ | ✅ |
| 底部 TabBar | ✅ | ✅ | ✅ |
| 空/加载/错误占位 | 登录引导 | 购买引导 | ✅ |

> 图例：✅ = 显示/可操作；❌ = 隐藏；⚠️ = 功能受限或可选。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员 |
|---------|------|---------|------|
| TabBar「预约」 | → `U-微信授权页` | → `U-套餐列表页`（提示购买） | → `U-我的预约页` |
| 预约卡片 | — | — | → `U-预约详情页`（带 booking_id 参数） |
| 一键签到按钮 | — | — | → 签到成功 Toast + 卡片状态刷新 |
| 取消预约按钮 | — | — | → 取消确认弹窗 → `U-预约详情页` / 本页刷新 |
| 查看记录按钮 | — | — | → `U-上课记录页`（定位到该条记录） |
| 再次预约按钮 | — | — | → `U-教练排班页` |
| 空状态「去预约」按钮 | — | — | → `U-教练排班页` |
| 顶部日历图标 | — | — | → 系统日历 / 提醒设置（P3） |

> **"引导登录"**：当游客点击受登录限制的功能时，统一跳转 `U-微信授权页`，不在当前页弹登录框。
> **参数说明**：跳转 `U-预约详情页` 携带 `booking_id={id}`；跳转 `U-上课记录页` 携带 `highlight_booking_id={id}`。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 学员无历史预约 / 当前分类下无预约 | 列表区替换为 Empty 组件：游泳主题插画 + "暂无预约" + "去预约一节课程吧" + Primary「去预约」按钮 |
| **加载中** | 首次进入 / 切换分类 / 下拉刷新 | 列表区显示骨架屏（3 个卡片占位），TabBar 保持可见 |
| **错误状态** | 网络断开 / 接口 5xx / 业务 4xx | 中央错误插画 + "网络异常，请重试" 或 "服务繁忙，请稍后重试" + Primary「重新加载」按钮 |
| **成功状态** | 数据正常返回 | 按 §4 显示规则矩阵完整展示分类标签、预约卡片、操作按钮；实时刷新卡片徽标 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 我的预约页展示专属预约卡片，含时间、教练、状态 | PRD §5.3.2 第 8 条 | §3.3 预约卡片 |
| 取消预约：≥24h 直接释放，<24h 需教练审批 | PRD §5.3.2 第 9 条 / §6.5.1 | §3.3.C 徽标定义 |
| 特殊原因申诉：2h 阈值内需提交证明材料 | PRD §5.3.2 第 9 条 / §6.10 / §14 C1 | §3.3.C「不可取消」徽标 |
| 预约状态机：已预约/待上课/上课中/已完成/已取消/旷课/候补中 | PRD §6.1 | §2.2 状态定义 / §3.3.C 徽标 |
| 学员到馆后可在我的预约中一键签到 | US-032 §4.1 | §3.3.E 一键签到按钮 |
| 教练确认后学员可查看上课记录 | US-033 §4.1 / PRD §5.3.3 | §3.3.E 查看记录按钮 |
| 教练标记旷课后学员端展示旷课状态与备注 | US-033 §4.2 / PRD §5.3.2 第 10 条 | §3.3.C 旷课徽标 |
| 候补队列展示候补序号与状态 | US-023 §4.1 | §3.3.C 候补中徽标 |
| TabBar「预约」为第 3 个 Tab | CROSS-BATCH-PRINCIPLES §6.1 | §1 页面基础 |
| 游客/注册用户查看「我的」类页面需登录/购买 | CROSS-BATCH-PRINCIPLES §1.2 | §2.1 适用身份 / §5 跳转矩阵 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/边框严格按 §3 规格
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含显示条件后缀（如 `Card-可取消-距开课24h外`、`Card-已完成-可查看记录`）
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] 已创建 TabBar 第 3 项「预约」的正确图标与选中态
- [ ] 分类标签栏「即将上课」「历史记录」切换动效为 300ms ease-out
- [ ] 状态徽标严格按 §3.3.C 定义的颜色与文案
- [ ] 状态徽标计算优先级按 §3.3.G 执行
- [ ] 卡片头部类型标签区分体验课/正价课
- [ ] 操作按钮显隐与状态匹配（签到/取消/查看记录/再次预约）
- [ ] 列表排序规则按 §3.6 执行（即将上课升序、历史记录降序）
- [ ] TabBar 红点与消息提醒规则已落实
- [ ] 空状态包含「去预约」CTA 按钮
- [ ] 页面骨架与 Auto Layout 符合 §3.9 规格
- [ ] 未遗漏跨页面联动规则（签到、取消、查看记录跳转）

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：完整定义我的预约页作为 Tab 第 3 项，覆盖学员身份的 upcoming/past 列表、时间窗徽标、操作按钮、四态与跳转矩阵 |
