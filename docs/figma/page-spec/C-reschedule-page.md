# 页面规格：C-改约页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-031
> **对应 PRD**：§5.4.2 预约管理、§6.5 取消与改约
> **版本**：v1.0 | **创建日期**：2026-08-02

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 改约页 |
| 页面名（Frame） | `C-改约页` |
| 平台 | 微信小程序 - 教练端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | 否 |
| 是否含 NavBar | **是**（白色背景，标题「改约」） |
| 返回按钮 | 是（返回 `C-上课记录确认页`） |

---

## 2. 教练状态维度

### 2.1 适用状态

| 状态 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 待审核（0） | 否 | 重定向至 `C-入驻提交成功页` |
| 已通过（1） | 是 | 完整改约功能 |
| 已驳回（2） | 否 | 重定向至 `C-入驻资料填写页` |
| 已离职（3） | 否 | 功能锁定，重定向至 `C-教练中心页` |
| 申请离职中（4） | 是 | 可改约，但需遵守 <24h 学员确认规则 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| 当前预约未开始 | 课程尚未开始 | 允许改约 |
| 当前预约进行中/已结束 | 课程已开始或已结束 | 不可改约，提示返回 |
| 目标时段可约 | 教练该时段开放且无冲突 | 可选中 |
| 目标时段已约/已请假/已过去 | 不可选择 | 对应格子禁用 |
| ≥24h 改约 | 距原课程开始 ≥24 小时 | 教练直接确认即可 |
| <24h 改约 | 距原课程开始 <24 小时 | 需学员在系统内确认 |

---

## 3. 元素清单（自上而下）

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 名称 | 状态栏 + 导航栏 |
| 尺寸 | 状态栏 375×44；NavBar 375×44 |
| 位置 | y=0 / y=44 |
| 背景/颜色 | `#FFFFFF`，底部 1px `#F0F0F0` |
| 圆角 | 无 |
| 字体 | 标题「改约」17px Medium `#262626`；左侧返回 24×24 `#262626` |
| 显示条件 | 全部可见状态 |
| 点击行为 | 返回 → `C-上课记录确认页` |

### 3.2 当前预约信息卡

| 字段 | 值 |
|------|-----|
| 名称 | 当前预约信息卡 |
| 尺寸 | 343×120 |
| 位置 | x=16，y=96 |
| 背景/颜色 | `#FFFFFF`，`shadow-card` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 标签「当前预约」12px Regular `#8C8C8C`；时间 20px Bold `#262626`；学员名 16px Medium `#262626`；剩余课时 14px Regular `#8C8C8C` |
| 显示条件 | 携带 booking_id 进入时 |
| 点击行为 | 无 |

#### 3.2.1 课程类型标签

| 字段 | 值 |
|------|-----|
| 名称 | 课程类型标签 |
| 尺寸 | 自适应，高 22px |
| 位置 | 时间右侧 |
| 背景/颜色 | 正价课 `#E6F7FF` 文字 `#1890FF`；体验课 `#F6FFED` 文字 `#52C41A` |
| 圆角 | `radius-pill`（999px） |
| 字体 | 12px Regular |
| 显示条件 | 当前预约信息卡内 |
| 点击行为 | 无 |

### 3.3 不可改约提示

| 字段 | 值 |
|------|-----|
| 名称 | 不可改约提示 |
| 尺寸 | 343×64 |
| 位置 | x=16，y=232 |
| 背景/颜色 | `#FFF1F0` |
| 圆角 | `radius-lg`（12px） |
| 字体 | 14px Bold `#FF4D4F`；正文 12px Regular `#262626` |
| 显示条件 | 当前预约已开始/已结束/已取消/已完成 |
| 点击行为 | 无 |

### 3.4 日期选择栏

| 字段 | 值 |
|------|-----|
| 名称 | 日期选择栏 |
| 尺寸 | 375×80 |
| 位置 | y=232（无提示时）/ y=312（有提示时） |
| 背景/颜色 | `#FFFFFF` |
| 圆角 | 无 |
| 字体 | 周标题 14px Bold `#262626`；日期 16px Bold；星期 12px Regular |
| 显示条件 | 当前预约可改约时 |
| 点击行为 | 切换选中日期，刷新下方时段网格 |

#### 3.4.1 日期 Chip

| 字段 | 值 |
|------|-----|
| 名称 | 日期 Chip |
| 尺寸 | 44×56 |
| 位置 | 横向等分 7 列 |
| 背景/颜色 | 未选中 透明 `#262626`；选中 `#1890FF` `#FFFFFF` |
| 圆角 | `radius-md`（8px） |
| 字体 | 日期 16px Bold；星期 12px Regular |
| 显示条件 | 日期选择栏内 |
| 点击行为 | 选中该日期 |

### 3.5 可约时段网格

| 字段 | 值 |
|------|-----|
| 名称 | 可约时段网格 |
| 尺寸 | 343×动态 |
| 位置 | x=16，y=日期栏下方 + 16px |
| 背景/颜色 | 透明 |
| 圆角 | 无 |
| 字体 | 时段标签 12px Regular `#8C8C8C`；格内文案 12px Medium |
| 显示条件 | 选中日期有排班数据 |
| 点击行为 | 点击可选格子选中目标时段 |

#### 3.5.1 左侧时间轴

| 字段 | 值 |
|------|-----|
| 名称 | 左侧时间轴 |
| 尺寸 | 32×动态 |
| 位置 | 网格最左侧 |
| 背景/颜色 | 透明 |
| 圆角 | 无 |
| 字体 | 12px Regular `#8C8C8C` |
| 显示条件 | 时段网格 |
| 点击行为 | 无 |

#### 3.5.2 时段格

| 字段 | 值 |
|------|-----|
| 名称 | 时段格 |
| 尺寸 | 44×56 |
| 位置 | 时间轴右侧 7 列 |
| 背景/颜色 | 可约 `#E6F7FF` 文字 `#1890FF`；已约/过去/不可约 `#F5F5F5` 文字 `#BFBFBF`；已请假 `#FFF1F0` 文字 `#FF4D4F`；选中 `gradient-primary` 白色文字 |
| 圆角 | `radius-md`（8px） |
| 字体 | 12px Medium |
| 显示条件 | 每个时段 |
| 点击行为 | 可约且未选中时选中；已选中时取消 |

### 3.6 底部确认栏

| 字段 | 值 |
|------|-----|
| 名称 | 底部确认栏 |
| 尺寸 | 375×83 |
| 位置 | 固定底部 y=729 |
| 背景/颜色 | `#FFFFFF`，`shadow-float` |
| 圆角 | 无 |
| 字体 | 已选时段摘要 14px Regular `#262626` |
| 显示条件 | 当前预约可改约时 |
| 点击行为 | 无 |

#### 3.6.1 确认改约按钮

| 字段 | 值 |
|------|-----|
| 名称 | 确认改约按钮 |
| 尺寸 | 160×44 |
| 位置 | 底部栏右侧，x=199，y=12 |
| 背景/颜色 | `gradient-primary`；禁用态 `#F5F5F5` `#BFBFBF` |
| 圆角 | `radius-md`（8px） |
| 字体 | 16px Medium `#FFFFFF` |
| 显示条件 | 已选中目标时段 |
| 点击行为 | ≥24h 直接改约成功 → 返回 `C-上课记录确认页`；<24h 进入学员确认流程（弹窗提示） |

---

## 4. 显示规则总表

| 元素 | 已通过（1） | 申请离职中（4） | 其他状态 |
|------|:-----------:|:----------------:|:---------:|
| 当前预约信息卡 | ✅ | ✅ | ❌ |
| 不可改约提示 | 条件显示 | 条件显示 | ❌ |
| 日期选择栏 | ✅ | ✅ | ❌ |
| 可约时段网格 | ✅ | ✅ | ❌ |
| 底部确认栏 | ✅ | ✅ | ❌ |
| 确认改约按钮 | ✅（已选时段） | ✅（已选时段） | ❌ |

---

## 5. 跳转动线矩阵

| 起点元素 | 目标页面 |
|---------|----------|
| 返回按钮 | → `C-上课记录确认页` |
| 确认改约按钮（≥24h 成功） | → `C-上课记录确认页`（Toast「改约成功」） |
| 确认改约按钮（<24h） | 弹出学员确认提示弹窗（无页面跳转） |
| 时段格 | 选中/取消选中（无页面跳转） |

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| 空状态 | 选中日期无可约时段 | 时段网格区显示「当日暂无可约时段」插画 |
| 加载中 | 拉取预约/排班数据 | 信息卡 + 日期栏 + 网格骨架屏 |
| 错误状态 | 网络/服务异常 | 中央错误插画 + 「加载失败，点击重试」 |
| 成功状态 | 改约成功 | Toast「改约成功」并返回 `C-上课记录确认页` |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 教练可为已绑定学员改约正价课程 | US-031 §3.3 / `CROSS-BATCH-PRINCIPLES-COACH.md` §3.3 | §2.1 / §3.6.1 |
| ≥24h 的改约教练直接操作 | `CROSS-BATCH-PRINCIPLES-COACH.md` §3.3 | §3.6.1 |
| <24h 的改约需学员确认 | `CROSS-BATCH-PRINCIPLES-COACH.md` §3.3 | §3.6.1 |
| 已取消/已完成/上课中的预约不可改约 | PRD §6.5 / CROSS-BATCH §3.2 | §2.2 / §3.3 |
| 改约操作记录 operator = coach | `CROSS-BATCH-PRINCIPLES-COACH.md` §3.3 | §3.6.1 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档与 `CROSS-BATCH-PRINCIPLES-COACH.md`
- [ ] 已读取 v1.4 全局设计规范（`docs/figma/README.md`）
- [ ] `C-改约页` Frame 名与页面名一致
- [ ] 已按 5 种教练状态定义 §4 显示矩阵
- [ ] 当前预约信息卡显示原 booking 时间/学员/套餐信息
- [ ] 日期选择栏与 `C-排班管理页` 时段格规格一致
- [ ] 不可约时段使用禁用色 `#BFBFBF`
- [ ] 选中时段使用 `gradient-primary`
- [ ] 未选中时段时确认按钮禁用
- [ ] 已按 §6 预留 4 态视觉占位
- [ ] 跳转动线只引用已存在的 C- 页面

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：定义教练端改约页结构、时段网格与 24h 确认规则 |
