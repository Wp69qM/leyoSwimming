# 页面规格：U-预约页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-018 / US-029 / US-023
> **对应 PRD**：§3.5 单教练约束、§4.1 套餐类型、§4.5 体验课规则、§5.3.2 预约功能、§6.1 预约状态机、§6.3.1 预占规则、§13.3 MVP 页面清单第 13 项
> **版本**：v1.0 | **创建日期**：2026-08-02
> **特别说明**：本页复用 `U-教练排班页` 的 2D 周×时段网格，但教练已由套餐绑定确定，不可切换；必须严格处理体验课格与正价课格的可见/可约规则。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 预约页 |
| 页面名（Frame） | `U-预约页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **否**（二级页面） |
| 是否含 NavBar | **是**（白色背景，标题居中） |
| 返回按钮 | **是**（左侧 24×24px 返回箭头，返回上一页） |

---

## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [US-018](../../../docs/stories/US-018-体验课学员-预约与取消/user-story.md) / [US-029](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| **游客** | 否 | 预约必须先登录并拥有有效套餐；游客点击预约入口前置跳转 `U-微信授权页` |
| **注册用户（无套餐）** | 否 | 无 active 套餐不可预约；进入本页时提示"请先购买套餐"并跳转 `U-套餐列表页` |
| **体验课学员** | 是 | 仅可预约标记为「体验课」的可约时段（`slot.course_type = 0`） |
| **正价课学员（已绑定本教练）** | 是 | 可预约本教练正价时段（`slot.course_type = 1`），是主预约路径 |
| **正价课学员（绑定其他教练）** | 否 | 单教练约束下不可预约其他教练；进入本页时跳转 `U-换教练流程页` 或 `U-我的套餐页` |
| **教练/管理员（以用户身份进入）** | 是 | 按其实际用户身份处理，不显示管理态 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `slot_status = available` | 教练已释放且无人预约 | 格子显示「可约」态，可点击 |
| `slot_status = booked_by_me` | 当前用户已预约该时段 | 格子显示「我的」态，高亮 |
| `slot_status = full` | 已被他人预约或锁定 | 格子显示「已满」态；若允许候补则叠加「候补」入口 |
| `slot_status = closed` | 教练请假 / 闭馆 / 未释放 | 格子显示「休息」态，不可点击 |
| `slot.course_type = 0` | 体验课时段 | 仅体验课学员可点击；正价课学员点击提示"请购买体验课套餐" |
| `slot.course_type = 1` | 正价课时段 | 正价课学员可点击；体验课学员不可约 |
| `waitlist_open = true` | 满员但可排队候补 | 满员格显示橙色「候补 N」而非纯灰「已满」 |
| `release_countdown` | 处于释放倒计时窗口 | 顶部显示倒计时条 |
| `package.available_count = 0` | 可用课时不足 | 底部按钮禁用，提示"课时不足，请购买加课套餐" |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为。
> 2. 所有尺寸以 375px 设计宽度为基准；开发使用 rpx（750rpx 基准）。
> 3. 颜色/字号/圆角/阴影必须引用 `docs/figma/README.md` §1 Token，禁止硬编码。

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，背景 `#FFFFFF`，y = 0 |
| NavBar | 375×44，背景 `#FFFFFF`，底部 1px `#F0F0F0`（`color-divider`），y = 44 |
| 左侧返回按钮 | 24×24 线性返回箭头，`color-text-primary` #262626，左 padding 16px，垂直居中 |
| 标题 | "选择上课时间"，17px / Medium（`font-size-body` + `font-weight-medium`），`color-text-primary`，水平居中 |
| 右侧占位 | 空（保持视觉平衡） |
| 显示条件 | 所有身份统一显示 |
| 点击行为 | 返回上一页（支付成功页 / 我的套餐页 / 教练详情页） |

### 3.2 套餐信息卡（Package Info Card）

> **来源**：[PRD §5.3.2 第 1 条](../../../docs/prd/prd.md) 学员进入预约页默认显示已绑定教练；[US-029 §4.1](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。
> 本页教练已由套餐确定，顶部需展示当前套餐摘要，无需再次选择教练。

| 字段 | 值 |
|------|-----|
| 容器 | 343×80，x = 16，y = 96（NavBar 底部 8px 间距），圆角 12px（`radius-lg`），背景 `#FFFFFF`，`shadow-card` |
| 左侧教练个人形象照 | 48×48 圆形（`Avatar-md`），背景 `#E6F7FF`，x = 16 / y = 16 |
| 教练姓名 | 16px / Medium `color-text-primary`，位于头像右侧 12px，y = 16 |
| 套餐类型标签 | 体验课 = Success Tag "体验课"；正价课 = Primary Tag "正价课"；位于姓名右侧 8px |
| 剩余课时 | "剩余 X 节课 · 有效期至 YYYY-MM-DD"，12px / Regular `color-text-secondary`，位于姓名下方 4px |
| 右侧箭头 | 16×16 `color-text-disabled`，不点击（本页教练已锁定） |
| 显示条件 | 所有可预约身份统一显示 |
| 点击行为 | 点击卡片 → `U-教练详情页`（仅查看，不可切换） |

### 3.3 释放倒计时条（Release Countdown Banner）

> **来源**：[US-003 §4.1](../../../docs/stories/US-003-游客-查看预约释放倒计时/user-story.md) / [PRD §5.3.2 第 3-4 条](../../../docs/prd/prd.md)。
> 复用 `U-教练排班页` §3.3  countdown 条规格。

| 字段 | 值 |
|------|-----|
| 容器 | 375×40，y = 184（套餐卡底部 8px 间距），背景 `gradient-primary`（#1890FF → #0050B3） |
| 左侧图标 | 16×16 闹钟/时钟占位，白色 80% 不透明度 |
| 文案 | 窗口期文案二选一：①"距离下周预约开放还有 XX 小时"；②"今日 10:00 开放下周预约" |
| 数字 | 20px / Bold 白色（仅文案①出现），其余文字 14px / Regular 白色 |
| 关闭按钮 | 右侧 16×16 "×" 白色 60%，允许用户临时关闭（本次会话内不显示） |
| 显示条件 | 游客/注册用户/学员均按 `release_rule` 与当前时间计算；非窗口期整栏隐藏 |

### 3.4 周选择器（Week Selector）

> **来源**：[PRD §5.3.2 第 2 条](../../../docs/prd/prd.md) 可预约未来 7 天内课程。
> 复用 `U-教练排班页` §3.4 周选择器规格。

| 字段 | 值 |
|------|-----|
| 容器 | 375×52，y = 184 或 224（根据倒计时条是否显示动态下移），背景 `#FFFFFF`，底部 1px `#F0F0F0` |
| Tab 区 | 两个胶囊 Tab：「本周」「下周」，居中排列，间距 8px |
| 未选中 Tab | 高 32px，水平 padding 16px，背景 `#F5F5F5`，圆角 999px（`radius-pill`），文字 14px / Regular `color-text-secondary` |
| 选中 Tab | 高 32px，水平 padding 16px，背景 `#1890FF`，圆角 999px，文字 14px / Medium `#FFFFFF` |
| 当前周提示 | 选中 Tab 下方可显示小字 "8.2 - 8.8"，10px / Regular `color-text-secondary` |
| 显示条件 | 所有身份统一显示 |
| 点击行为 | 切换本周/下周，2D 网格内容横向滑动切换（300ms ease-out） |

### 3.5 二维预约网格（2D Booking Grid）

> **核心元素**。Y 轴为时段，X 轴为周一~周日，每个格子展示该日该时段的预约状态。
> **来源**：[PRD §5.3.2 第 8 条](../../../docs/prd/prd.md) 学员端预约卡片展示规则 / [US-029 §14.2](../../../docs/stories/US-029-学员-预约正价课程/user-story.md)。
> **硬性约束**：必须使用二维表格，禁止做成「日期 Tab + 纵向时段卡片列表」。
> 复用 `U-教练排班页` §3.5 网格结构，但本页课程类型由套餐决定。

#### A. 网格整体结构

| 字段 | 值 |
|------|-----|
| 容器（视口） | 375×460，y = 236 或 276（根据周选择器/倒计时条位置），背景 `#FFFFFF`，超出部分可滚动 |
| 视口高度 | **460px**（= 812 - 88 状态栏/NavBar - 52 周选择器 - 68 底部栏 - 104 上下间距与套餐卡/倒计时条） |
| 内容总宽度 | **384px**（= 48px frozen 时段列 + 7 × 48px 日期列） |
| 内容总高度 | **720px**（= 15 个时段行 × 48px；超出视口高度，需垂直滚动） |
| 滚动行为 | 水平滚动：日期列左右滑动，时段列保持 sticky 左侧；垂直滚动：整表上下滑动，表头保持 sticky 顶部 |
| 边框 | 所有单元格之间 1px `#F0F0F0`（`color-divider`） |

#### B. 表头行（Sticky Header Row）

| 字段 | 值 |
|------|-----|
| 容器 | 高 44px，背景 `#FFFFFF`，z-index 高于内容行 |
| 左上角占位 | 48×44，背景 `#FAFAFA`，用于与左侧时段列对齐 |
| 日期列头 | 7 列，每列 48×44，背景 `#FFFFFF`，文字居中 |
| 星期文字 | 12px / Medium `color-text-secondary`，如 "周一" |
| 日期数字 | 14px / Bold `color-text-primary`，如 "8.2"，位于星期文字下方 2px |
| 今天高亮 | 当天日期列头背景改为 `#E6F7FF`，日期数字颜色 `#1890FF` |

#### C. 左侧时段列（Sticky First Column）

| 字段 | 值 |
|------|-----|
| 容器 | 宽 48px，高 720px，背景 `#FAFAFA`，sticky 左侧 |
| 时段行 | 15 行，每行 48×48 |
| 时段文字 | 10px / Regular `color-text-secondary`，显示起始时间，如 "07:00"；垂直居中 |
| 时段范围 | 默认 07:00-08:00 至 21:00-22:00（15 个时段），后台可配置 |

#### D. 网格单元格（Grid Cell）

> **单元格尺寸**：48 × 48 px，圆角 0（表格内无圆角，仅网格最外层容器圆角 12px）。
> **单元格内边距**：4px。

| 字段 | 值 |
|------|-----|
| 单元格基础 | 48×48，背景 `#FFFFFF`，1px 右边/底边 `#F0F0F0`，内文字居中 |
| 默认字体 | 10px / Medium，行高 1.2，最多两行 |

#### E. 单元格状态定义（7 种状态）

> 在 `U-教练排班页` 6 种状态基础上，增加「体验课可约」与「正价课可约」的区分，以支持体验课规则。

| 状态 | 背景 | 文字 | 边框/装饰 | 点击行为 | 来源 |
|------|------|------|----------|---------|------|
| **正价可约 available-regular** | `#FFFFFF` | "可约" 10px Medium `#1890FF` | 1px `#1890FF` 实线边框 | 正价学员点击选中；体验课学员点击提示"请购买正价套餐" | PRD §5.3.2 第 8 条 |
| **体验可约 available-trial** | `#F6FFED` | "体验" 10px Medium `#52C41A` | 1px `#52C41A` 实线边框 | 体验课学员可点击；正价学员点击提示"请购买体验课套餐" | US-018 §4.1 |
| **我的 my-booking** | `#1890FF` | "我的" 10px Medium `#FFFFFF` | 无 | 点击可查看预约详情 / 取消入口 | US-029 §4.1 |
| **已满 full** | `#F5F5F5` | "已满" 10px Medium `#BFBFBF` | 无 | 不可预约 | PRD §5.3.2 第 8 条 |
| **休息 closed** | `#D9D9D9` | "休息" 10px Medium `#FFFFFF` | 无 | 不可点击 | US-014 / PRD §5.4.5 |
| **候补 waitlist-available** | `#FFF2E8` | "候补 N" 10px Medium `#FF6B35` | 1px `#FF6B35` 实线边框 | 点击选中，底部栏出现「加入候补」 | US-023 §4.1 |
| **不可约 disabled-for-me** | `#F5F5F5` | "—" 或锁图标 10px Medium `#BFBFBF` | 无 | 不可点击；toast 说明原因（课程类型不匹配/课时不足） | 本页新增 |

> **状态优先级**：当多个状态可能同时存在时，按以下优先级显示：
> 1. 我的预约（my-booking）
> 2. 休息（closed）
> 3. 候补（waitlist-available）
> 4. 已满（full）
> 5. 不可约（disabled-for-me）
> 6. 体验可约（available-trial）
> 7. 正价可约（available-regular）

#### F. 课程类型不匹配处理

| 字段 | 值 |
|------|-----|
| 体验课学员看到正价可约格 | 显示为「不可约」态或保持「可约」但点击后 toast "请购买正价套餐" |
| 正价课学员看到体验可约格 | 显示为「不可约」态或保持「体验」但点击后 toast "请购买体验课套餐" |
| 决策 | 为减少误点，本页采用「显示为不可约态」策略，与课时不足格统一视觉 |

### 3.6 图例说明（Legend）

> **来源**：提升用户识别效率，是 2D 网格的必要辅助。
> 复用 `U-教练排班页` §3.6 图例规格，并增加课程类型说明。

| 字段 | 值 |
|------|-----|
| 容器 | 343×48，x = 16，位于网格下方 8px，背景透明 |
| 图例项 | 横向排列，每项包含 12×12 色块 + 10px 文字，间距 10px |
| 项内容 | 可约（白底蓝框）/ 体验（绿底）/ 我的（蓝底）/ 已满（灰底）/ 休息（深灰）/ 候补（橙底） |
| 文字 | 10px / Regular `color-text-secondary` |
| 显示条件 | 所有身份统一显示；空状态时隐藏 |

### 3.7 底部固定操作栏（Bottom Action Bar）

> **来源**：[US-018 §4.1](../../../docs/stories/US-018-体验课学员-预约与取消/user-story.md) / [US-029 §4.1](../../../docs/stories/US-029-学员-预约正价课程/user-story.md) / [PRD §5.3.2 第 5-6 条](../../../docs/prd/prd.md)。

| 字段 | 值 |
|------|-----|
| 容器 | 375×68，固定底部 y = 744（812 - 68），背景 `#FFFFFF`，顶部 1px `#F0F0F0`，`shadow-float` |
| 左侧信息区 | x = 16，y = 12，宽度 220px |
| 选中时段文字 | 14px / Medium `color-text-primary`，如 "周三 08:00-09:00" |
| 课程类型提示 | 12px / Regular `color-text-secondary`，如 "体验课 · 剩余 1 节" |
| 右侧按钮 | 宽 115px，高 44px，x = 244，y = 12，圆角 8px（`radius-md`） |
| 主按钮（确认预约） | 背景 `gradient-primary`，文字 "确认预约" 16px / Medium #FFFFFF，`shadow-primary` |
| 候补按钮（加入候补） | 背景 `gradient-accent`，文字 "加入候补" 16px / Medium #FFFFFF，`shadow-accent` |
| 禁用按钮 | 背景 `#F5F5F5`，文字 `color-text-disabled` #BFBFBF，如 "课时不足" / "不可预约" |
| 显示条件 | 未选中格子时显示默认提示 "请选择一个时段"；选中后显示对应按钮；可用课时为 0 时禁用 |
| 点击行为 | 确认预约 → 调用预约 API → 成功跳转 `U-预约成功页`；加入候补 → 调用候补 API → Toast "已加入候补" |

### 3.8 课时不足提示条（Quota Warning Banner）

> **来源**：[PRD §6.3.4](../../../docs/prd/prd.md) 预占时发现可用课时不足 / [US-029 §4.2](../../../docs/stories/US-029-学员-预约正价课程/user-story.md) 异常分支 3。

| 字段 | 值 |
|------|-----|
| 容器 | 375×36，位于周选择器与网格之间，背景 `#FFF1F0` |
| 左侧图标 | 16×16 错误圆形占位，`color-error` #FF4D4F |
| 文案 | "当前套餐剩余课时不足，请购买加课套餐"，12px / Regular `color-error` |
| 右侧操作 | "去加课" 文字按钮 12px / Medium `color-primary` |
| 显示条件 | 仅当 `package.available_count = 0` 时显示 |
| 点击行为 | "去加课" → `U-套餐列表页`（已过滤当前教练） |

### 3.9 空状态 / 加载 / 错误占位

| 字段 | 值 |
|------|-----|
| 空状态 | 网格区域替换为 [README.md §2.5.9](../../../docs/figma/README.md) Empty 组件：游泳主题插画 + "暂无可约时段" + 释放倒计时提示 |
| 加载状态 | 网格区域显示骨架屏：灰色矩形模拟表头 + 6 行 6 列浅灰格子（`color-divider` #F0F0F0） |
| 错误状态 | 中央错误插画 + "加载失败，点击重试" + Primary 按钮 |
| 显示条件 | 见 §6 四态设计 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户（无套餐） | 体验课学员 | 正价课学员（绑定本教练） |
|------|:---:|:---:|:---:|:---:|
| NavBar 返回 + 标题 | — | — | ✅ | ✅ |
| 套餐信息卡 | — | — | ✅ | ✅ |
| 释放倒计时条 | — | — | ✅ | ✅ |
| 周选择器 | — | — | ✅ | ✅ |
| 2D 预约网格 | — | — | ✅ 仅体验格可约 | ✅ 仅正价格可约 |
| 图例 | — | — | ✅ | ✅ |
| 底部操作栏 | — | — | ✅ | ✅ |
| 课时不足提示条 | — | — | 按 available | 按 available |
| 空/加载/错误占位 | — | — | ✅ | ✅ |

> 图例：✅ = 显示/可操作；— = 不适用（游客/无套餐用户不会进入本页）。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户（无套餐） | 体验课学员 | 正价课学员（绑定本教练） |
|---------|------|---------|---------|---------|
| 返回按钮 | — | — | → 上一页 | → 上一页 |
| 套餐信息卡 | — | — | → `U-教练详情页` | → `U-教练详情页` |
| 可约正价格 | — | — | Toast "请购买正价套餐" | → 选中态 + 确认预约 |
| 可约体验格 | — | — | → 选中态 + 确认预约 | Toast "请购买体验课套餐" |
| 我的预约格 | — | — | → `U-预约详情页` | → `U-预约详情页` |
| 候补格 | — | — | → 选中态 + 加入候补 | → 选中态 + 加入候补 |
| 确认预约按钮 | — | — | → `U-预约成功页` | → `U-预约成功页` |
| 加入候补按钮 | — | — | → `U-候补/关注页`（可选） | → `U-候补/关注页`（可选） |
| "去加课" 文字按钮 | — | — | → `U-套餐列表页` | → `U-套餐列表页`（过滤当前教练） |

> **前置拦截**：游客/无套餐用户/绑定其他教练用户尝试进入本页时，前置跳转授权页、套餐列表页或换教练流程页。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 该教练当前周无可约时段（未释放或全部关闭） | 网格区域替换为 Empty 组件：游泳主题插画 + "暂无可约时段" + 释放倒计时提示（若处于窗口期） |
| **加载中** | 首次进入 / 切换周 / 下拉刷新 | 网格区域显示骨架屏（表头 + 6×6 灰色格子脉冲动画），底部栏显示默认占位文字 |
| **错误状态** | 网络断开 / 接口 5xx / 业务 4xx | 中央错误插画 + "网络异常，请重试" 或 "服务繁忙，请稍后重试" + Primary「重新加载」按钮 |
| **成功状态** | 数据正常返回 | 按 §4 显示规则矩阵完整展示 2D 网格、图例、底部栏；选中态按用户操作实时更新 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 学员进入预约页默认显示已绑定教练的可约时间段 | PRD §5.3.2 第 1 条 | §3.2 套餐信息卡默认展示目标教练 |
| 可预约未来 7 天内课程；每周三 10:00 释放下周时段 | PRD §5.3.2 第 2-3 条 | §3.4 周选择器；§3.3 倒计时条 |
| 同一时段 (coach_id, start_time) 唯一；多人并发仅第一个成功 | PRD §5.3.2 第 5 条 | §3.5 提交预约后锁单，失败时格子刷新为「已满」 |
| 正价套餐学员提交预约即成功并预占课时 | PRD §5.3.2 第 6 条 / §6.3.1 | §3.7 确认预约按钮直接创建 booking |
| 体验课学员仅可预约体验课时段 | PRD §4.5 / US-018 §4.1 / US-018 §4.3 | §3.5-E 体验可约格；§3.5-F 课程类型不匹配处理 |
| 已约满/锁定时段显示「已满」；支持候补 | PRD §5.3.2 第 8 条 / 第 11-13 条 | §3.5 已满格与候补格视觉区分 |
| 预约状态机：无 → 已预约 / 候补中 | PRD §6.1 | §3.5 格子状态对应 booking/waitlist 状态 |
| 课时预占规则：预约 +1 reserved | PRD §6.3.1 | §3.7 确认预约后 package.reserved +1 |
| 预占时发现可用课时不足拒绝预约 | PRD §6.3.4 / US-029 §4.2 | §3.8 课时不足提示条 |
| 单教练约束：正价学员同一时期只能绑定一个教练 | PRD §4.4 | 前置拦截：绑定其他教练用户不进入本页 |
| MVP 页面清单第 13 项「预约页」用于选时段+锁定 | PRD §13.3 | 本页 Frame 名与功能定位 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] **已创建真正的 2D 网格**：7 列日期 × 15 行时段，禁止做成列表或卡片堆叠
- [ ] 左侧时段列已设置为 sticky first column（48px 宽）
- [ ] 顶部日期行已设置为 sticky header row（44px 高）
- [ ] 每个单元格尺寸严格为 48×48px，状态颜色按 §3.5.E 执行
- [ ] 正价可约格为白底 + 1px #1890FF 边框 + "可约" 文字
- [ ] 体验可约格为 #F6FFED 绿底 + #52C41A 绿字 "体验"
- [ ] 我的预约格为 #1890FF 蓝底 + 白字 "我的"
- [ ] 已满格为 #F5F5F5 灰底 + #BFBFBF 灰字 "已满"
- [ ] 休息格为 #D9D9D9 深灰底 + 白字 "休息"
- [ ] 候补格为 #FFF2E8 橙底 + #FF6B35 橙字 "候补 N"
- [ ] 课程类型不匹配格显示为不可约态（灰底 + "—"）
- [ ] 底部栏已包含选中时段信息 + 对应操作按钮
- [ ] 已按 §5 跳转动线矩阵处理前置拦截
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含课程类型/状态后缀（如 `Cell-可约-体验课`、`Cell-不可约-正价学员看体验格`）
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] 未遗漏课时不足提示条
- [ ] 未遗漏释放倒计时条动态显隐
- [ ] 未遗漏图例说明

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：复用 `U-教练排班页` 2D 网格结构，明确教练由套餐锁定、体验课格/正价课格按身份区分、课时不足提示、确认预约跳转预约成功页 |
