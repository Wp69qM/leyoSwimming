# 页面规格：{U/C/A}-{页面名}

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：{列出影响本页面的所有 US 编号与标题}
> **对应 PRD**：{列出 PRD 章节}
> **版本**：v1.0 | **创建日期**：{YYYY-MM-DD}

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | {页面名} |
| 页面名（Frame） | `{U/C/A}-{页面名}` |
| 平台 | 微信小程序 - 用户端 / 教练端 / Web 管理端 |
| 画板尺寸 | 375 × 812 px（小程序）/ 1440 × 900 px（Web） |
| 页面背景 | `#F5F7FA` |
| 是否含 TabBar | 是 / 否 |
| 是否含 NavBar | 是 / 否 |
| 返回按钮 | 是 / 否（二级页面通常需要） |

---

## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| 游客 | 是/否 | — |
| 注册用户 | 是/否 | — |
| 学员 | 是/否 | — |
| 教练 | 是/否 | — |
| 管理员 | 是/否 | — |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `{state_1}` | — | — |
| `{state_2}` | — | — |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为
> 2. 快捷入口 ≠ TabBar 重复
> 3. 同一业务入口在不同身份下形态可能不同，必须分身份定义

### 3.1 {元素 1}

| 字段 | 值 |
|------|-----|
| 名称 | — |
| 尺寸 | — |
| 位置 | — |
| 背景/颜色 | — |
| 圆角 | — |
| 字体 | — |
| 显示条件 | — |
| 点击行为 | — |

### 3.2 {元素 2}

...

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 | 教练 | 管理员 |
|------|:---:|:---:|:---:|:---:|:---:|
| 元素 1 | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |
| 元素 2 | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ | ✅/❌ |

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员 | 教练 | 管理员 |
|---------|------|---------|------|------|--------|
| 元素 1 | → 页面 A | → 页面 A | → 页面 A | → 页面 A | → 页面 A |
| 元素 2 | → 页面 B | → 页面 B | → 页面 B | → 页面 B | → 页面 B |

> **"引导登录"**：当游客点击受登录限制的功能时，应弹出登录引导或跳转登录页，而不是直接提示错误。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| 空状态 | 无数据/未操作 | — |
| 加载中 | API 请求中 | 骨架屏或 loading |
| 错误状态 | 网络/服务错误 | 错误插画 + 重试 |
| 成功状态 | 默认 | 按元素清单完整显示 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 规则 1 | — | — |
| 规则 2 | — | — |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/边框严格按 §3 规格
- [ ] 已按 §5 跳转动线矩阵处理游客登录引导
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含显示条件后缀
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] 未遗漏任何跨页面联动规则

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | {YYYY-MM-DD} | {作者} | 初版 |
