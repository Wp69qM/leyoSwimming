# 页面规格：U-订单确认页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-017 / US-019 / US-020 / US-025 / US-045
> **对应 PRD**：§3.5 单教练约束、§4.1 套餐类型、§4.5 体验课规则、§5.3.4 购买与支付、§6.2 订单状态机、§6.9 签署流程、§13.3 MVP 页面清单第 11 项
> **版本**：v1.1 | **创建日期**：2026-08-12
> **特别说明**：本页是「购买套餐 → 生成待支付订单」的确认入口，必须同时展示套餐信息、教练信息、价格明细与协议签署，并在提交前完成所有业务校验。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 订单确认页 |
| 页面名（Frame） | `U-订单确认页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **否**（二级页面） |
| 是否含 NavBar | **是**（白色背景，标题居中） |
| 返回按钮 | **是**（左侧 24×24px 返回箭头，返回上一页） |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | U-订单确认页 | https://www.calicat.cn/design/2083742072257646592?node-id=d53b5fa3-bba6-4791-8079-5c09dc2ef376 |

---
## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [US-017](../../../docs/stories/US-017-游客-购买体验课套餐/user-story.md) / [US-020](../../../docs/stories/US-020-学员-购买正价套餐/user-story.md)。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| **游客** | 否 | 购买套餐必须先登录；从套餐详情/教练详情点击购买时统一跳转 `U-微信授权页`，不会直接进入本页 |
| **注册用户** | 是 | 可购买体验课/正价套餐；无当前绑定教练时按正常流程确认 |
| **学员（已绑定本教练）** | 是 | 为当前教练加课；顶部显示"为 教练A 加课"提示 |
| **学员（已绑定其他教练）** | 是 | 进入本页即触发单教练约束阻断；顶部显示冲突提示条，提交按钮禁用 |
| **教练/管理员（以用户身份进入）** | 是 | 按其实际用户身份处理，不显示管理态 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `package_type = 0` | 体验套餐 | 标题显示"体验课订单"，数量固定为 1 节，不可调整 |
| `package_type = 1` | 正价套餐 | 标题显示"正价课订单"，数量按标准套餐或自定义课时展示 |
| `order_source = standard` | 标准套餐（1/6/8/10 节）| 数量锁定为套餐固定课时，显示单价/总价 |
| `order_source = custom` | 自定义课时 | 数量可由用户增减，按教练参考单价实时计算总价 |
| `coach.status ≠ 1` | 教练未在职/离职审批中 | 顶部显示警告条，提交按钮置灰 |
| `user_has_other_coach_active` | 用户已绑定其他教练 active 套餐 | 顶部显示单教练冲突提示条，提交按钮禁用 |
| `agreements_signed = false` | 未勾选协议 | 提交按钮禁用，点击时提示"请先同意协议" |
| `minor_guardian_missing` | 未成年用户未填写监护人手机号 | 显示监护人输入区，未完成时提交按钮禁用 |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为。
> 2. 所有尺寸以 375px 设计宽度为基准；开发使用 rpx（750rpx 基准）。
> 3. 颜色/字号/圆角/阴影必须引用 `docs/figma/README.md` §1 Token，禁止硬编码。

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，背景 `#FFFFFF`，y = 0 |
| NavBar | 375×44，背景 `#FFFFFF`，底部 1px `#F0F0F0`（`color-divider`），y = 44 |
| 左侧返回按钮 | 24×24 线性返回箭头，`color-text-primary` #262626，左 padding 16px，垂直居中 |
| 标题 | "确认订单"，17px / Medium（`font-size-body` + `font-weight-medium`），`color-text-primary`，水平居中 |
| 右侧占位 | 空（保持视觉平衡） |
| 显示条件 | 所有身份统一显示 |
| 点击行为 | 返回上一页（套餐详情页 / 教练详情页 / 我的套餐页） |

### 3.2 教练信息卡（Coach Info Card）

> **来源**：[PRD §5.3.1 第 1 条](../../../docs/prd/prd.md) 教练资料展示；本页需让用户再次确认购买目标教练。

| 字段 | 值 |
|------|-----|
| 容器 | 343×72，x = 16，y = 96（NavBar 底部 8px 间距），圆角 12px（`radius-lg`），背景 `#FFFFFF`，`shadow-card` |
| 教练个人形象照 | 48×48 圆形（`Avatar-md`），背景 `#E6F7FF`，x = 16 / y = 12 |
| 教练姓名 | 16px / Medium `color-text-primary`，位于头像右侧 12px，y = 14 |
| 状态徽标 | 使用 [README.md §3.1](../../../docs/figma/README.md) 教练实时状态徽标：空闲中 / 上课中 / 休息中 / 已下班 / 请假中；位于姓名右侧 8px |
| 副信息 | "任教 X 年 · 评分 4.8"，12px / Regular `color-text-secondary`，位于姓名下方 4px |
| 显示条件 | 所有身份统一显示；教练信息随上一页传入 |
| 点击行为 | 点击卡片 → `U-教练详情页`（带 coach_id 参数） |

### 3.3 套餐信息卡（Package Info Card）

> **来源**：[US-017 §4.1](../../../docs/stories/US-017-游客-购买体验课套餐/user-story.md) / [US-020 §4.1](../../../docs/stories/US-020-学员-购买正价套餐/user-story.md) / [US-045 §4.1](../../../docs/stories/US-045-管理员-配置标准与自定义套餐/user-story.md) / [PRD §4.1](../../../docs/prd/prd.md) / [PRD §5.5.3](../../../docs/prd/prd.md)。
> 套餐信息卡需区分体验课与正价课、标准套餐与自定义课时。**提交订单时，系统需将套餐模板的 `package_mode`、`teaching_type`、`stroke_ids`、`total_hours`、`duration_minutes`、`valid_days`、`original_price`、`price`、`refund_enabled`、`refund_ratio`、`refund_valid_days` 等字段快照保存到订单记录中，避免后续模板变更影响本订单。**

#### A. 体验课套餐信息

| 字段 | 值 |
|------|-----|
| 容器 | 343×140，x = 16，y = 176（教练卡下方 12px），背景 `gradient-accent`，`radius-lg` 12px，`shadow-raised` |
| 套餐模式标签 | "体验课" 胶囊标签，背景 #FFFFFF 24% 不透明度，文字 12px / Medium #FFFFFF，位于左上角 12px |
| 主标题 | "1 节体验课"，20px / Bold #FFFFFF，x = 16，y = 48 |
| 教学类型/泳姿 | "一对一 · 自由泳"，14px / Regular #FFFFFF 80%，位于主标题下方 4px |
| 课时/时长/有效期 | "1 节 · 60 分钟/节 · 30 天有效"，14px / Regular #FFFFFF 80%，位于教学类型下方 4px |
| 原价/售价 | 左侧 "原价 ¥XXX" 12px / Regular #FFFFFF 60% 删除线；右侧 "售价 ¥99" 20px / Bold #FFFFFF，右侧对齐 x = 327 |
| 退款规则概要 | "开课前 X 天内可退 XX%" / "本套餐不支持退款"，12px / Regular #FFFFFF 80%，位于价格下方 4px |
| 数量 | 固定为 1，不展示数量调节器 |

#### B. 正价标准套餐信息

| 字段 | 值 |
|------|-----|
| 容器 | 343×168，x = 16，y = 176，背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "正价套餐"，16px / Medium `color-text-primary`，顶部 padding 16px，左侧 padding 16px |
| 套餐模式标签 | "正价套餐" 胶囊标签，背景 `color-primary` 浅色，文字 12px / Medium `color-primary`，位于区标题右侧 12px |
| 套餐名称 | "X 节标准课"，16px / Medium `color-text-primary`，y = 48，x = 16 |
| 教学类型/泳姿 | "一对一 · 自由泳/蛙泳"，12px / Regular `color-text-secondary`，位于套餐名称下方 4px |
| 课时/时长/有效期 | "X 节 · 60 分钟/节 · 有效期 Y 天"，12px / Regular `color-text-secondary`，位于教学类型下方 4px |
| 原价/售价 | 左侧 "原价 ¥XXXX" 12px / Regular `color-text-secondary` 删除线；右侧 "售价 ¥XXXX" 20px / Bold `color-text-primary` |
| 退款规则概要 | "开课前 X 天内可退 XX%" / "本套餐不支持退款"，12px / Regular `color-text-secondary` |
| 数量 | 锁定为套餐固定课时，右侧显示 "X 节" 14px / Medium `color-text-primary` |

#### C. 自定义课时信息

| 字段 | 值 |
|------|-----|
| 容器 | 343×192，x = 16，y = 176，背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "自定义课时"，16px / Medium `color-text-primary`，顶部 padding 16px |
| 套餐模式标签 | "正价套餐" 胶囊标签，背景 `color-primary` 浅色，文字 12px / Medium `color-primary`，位于区标题右侧 12px |
| 参考单价说明 | "按教练参考单价 ¥XXX/节 计算"，12px / Regular `color-text-secondary` |
| 教学类型/泳姿 | 显示当前教练默认教学类型与可选泳姿，12px / Regular `color-text-secondary` |
| 课时/时长/有效期 | "X 节 · 60 分钟/节 · 有效期 Y 天"，12px / Regular `color-text-secondary` |
| 原价/售价 | 左侧 "原价 ¥XXXX" 12px / Regular `color-text-secondary` 删除线；右侧 "售价 ¥XXXX" 20px / Bold `color-text-primary` |
| 退款规则概要 | "开课前 X 天内可退 XX%" / "本套餐不支持退款"，12px / Regular `color-text-secondary` |
| 数量调节器 | 横向步进器：减号按钮 32×32 / 数量显示 48×32 / 加号按钮 32×32；最小 1，最大由教练可售课时上限决定 |
| 调节器位置 | 右侧，y = 112 |
| 实时总价 | "合计 ¥XXXX"，20px / Bold `color-text-primary`，底部 padding 16px |

### 3.4 价格明细区（Price Breakdown）

> **来源**：[PRD §5.3.4](../../../docs/prd/prd.md) 购买套餐生成订单；价格明细必须透明展示。

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应，x = 16，y = （套餐卡下方 12px），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "价格明细"，16px / Medium `color-text-primary`，顶部 padding 16px |
| 商品总价行 | 左侧 "商品总价" 14px / Regular `color-text-secondary`；右侧 "¥XXXX" 14px / Medium `color-text-primary` |
| 优惠减免行 | 左侧 "优惠减免" 14px / Regular `color-text-secondary`；右侧 "-¥0" 14px / Medium `color-success`；无优惠时隐藏 |
| 分割线 | 1px `#F0F0F0`，左右 padding 16px |
| 应付总额行 | 左侧 "应付总额" 16px / Medium `color-text-primary`；右侧 "¥XXXX" 24px / Bold `color-error` |
| 底部 padding | 16px |
| 显示条件 | 所有订单类型统一显示 |

### 3.5 协议签署区（Agreement Section）

> **来源**：[PRD §6.9](../../../docs/prd/prd.md) 签署流程 / [US-017 §4.1](../../../docs/stories/US-017-游客-购买体验课套餐/user-story.md) / [US-020 §4.1](../../../docs/stories/US-020-学员-购买正价套餐/user-story.md)。
> 体验课仅需《用户须知》；正价课需《用户须知》+ 健康承诺书 + 免责协议。

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应，x = 16，y = （价格明细区下方 12px），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "购买协议"，16px / Medium `color-text-primary`，顶部 padding 16px |
| 复选框组 | 每项 40px 高，左侧 20×20 复选框，右侧协议文案 14px / Regular `color-text-primary` |
| 《用户须知》 | "我已阅读并同意《用户须知》"，"《用户须知》"为 14px / Medium `color-primary` 文字按钮；内嵌退款规则说明 |
| 退款规则说明 | 在《用户须知》中展示：当 `refund_enabled=true` 时显示“开课前 `refund_valid_days` 天内可申请退款，退款比例 `refund_ratio%`”；`refund_enabled=false` 时显示“本套餐不支持退款”。文案 12px / Regular `color-text-secondary`，位于用户须知下方 4px |
| 健康承诺书 | 仅正价课显示："我已阅读并同意《健康承诺书》" |
| 免责协议 | 仅正价课显示："我已阅读并同意《免责协议》" |
| 未勾选提示 | 点击提交时若有未勾选项，toast "请先同意全部协议" |
| 底部 padding | 16px |
| 显示条件 | 所有登录用户统一显示；游客不会进入本页 |

### 3.6 未成年人监护人区（Guardian Section）

> **来源**：[PRD §6.9](../../../docs/prd/prd.md) 未成年人保护 / [US-020 §4.1](../../../docs/stories/US-020-学员-购买正价套餐/user-story.md) 异常分支 4。
> 仅当用户年龄 < 18 岁且购买正价课时显示。

| 字段 | 值 |
|------|-----|
| 容器 | 343×128，x = 16，y = （协议区下方 12px），背景 `#FFF2E8`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "监护人手机号"，16px / Medium `color-text-primary`，顶部 padding 16px |
| 说明文案 | "您尚未成年，需填写监护人手机号并验证"，12px / Regular `color-text-secondary` |
| 手机号输入框 | 311×44，y = 52，背景 #FFFFFF，边框 1px `color-border`，圆角 8px，placeholder "请输入监护人手机号" |
| 验证码行 | 验证码输入框 191×44 + 获取验证码按钮 110×44，间距 10px |
| 获取验证码按钮 | Secondary 按钮样式，文字 "获取验证码"，倒计时 60s 时显示 "XXs 后重试" |
| 显示条件 | 仅正价课 + 用户未成年 + 未验证监护人时显示 |

### 3.7 业务警告条（Business Warning Banner）

> **来源**：[PRD §3.5](../../../docs/prd/prd.md) 单教练约束 / [PRD §4.5](../../../docs/prd/prd.md) 体验课规则 / [US-020 §4.2](../../../docs/stories/US-020-学员-购买正价套餐/user-story.md) 异常分支 2。

#### A. 单教练冲突提示条

| 字段 | 值 |
|------|-----|
| 容器 | 343×48，x = 16，背景 `color-warning` 浅色 `#FFFBE6`，`radius-lg` 8px |
| 左侧图标 | 16×16 警告三角占位，`color-warning` #FAAD14 |
| 文案 | "您已绑定教练李教练，暂不可购买其他教练套餐"，14px / Regular `color-warning` |
| 右侧操作 | "更换教练" 文字按钮 14px / Medium `color-primary` |
| 显示条件 | 仅学员已绑定其他教练时显示 |
| 点击行为 | "更换教练" → `U-换教练流程页`（US-022） |

#### B. 教练不可用提示条

| 字段 | 值 |
|------|-----|
| 容器 | 343×40，x = 16，背景 `#FFFBE6`，`radius-lg` 8px |
| 左侧图标 | 16×16 警告三角占位，`color-warning` #FAAD14 |
| 文案 | "该教练暂不可购买，请选择其他教练"，14px / Regular `color-warning` |
| 显示条件 | coach.status ≠ 1 时显示 |

#### C. 体验课已购买提示条

| 字段 | 值 |
|------|-----|
| 容器 | 343×40，x = 16，背景 `#FFF2E8`，`radius-lg` 8px |
| 左侧图标 | 16×16 信息图标占位，`color-accent` #FF6B35 |
| 文案 | "您已持有体验套餐，无需重复购买"，14px / Regular `color-accent` |
| 右侧操作 | "去预约" 文字按钮 14px / Medium `color-primary` |
| 显示条件 | 用户已有 active/exhausted 体验套餐时显示；本页应被前置拦截，若进入则显示此条 |
| 点击行为 | "去预约" → `U-教练排班页` |

#### D. 套餐已下架提示条

| 字段 | 值 |
|------|-----|
| 容器 | 343×40，x = 16，背景 `#FFF2E8`，`radius-lg` 8px |
| 左侧图标 | 16×16 警告三角占位，`color-error` #FF4D4F |
| 文案 | "该套餐已下架，请重新选择"，14px / Regular `color-error` |
| 显示条件 | 进入本页时检测到 `package_template.status ≠ 'active'` 或模板已被删除 |
| 交互影响 | 底部"提交订单"按钮禁用；点击提示条返回上一页 |

### 3.8 底部固定提交栏（Bottom Submit Bar）

> **来源**：[US-017 §4.1](../../../docs/stories/US-017-游客-购买体验课套餐/user-story.md) / [US-020 §4.1](../../../docs/stories/US-020-学员-购买正价套餐/user-story.md) / [PRD §6.2](../../../docs/prd/prd.md) 订单状态机。

| 字段 | 值 |
|------|-----|
| 容器 | 375×64 + 底部安全区 34px（总高 98px），固定底部 y = 714，背景 #FFFFFF，`shadow-float` |
| 左侧价格区 | x = 16，y = 12 |
| 应付总额标签 | "应付总额" 12px / Regular `color-text-secondary` |
| 应付总额数字 | "¥XXXX" 24px / Bold `color-error`，位于标签下方 2px |
| 右侧提交按钮 | 宽 160px，高 44px，x = 199，y = 12，圆角 8px（`radius-md`） |
| 主按钮（提交订单） | 背景 `gradient-primary`，文字 "提交订单" 16px / Medium #FFFFFF，`shadow-primary` |
| 禁用按钮 | 背景 `#F5F5F5`，文字 `color-text-disabled` #BFBFBF，如 "提交订单" |
| 显示条件 | 正常状态显示主按钮；单教练冲突/教练不可用/套餐已下架/未签协议/未验证监护人时按钮禁用 |
| 点击行为 | 提交订单 → 校验模板仍上架 → 调用创建订单 API（将当前模板字段快照写入订单）→ 成功跳转 `U-支付页`（带 order_id 参数）；若模板已下架则 toast "该套餐已下架，请重新选择" |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员（本教练） | 学员（其他教练） |
|------|:---:|:---:|:---:|:---:|
| NavBar 返回 + 标题 | — | ✅ | ✅ | ✅ |
| 教练信息卡 | — | ✅ | ✅ | ✅ |
| 套餐信息卡 | — | ✅ | ✅ | ✅ |
| 价格明细区 | — | ✅ | ✅ | ✅ |
| 协议签署区 | — | ✅ | ✅ | ✅ |
| 未成年人监护人区 | — | ✅（未成年正价课） | ✅（未成年正价课） | ✅（未成年正价课） |
| 单教练冲突提示条 | — | ❌ | ❌ | ✅ |
| 教练不可用提示条 | — | 按 coach.status | 按 coach.status | 按 coach.status |
| 体验课已购买提示条 | — | 按体验套餐状态 | 按体验套餐状态 | 按体验套餐状态 |
| 底部提交栏 | — | ✅ | ✅ | ✅（按钮禁用） |

> 图例：✅ = 显示/可操作；❌ = 隐藏；— = 不适用（游客不会进入本页）。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员（本教练） | 学员（其他教练） |
|---------|------|---------|------------|------------|
| 返回按钮 | — | → 上一页 | → 上一页 | → 上一页 |
| 教练信息卡 | — | → `U-教练详情页` | → `U-教练详情页` | → `U-教练详情页` |
| 《用户须知》文字按钮 | — | → 协议详情页（WebView） | 同左 | 同左 |
| 《健康承诺书》文字按钮 | — | → 协议详情页 | 同左 | 同左 |
| 《免责协议》文字按钮 | — | → 协议详情页 | 同左 | 同左 |
| "更换教练" 文字按钮 | — | — | — | → `U-换教练流程页`（US-022） |
| "去预约" 文字按钮 | — | → `U-教练排班页` | 同左 | 同左 |
| 提交订单按钮 | — | → `U-支付页`（order_id） | → `U-支付页`（order_id） | 禁用，无跳转 |

> **游客拦截**：游客点击购买入口时，前置跳转 `U-微信授权页`，不会到达本页。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 无套餐信息传入（如直接 deep link 进入缺少参数） | 中央 Empty 组件：插画 + "暂无订单信息" + "返回首页" 按钮（→ `U-首页`） |
| **加载中** | 首次进入 / 拉取教练/套餐详情 / 提交订单中 | 卡片区域显示骨架屏（浅灰 #F5F5F5 矩形占位）；提交按钮显示 loading 态 |
| **错误状态** | 网络断开 / 接口 5xx / 业务 4xx（如套餐已下架） | 中央错误插画 + "加载失败，点击重试" + Primary「重新加载」按钮；套餐模板下架或不存在时显示 "该套餐已下架，请重新选择" |
| **成功状态** | 数据正常返回 | 按 §4 显示规则矩阵完整展示；提交成功后跳转支付页 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 游客购买需先登录 | PRD §5.1 / US-017 §6.3 | 本页不直接对游客可见，前置跳转授权页 |
| 体验套餐 1 节/30 天，同用户最多 1 份 active/exhausted | PRD §4.1 / §4.5 / US-017 §3 | §3.3-A 体验课数量锁定为 1；§3.7-C 已购买提示 |
| 正价套餐分标准套餐与自定义课时 | PRD §5.5.3 / US-019 §4.1 | §3.3-B 标准套餐卡 / §3.3-C 自定义课时卡 |
| 单教练约束：同一用户 active 套餐只能绑定一名教练 | PRD §3.5 / §4.4 / US-020 §3 | §3.7-A 冲突提示条 + §3.8 提交按钮禁用 |
| 购买前必须签署《用户须知》等协议 | PRD §6.9 / US-017 §4.1 / US-020 §4.1 | §3.5 协议签署区 |
| 未成年人购买正价课需监护人手机号与短信校验 | PRD §6.9 / US-020 §4.1 | §3.6 监护人区 |
| 订单状态机：提交后 order.status = 待支付 | PRD §6.2 / US-020 §4.1 | §3.8 提交后跳转支付页 |
| 教练 status ≠ 1 时不可购买 | US-017 §4.2 / US-020 §4.2 | §3.7-B 教练不可用提示条 |
| 标准套餐课时数固定，自定义课时按参考单价计算 | PRD §5.5.3 / US-019 §4.1 | §3.3-B 数量锁定 / §3.3-C 数量调节器 |
| 已购套餐保存模板快照，避免后续模板变更影响历史订单 | US-045 §5 规则 5 | §3.3 套餐信息卡；§3.8 提交订单快照 |
| 套餐模板下架时不可提交订单 | US-045 §4.2 / 本 US 新增 | §3.7-D 套餐已下架提示条；§3.8 / §6 错误状态 |
| 购买须知中需展示退款规则 | US-045 §4.1 | §3.5 协议签署区退款规则说明 |
| MVP 页面清单第 11 项「订单确认页」用于选择教练+课时 | PRD §13.3 | 本页 Frame 名与功能定位 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/边框严格按 §3 规格
- [ ] 已按 §5 跳转动线矩阵处理游客前置拦截
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含身份/状态后缀（如 `PackageCard-体验课`、`WarningBanner-单教练冲突`）
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] 未遗漏单教练冲突提示条与提交按钮禁用态
- [ ] 未遗漏协议签署区复选框与未勾选提示
- [ ] 未遗漏未成年人监护人手机号输入区
- [ ] 体验课数量已锁定为 1 节，不展示调节器
- [ ] 自定义课时已展示数量步进器与实时总价
- [ ] 已展示套餐模式标签、教学类型、泳姿、课时数、每节课时长、有效期、原价/售价、退款规则概要
- [ ] 已明确提交订单时保存模板字段快照
- [ ] 已处理套餐已下架异常状态与提示

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.1 | 2026-08-12 | PM | 适配 US-045：套餐信息卡增加模式标签、教学类型、泳姿、课时/时长/有效期、原价/售价、退款规则；提交订单时快照保存模板字段；增加套餐已下架异常状态；购买须知中增加退款规则说明 |
| v1.0 | 2026-08-02 | PM | 初版：覆盖游客/注册用户/学员（本教练/其他教练）四种身份，区分体验课/正价标准套餐/自定义课时三种订单类型，包含协议签署、监护人验证、单教练冲突、价格明细与底部提交栏 |
