# 页面规格：U-支付页

> **性质**：Figma 设计交付的唯一指令源。AI 生成 Figma 时**必须**按本规格严格执行。
> **核心约束**：本规格的每一条元素显隐规则必须可追溯到 [PRD](../../prd/prd.md) 或具体 US 的某条业务规则。
> **归属 US**：US-017 / US-020 / US-025
> **对应 PRD**：§3.3 身份转换、§4.2 套餐状态机、§5.3.4 购买与支付、§6.2 订单状态机、§12.2 支付链路、§13.3 MVP 页面清单第 12 项
> **版本**：v1.0 | **创建日期**：2026-08-02
> **特别说明**：MVP 仅支持微信支付；支付页是支付流程关键页，必须清晰展示订单号、应付金额、支付倒计时，避免用户误操作。

---

## 1. 页面基础

| 字段 | 值 |
|------|-----|
| 页面名（中文） | 支付页 |
| 页面名（Frame） | `U-支付页` |
| 平台 | 微信小程序 - 用户端 |
| 画板尺寸 | 375 × 812 px |
| 页面背景 | `#F5F7FA`（`color-bg-page`） |
| 是否含 TabBar | **否** |
| 是否含 NavBar | **是**（白色背景，标题居中，**无返回按钮**） |
| 返回按钮 | **否**（支付流程关键页，防止误触返回导致重复支付） |

---

## 1.1 Calicat 设计稿链接

| 页面/状态 | Calicat Frame | 链接 |
|----------|---------------|------|
| 主页面 | U-支付页 | https://www.calicat.cn/design/2083742072257646592?node-id=968b0e7c-bbde-4c65-89af-c81d2d677e31 |

---
## 2. 用户身份与业务状态维度

> **本节是 Page Spec 根基**。先定义"谁可能看到这个页面"以及"以什么业务状态看到"。
> 身份定义来源：[PRD §3.2](../../../docs/prd/prd.md) 与 [US-025](../../../docs/stories/US-025-学员-支付套餐订单/user-story.md)。

### 2.1 适用身份

| 身份 | 是否可见 | 差异说明 |
|------|:-------:|---------|
| **游客** | 否 | 支付必须先登录；游客不会直接到达本页 |
| **注册用户** | 是 | 支付体验课/正价套餐订单 |
| **学员** | 是 | 为当前教练加课支付或首次购买支付 |
| **教练/管理员（以用户身份进入）** | 是 | 按其实际用户身份处理，不显示管理态 |

### 2.2 关键业务状态

| 状态 | 含义 | 对本页影响 |
|------|------|-----------|
| `order.status = 1` 待支付 | 订单未支付 | 正常展示支付信息，按钮可点击 |
| `order.status = 3` 已取消 | 订单超时或用户主动取消 | 进入本页时检测到此状态，跳转订单详情并提示"订单已取消" |
| `order.status = 2` 已支付 | 订单已支付 | 进入本页时检测到此状态，直接跳转 `U-预约成功页` 或 `U-我的套餐页` |
| `payment_countdown > 0` | 订单仍在 24h 有效期内 | 顶部显示倒计时；倒计时结束自动触发订单取消 |
| `payment_countdown = 0` | 订单已过期 | 按钮禁用，提示"订单已过期，请重新下单" |
| `pay_channel = wechat` | 微信支付（MVP 唯一） | 支付方式区仅显示微信支付，支付宝入口隐藏/禁用 |
| `paying = true` | 前端已调起微信支付 SDK | 主按钮显示 loading，防止重复点击 |

---

## 3. 元素清单（自上而下）

> **设计原则**：
> 1. 每个元素必须说明：名称、尺寸、位置、颜色、圆角、字体、显示条件、交互行为。
> 2. 所有尺寸以 375px 设计宽度为基准；开发使用 rpx（750rpx 基准）。
> 3. 颜色/字号/圆角/阴影必须引用 `docs/figma/README.md` §1 Token，禁止硬编码。

### 3.1 状态栏 + NavBar

| 字段 | 值 |
|------|-----|
| 状态栏 | 375×44，背景 `#FFFFFF`，y = 0 |
| NavBar | 375×44，背景 `#FFFFFF`，底部 1px `#F0F0F0`（`color-divider`），y = 44 |
| 标题 | "支付订单"，17px / Medium（`font-size-body` + `font-weight-medium`），`color-text-primary`，水平居中 |
| 返回按钮 | 无 |
| 右侧占位 | 空（保持视觉平衡） |
| 显示条件 | 所有身份统一显示 |
| 点击行为 | 无返回按钮；用户可通过系统手势返回，但前端需拦截并二次确认 |

### 3.2 支付金额区（Payment Amount Header）

> **来源**：[US-025 §4.1](../../../docs/stories/US-025-学员-支付套餐订单/user-story.md) / [PRD §5.3.4](../../../docs/prd/prd.md)。
> 支付金额区是本页视觉焦点，需突出显示应付总额。

| 字段 | 值 |
|------|-----|
| 容器 | 375×180，y = 88，背景 `gradient-primary`（#1890FF → #0050B3） |
| 订单号标签 | "订单编号"，12px / Regular #FFFFFF 80%，居中，y = 28 |
| 订单号 | order.order_no，14px / Medium #FFFFFF，居中，y = 46 |
| 复制按钮 | "复制" 文字按钮 12px / Medium #FFFFFF 80%，位于订单号右侧 8px |
| 金额标签 | "应付金额"，12px / Regular #FFFFFF 80%，居中，y = 82 |
| 金额数字 | "¥XXXX"，40px / Bold #FFFFFF，居中，y = 102 |
| 倒计时标签 | "支付剩余时间"，12px / Regular #FFFFFF 80%，居中，y = 146 |
| 倒计时数字 | "XX:XX:XX"，20px / Bold #FFFFFF，居中，y = 162 |
| 显示条件 | 待支付/已支付/已取消均显示；已过期时倒计时显示 "00:00:00" 并标红 |
| 点击行为 | 点击订单号复制按钮 → Toast "订单号已复制" |

### 3.3 订单信息卡（Order Info Card）

> **来源**：[US-025 §4.1](../../../docs/stories/US-025-学员-支付套餐订单/user-story.md) / [PRD §6.2](../../../docs/prd/prd.md) 订单状态机。

| 字段 | 值 |
|------|-----|
| 容器 | 343×120，x = 16，y = 284（金额区下方 16px），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 左侧套餐图标 | 48×48 圆形，背景 `color-primary` 浅色 #E6F7FF，内放 "课" 字 20px / Medium `color-primary` |
| 套餐名称 | 16px / Medium `color-text-primary`，位于图标右侧 12px，y = 20 |
| 套餐描述 | "教练：张教练 · X 节课"，12px / Regular `color-text-secondary`，位于套餐名称下方 4px |
| 订单状态标签 | 使用 [README.md §3.3](../../../docs/figma/README.md) 订单状态徽标：待支付 #FFFBE6/#FAAD14，位于右侧 16px，y = 20 |
| 分割线 | 1px `#F0F0F0`，位于 y = 72 |
| 创建时间行 | 左侧 "创建时间" 12px / Regular `color-text-secondary`；右侧 order.created_at 12px / Regular `color-text-primary` |
| 有效期行 | 左侧 "有效期至" 12px / Regular `color-text-secondary`；右侧 order.expire_at 12px / Regular `color-text-primary` |
| 底部 padding | 16px |
| 显示条件 | 所有订单状态统一显示 |

### 3.4 支付方式区（Payment Method Section）

> **来源**：[PRD §5.3.4](../../../docs/prd/prd.md) 支持微信支付、支付宝支付；但 **MVP 任务约束：仅微信支付**。
> 支付宝入口在 MVP 中**不显示**，避免用户误选。

| 字段 | 值 |
|------|-----|
| 容器 | 343×自适应，x = 16，y = （订单信息卡下方 12px），背景 `#FFFFFF`，`radius-lg` 12px，`shadow-card` |
| 区标题 | "支付方式"，16px / Medium `color-text-primary`，顶部 padding 16px |
| 微信支付项 | 311×56，x = 16，y = 48，垂直居中 |
| 微信图标 | 32×32 圆形占位，`color-success` #52C41A 背景，内放 "微" 字 16px / Medium #FFFFFF |
| 微信支付文案 | "微信支付"，16px / Medium `color-text-primary`，位于图标右侧 12px |
| 推荐标签 | "推荐" 胶囊标签，背景 `color-success` 浅色，文字 `color-success`，位于文案右侧 8px |
| 单选按钮 | 右侧 20×20 圆形，选中态为 `color-primary` 填充 + 白色对勾 |
| 支付宝支付项 | **MVP 隐藏**，不显示 |
| 底部说明 | "MVP 阶段仅支持微信支付"，12px / Regular `color-text-secondary`，底部 padding 16px |
| 显示条件 | 待支付状态显示；已支付/已取消/已过期时隐藏 |

### 3.5 支付安全提示（Security Notice）

> **来源**：[PRD §12.2](../../../docs/prd/prd.md) 支付链路具备重试与对账机制。

| 字段 | 值 |
|------|-----|
| 容器 | 343×40，x = 16，y = （支付方式区下方 12px），背景 `#F6FFED`，`radius-lg` 8px |
| 左侧图标 | 16×16 对勾圆形占位，`color-success` #52C41A |
| 文案 | "支付安全由微信官方保障，请放心支付"，12px / Regular `color-success` |
| 显示条件 | 待支付状态显示 |

### 3.6 底部固定支付栏（Bottom Payment Bar）

> **来源**：[US-025 §4.1](../../../docs/stories/US-025-学员-支付套餐订单/user-story.md) / [PRD §6.2](../../../docs/prd/prd.md) 订单状态机。

| 字段 | 值 |
|------|-----|
| 容器 | 375×64 + 底部安全区 34px（总高 98px），固定底部 y = 714，背景 #FFFFFF，`shadow-float` |
| 左侧价格区 | x = 16，y = 12 |
| 应付标签 | "应付" 12px / Regular `color-text-secondary` |
| 应付金额 | "¥XXXX" 24px / Bold `color-error`，位于标签右侧 8px，同一行基线对齐 |
| 右侧支付按钮 | 宽 160px，高 44px，x = 199，y = 12，圆角 8px（`radius-md`） |
| 主按钮（确认支付） | 背景 `gradient-primary`，文字 "确认支付" 16px / Medium #FFFFFF，`shadow-primary` |
| 加载按钮 | 背景 `gradient-primary`，文字左侧 16×16 loading 占位，显示 "支付中..." |
| 禁用按钮 | 背景 `#F5F5F5`，文字 `color-text-disabled` #BFBFBF，如 "订单已过期" |
| 显示条件 | 待支付且倒计时 > 0 时显示主按钮；支付调起中显示加载按钮；过期/已取消显示禁用按钮 |
| 点击行为 | 确认支付 → 调用微信支付统一下单 → 调起微信 SDK → 成功回调后跳转 `U-预约成功页` 或 `U-我的套餐页` |

### 3.7 支付结果提示区（Payment Result Overlay）

> **来源**：[US-025 §4.2](../../../docs/stories/US-025-学员-支付套餐订单/user-story.md) 异常分支。
> 支付成功/失败/取消后，本页以 overlay 或全屏形式展示结果，再跳转后续页面。

#### A. 支付成功

| 字段 | 值 |
|------|-----|
| 容器 | 全屏 overlay，背景 #FFFFFF 95% 不透明度 |
| 成功图标 | 80×80 圆形，`color-success` #52C41A 背景，内放对勾 40px / Bold #FFFFFF |
| 主文案 | "支付成功"，24px / Bold `color-text-primary`，居中 |
| 副文案 | "您已获得 X 节课程，快去预约吧"，14px / Regular `color-text-secondary` |
| 跳转倒计时 | "3 秒后自动跳转..."，12px / Regular `color-text-secondary` |
| 显示条件 | 微信支付回调成功 |

#### B. 支付失败 / 取消

| 字段 | 值 |
|------|-----|
| 容器 | 底部弹层（Bottom Sheet），高 240px，背景 #FFFFFF，`radius-xl` 16px 顶部圆角 |
| 标题 | "支付未完成"，18px / Medium `color-text-primary`，居中 |
| 说明 | "您可在 24 小时内继续支付，逾期订单将自动取消"，14px / Regular `color-text-secondary` |
| 主按钮 | "重新支付"，全宽 343px，`gradient-primary` |
| 次按钮 | "返回订单详情"，全宽 343px，Secondary 样式 |
| 显示条件 | 用户取消支付或支付失败 |

### 3.8 空状态 / 加载 / 错误占位

| 字段 | 值 |
|------|-----|
| 空状态 | 无 order_id 传入时显示 Empty 组件：插画 + "暂无订单信息" + "返回首页" 按钮（→ `U-首页`） |
| 加载状态 | 页面首次加载时显示骨架屏：金额区渐变占位 + 卡片灰色矩形占位 |
| 错误状态 | 中央错误插画 + "加载失败，点击重试" + Primary 按钮；订单不存在时显示 "订单不存在" |
| 显示条件 | 见 §6 四态设计 |

---

## 4. 显示规则总表

| 元素 | 游客 | 注册用户 | 学员 |
|------|:---:|:---:|:---:|
| NavBar 标题（无返回） | — | ✅ | ✅ |
| 支付金额区 | — | ✅ | ✅ |
| 订单信息卡 | — | ✅ | ✅ |
| 支付方式区（仅微信） | — | ✅ | ✅ |
| 支付安全提示 | — | ✅ | ✅ |
| 底部支付栏 | — | ✅ | ✅ |
| 支付成功 overlay | — | ✅ | ✅ |
| 支付失败 bottom sheet | — | ✅ | ✅ |
| 空/加载/错误占位 | — | ✅ | ✅ |

> 图例：✅ = 显示/可操作；— = 不适用（游客不会进入本页）。

---

## 5. 跳转动线矩阵

| 起点元素 | 游客 | 注册用户 | 学员 |
|---------|------|---------|------|
| 系统手势返回 | — | 弹二次确认 "是否放弃支付？" | 同左 |
| 复制订单号 | — | Toast "订单号已复制" | 同左 |
| 订单信息卡 | — | → `U-订单详情页`（order_id） | 同左 |
| 确认支付按钮 | — | 调起微信支付 SDK | 同左 |
| 支付成功 overlay | — | → `U-预约成功页`（体验课/首次正价课）或 `U-我的套餐页`（加课） | 同左 |
| 重新支付按钮 | — | 重新调起微信支付 SDK | 同左 |
| 返回订单详情按钮 | — | → `U-订单详情页`（order_id） | 同左 |
| 订单过期提示 | — | → `U-套餐列表页` / `U-教练详情页` 重新下单 | 同左 |

> **游客拦截**：游客不会直接到达本页；若通过深链进入，检测未登录后跳转 `U-微信授权页`。

---

## 6. 四态设计

| 状态 | 触发 | 表现 |
|------|------|------|
| **空状态** | 无 order_id 传入或订单不存在 | 中央 Empty 组件：插画 + "暂无订单信息" + "返回首页" 按钮（→ `U-首页`） |
| **加载中** | 首次进入 / 查询订单状态 / 调起支付 SDK 前 | 金额区显示渐变占位；卡片区域显示骨架屏（浅灰 #F5F5F5 矩形占位） |
| **错误状态** | 网络断开 / 接口 5xx / 订单不存在 / 支付渠道异常 | 中央错误插画 + "加载失败，点击重试" + Primary「重新加载」按钮；支付渠道异常时显示 "支付服务繁忙，请稍后重试" |
| **成功状态** | 订单待支付且倒计时 > 0 | 按 §4 显示规则矩阵完整展示；支付成功后显示成功 overlay 并自动跳转 |

---

## 7. 业务规则引用

| 规则 | PRD / US 引用 | 在本页面应用 |
|------|--------------|-------------|
| 游客支付需先登录 | PRD §5.1 / US-025 §3 | 本页不直接对游客可见 |
| MVP 仅支持微信支付（任务约束） | PRD §5.3.4 / 本批次全局规则 | §3.4 支付方式区仅显示微信支付 |
| 订单状态机：待支付 → 已支付 / 已取消 | PRD §6.2 / US-025 §4.1 | §3.2 / §3.6 根据状态显示不同按钮 |
| 订单 24h 支付有效期 | PRD §6.2 / US-025 §4.2 | §3.2 倒计时展示；§3.6 倒计时结束后按钮禁用 |
| 支付成功后创建 active 套餐并升级身份 | PRD §3.3 / §4.2 / US-025 §4.1 | §3.7-A 支付成功提示 + 跳转预约页 |
| 支付回调幂等处理 | PRD §12.2 / US-025 §4.2 | §3.7-A 成功 overlay 仅展示一次 |
| 支付失败保持待支付状态 | US-025 §4.2 | §3.7-B 失败 bottom sheet |
| MVP 页面清单第 12 项「支付页」用于微信/支付宝支付 | PRD §13.3 | 本页 Frame 名与功能定位；MVP 仅微信 |

---

## 8. AI 生成检查清单

- [ ] 已读取本文档（Page Spec）
- [ ] 已读取 v1.3 全局设计规范（`docs/figma/README.md`）
- [ ] 已按 §2 身份维度决定 §4 显示矩阵
- [ ] 所有元素尺寸/颜色/圆角/边框严格按 §3 规格
- [ ] 已按 §5 跳转动线矩阵处理系统返回拦截
- [ ] 已按 §6 状态矩阵预留 4 态视觉占位
- [ ] 元素命名包含状态后缀（如 `AmountHeader-待支付`、`Button-确认支付-可用`）
- [ ] 页面整体风格符合「泳池蓝 + 活力橙」主题
- [ ] 已使用 Auto Layout 实现居中对齐
- [ ] **NavBar 无返回按钮**
- [ ] **支付方式区仅显示微信支付，支付宝入口已移除/隐藏**
- [ ] 订单号、应付金额、支付倒计时三大要素已展示
- [ ] 倒计时归零后支付按钮已置灰并提示订单过期
- [ ] 支付成功 overlay 与失败 bottom sheet 已定义
- [ ] 未遗漏支付安全提示

---

## 9. 变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-08-02 | PM | 初版：MVP 仅微信支付，突出订单号/金额/倒计时，定义支付成功 overlay 与失败 bottom sheet，NavBar 无返回按钮 |
