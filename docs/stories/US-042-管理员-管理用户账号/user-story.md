# US-042 管理员管理用户账号

> **状态**：[REVIEW]（评审中）
> **优先级**：[MVP]
> **估时**：1 人天
> **作者**：PM　|　**最后更新**：2026-07-30
> **配套文档**：Figma：[待设计填写]　·　技术设计：[./tech-design.md](./tech-design.md)　·　测试计划：[./test-plan.md](./test-plan.md)

---

## 1. 基本信息

| 字段 | 值 |
|------|----|
| **编号** | US-042 |
| **标题** | 管理员管理用户账号 |
| **角色（Actor）** | 管理员（主）、系统（辅） |
| **业务价值（Why）** | 让管理员可查看、筛选用户列表，并在必要时重置密码/手机号/邮箱，维护账号角色权限 |
| **优先级** | [MVP] |
| **估时** | 1 人天 |
| **配套文档** | Figma / 技术设计 / 测试计划（链接见上方） |

---

## 2. 触发条件

- **触发方**：管理员
- **触发动作**：在管理后台「用户管理 → 用户账号」进行查询、重置或修改操作
- **触发时机**：管理员主动操作

---

## 3. 前置条件

- [x] 管理员已登录且具有 `MANAGE_USER_ACCOUNT` 权限
- [x] 目标用户账号存在于系统中
- [x] 操作符合 RBAC 约束（例如不能修改超级管理员账号）

---

## 4. 业务流程

### 4.1 主路径

1. 管理员进入「用户管理 → 用户账号」
2. 系统展示用户列表，支持按身份、注册时间筛选
3. 管理员搜索/筛选目标用户
4. 管理员点击用户进入详情/编辑页
5. 管理员执行以下任一操作：
   - 重置密码：系统生成随机密码并强制首次登录修改
   - 修改手机号：校验新手机号唯一性后更新
   - 修改邮箱：校验新邮箱唯一性后更新
   - 配置角色权限：勾选角色并保存
6. 系统记录操作审计日志
7. 返回操作成功提示

### 4.2 异常分支

- **分支 1**：管理员无权限 → 返回错误 `ADMIN_PERMISSION_DENIED`
- **分支 2**：目标用户不存在 → 返回错误 `USER_NOT_FOUND`
- **分支 3**：新手机号/邮箱已被占用 → 返回错误 `PHONE_ALREADY_EXISTS` / `EMAIL_ALREADY_EXISTS`

---

## 5. 业务规则引用

| # | 规则 | 章节 |
|---|------|------|
| 1 | 管理员可查看所有游客、注册用户，按身份、注册时间筛选与统计 | [§5.5.1](../../prd/prd.md) |
| 2 | 管理员可重置密码、手机号、邮箱 | [§5.5.1](../../prd/prd.md) |
| 3 | 管理员可配置角色权限 | [§5.5.1](../../prd/prd.md) |
| 4 | 敏感操作需记录审计日志 | [§11.1](../../prd/prd.md) |

---

## 6. 验收标准（业务级 Gherkin）

> 本 US 为 L2（1 人天），包含 2 个正常场景 + 3 个异常场景 = 5 个场景。

### 6.1 场景 1：管理员重置用户密码

```gherkin
Given 管理员 M 已登录且具有用户管理权限
And   用户 U 的手机号为"13800138000"
When  管理员 M 对用户 U 点击「重置密码」
Then  系统生成随机密码并更新 user.password_hash
And   user.force_change_password = true
And   audit_log 新增 1 条 action='ADMIN_RESET_PASSWORD' 记录
And   返回 HTTP 200 与提示"密码已重置，用户首次登录需修改密码"
```

### 6.2 场景 2：管理员修改用户手机号

```gherkin
Given 管理员 M 已登录且具有用户管理权限
And   用户 U 当前手机号为"13800138000"
And   新手机号"13900139000"未被占用
When  管理员 M 将用户 U 手机号修改为"13900139000"
Then  user.phone 更新为"13900139000"
And   audit_log 新增 1 条 action='ADMIN_UPDATE_PHONE' 记录
And   返回 HTTP 200 与提示"手机号已更新"
```

### 6.3 场景 3：无权限管理员操作失败

```gherkin
Given 管理员 M2 已登录但无用户管理权限
When  管理员 M2 调用用户列表或重置密码接口
Then  返回错误码 ADMIN_PERMISSION_DENIED
And   HTTP 状态码 403
And   不修改任何用户数据
```

### 6.4 场景 4：目标用户不存在

```gherkin
Given 管理员 M 已登录且具有用户管理权限
And   用户 ID 999999 不存在
When  管理员 M 查询或修改该用户
Then  返回错误码 USER_NOT_FOUND
And   HTTP 状态码 404
```

### 6.5 场景 5：手机号已被占用

```gherkin
Given 管理员 M 已登录且具有用户管理权限
And   用户 U 当前手机号为"13800138000"
And   手机号"13900139000"已被用户 U2 占用
When  管理员 M 将用户 U 手机号修改为"13900139000"
Then  返回错误码 PHONE_ALREADY_EXISTS
And   HTTP 状态码 409
And   user.phone 保持"13800138000"不变
```

---

## 7. 数据/API/状态机影响

### 7.1 数据表影响

| # | 表名 | 操作 | 说明 |
|---|------|------|------|
| 1 | `user` | 修改 | 密码、手机号、邮箱、角色等字段 |
| 2 | `user_role` | 修改 | 用户角色关联表 |
| 3 | `audit_log` | 新增 | 记录敏感操作 |

### 7.2 API 影响

| # | API | 方法 | 操作 | 说明 |
|---|-----|------|------|------|
| 1 | `/api/admin/v1/users` | GET | 新增 | 查询用户列表 |
| 2 | `/api/admin/v1/users/{id}` | GET | 新增 | 查看用户详情 |
| 3 | `/api/admin/v1/users/{id}/reset-password` | POST | 新增 | 重置密码 |
| 4 | `/api/admin/v1/users/{id}/phone` | PUT | 新增 | 修改手机号 |
| 5 | `/api/admin/v1/users/{id}/email` | PUT | 新增 | 修改邮箱 |
| 6 | `/api/admin/v1/users/{id}/roles` | PUT | 新增 | 配置角色权限 |

### 7.3 状态机影响

| # | 实体 | 转换 | 触发条件 | 说明 |
|---|------|------|---------|------|
| 1 | — | — | — | 本 US 不涉及业务状态机转换 |

---

## 8. 边界场景

### 8.1 边界场景 1：并发修改同一用户手机号

- **触发条件**：两个管理员同时修改同一用户手机号
- **预期行为**：使用乐观锁，后到请求返回 `USER_CONCURRENTLY_UPDATED`，先到请求成功
- **用户可见反馈**：第二次操作提示「用户信息已被他人更新，请刷新后重试」

### 8.2 边界场景 2：重置密码后用户立即登录

- **触发条件**：管理员重置密码后，用户用新密码登录
- **预期行为**：系统强制用户首次登录修改密码
- **用户可见反馈**：登录后跳转密码修改页

### 8.3 边界场景 3：批量导出用户列表

- **触发条件**：管理员需要导出筛选后的用户列表
- **预期行为**：系统支持按当前筛选条件导出 CSV（最多 10000 条），异步生成下载链接
- **用户可见反馈**：提示"导出任务已创建，完成后可下载"

---

## 9. 依赖关系

### 9.1 前置 US（本故事依赖）

- [x] US-004（用户微信授权登录）/ US-006（用户手机号/账号密码登录）—— 产生 user 记录

### 9.2 后续 US（依赖本故事）

- [ ] US-047（管理员配置场馆、公告等）—— 同属于管理后台用户管理模块

---

## 10. INVEST 自检

- [x] **I**ndependent（独立）- 可独立交付，不依赖其他后台 US
- [x] **N**egotiable（可协商）- 字段与角色可在设计中细化
- [x] **V**aluable（有价值）- 支撑管理后台基础用户运营
- [x] **E**stimable（可估算）- 1 人天，范围明确
- [x] **S**mall（足够小）- 一个 Sprint 内可完成
- [x] **T**estable（可测试）- 5 个 GWT 场景可客观验证

---

## 11. 完整性检查

### 11.1 字段完整性

- [x] 15 个章节全部填写
- [x] 无"待定"/"TBD"占位符（除 Figma 链接状态待设计填写）
- [x] 错误码明确（ADMIN_PERMISSION_DENIED / USER_NOT_FOUND / PHONE_ALREADY_EXISTS）

### 11.2 业务规则

- [x] 引用 §5.5.1 / §11.1
- [x] 与 RBAC 权限规则一致
- [x] 与审计日志规则一致

### 11.3 验收标准

- [x] 2 正常 + 3 异常 = 5 个 GWT 场景
- [x] 每个 Then 含具体状态码 / DB 字段值
- [x] 业务规则可被验证

### 11.4 配套文档

- [x] Figma 链接/状态已列出
- [x] 技术设计文档已创建
- [x] 测试计划已创建

---

## 12. 备注

- **幂等键**：修改类接口使用 `admin:user:{user_id}:version:{user.updated_at}` 乐观锁
- **事务边界**：手机号/邮箱更新与审计日志写入在同一事务
- **性能要求**：用户列表 P99 < 200ms，详情/修改接口 P99 < 200ms
- **安全**：重置密码必须生成强随机密码，首次登录强制修改，操作记录审计日志

---

## 13. Figma 链接

> Figma **设计系统规范**见 [docs/spec/figma/README.md](../../spec/figma/README.md)。

| # | 内容 | 链接 / node-id | 状态 |
|---|------|---------------|------|
| 1 | 用户账号列表页 Figma file URL | `https://www.figma.com/file/leyoSwimming/admin-user-list` | 🔲 待设计填写 |
| 2 | 用户详情/编辑页 Figma file URL | `https://www.figma.com/file/leyoSwimming/admin-user-detail` | 🔲 待设计填写 |
| 3 | 重置密码确认弹窗 frame node-id | `admin-user-detail:reset-password` | 🔲 待设计填写 |

### 13.1 状态截图清单

| 页面 | 空状态 | 加载状态 | 错误状态 | 成功状态 | 备注 |
|------|--------|---------|---------|---------|------|
| **用户账号列表** | 🔲 | 🔲 | 🔲 | 🔲 | 空状态为无匹配用户 |
| **用户详情/编辑** | — | 🔲 | 🔲 | 🔲 | 错误状态含字段校验提示 |

---

## 14. 页面级设计决策

### 14.1 敏感操作二次确认

- **背景**：重置密码/修改手机号属于高风险操作
- **选项**：A. 直接执行；B. 弹窗二次确认并记录原因
- **结论**：选择 B，降低误操作风险
- **影响范围**：用户详情页操作按钮

### 14.2 角色权限配置方式

- **背景**：需要灵活配置管理员/客服等角色
- **选项**：A. 下拉单选；B. 多选框
- **结论**：选择 B，支持一个用户多个角色
- **影响范围**：用户详情页角色区域

### 14.3 页面级交互说明

| 交互 | 触发 | 反馈 | 备注 |
|------|------|------|------|
| 筛选用户 | 选择身份/时间 | 列表实时刷新 | 支持组合筛选 |
| 重置密码 | 点击按钮 | 二次确认弹窗 → 成功提示 | 不展示明文密码 |
| 修改手机号 | 保存 | 唯一性校验 → 成功/失败提示 | |
| 配置角色 | 勾选角色 | 保存后生效 | |

---

## 15. 设计评审记录

| 日期 | 评审人 | 反馈 | 处置 |
|------|--------|------|------|
| 2026-07-30 | PM | 重置密码后是否强制首次登录修改 | 已在 §6.1 明确 force_change_password=true |

---

## 附录：变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-07-30 | PM | 初版 |

---

<p align="right">
<sub>本文档遵循 <a href="../../spec/user-story/SPECIFICATION.md">US 拆分规范</a>　·　复制自 <a href="../../spec/user-story/TEMPLATE.md">TEMPLATE.md</a></sub>
</p>
