# US-046 管理员查看与处理订单

> **状态**：[REVIEW]（评审中）
> **优先级**：[MVP]
> **估时**：1.5 人天
> **作者**：PM　|　**最后更新**：2026-07-30
> **配套文档**：Figma：[待设计填写]　·　技术设计：[./tech-design.md](./tech-design.md)　·　测试计划：[./test-plan.md](./test-plan.md)

---

## 1. 基本信息

| 字段 | 值 |
|------|----|
| **编号** | US-046 |
| **标题** | 管理员查看与处理订单 |
| **角色（Actor）** | 管理员（主）、系统（辅）|
| **业务价值（Why）** | 管理员可在后台查看全部订单并处理退款/争议，保障交易闭环 |
| **优先级** | [MVP] |
| **估时** | 1.5 人天 |
| **配套文档** | Figma / 技术设计 / 测试计划（链接见上）|

---

## 2. 触发条件

- **触发方**：管理员
- **触发动作**：登录 Web 管理后台，进入「订单管理」
- **触发时机**：需要查看订单、处理退款、处理争议退款时

---

## 3. 前置条件

- [x] 管理员已登录且具有「订单管理」权限
- [x] 订单数据已产生（依赖 US-025 学员支付套餐订单）
- [x] 退款申请数据已产生（依赖 US-027 学员申请退款）

---

## 4. 业务流程

### 4.1 主路径

1. 管理员进入「订单管理」页面
2. 系统展示订单列表（分页、筛选、排序）
3. 管理员点击某订单查看详情
4. 系统展示订单状态、支付信息、关联套餐、退款记录
5. 管理员执行处理：确认退款 / 拒绝退款 / 标记异常
6. 系统更新订单状态并触发后续流程（原路退回、通知用户等）

### 4.2 异常分支

- **分支 1**：订单状态不允许当前操作 → 返回 `ORDER_STATUS_INVALID`
- **分支 2**：退款金额超过已支付金额 → 返回 `REFUND_AMOUNT_EXCEEDED`
- **分支 3**：管理员无权限 → 返回 HTTP 403

---

## 5. 业务规则引用

| # | 规则 | 章节 |
|---|------|------|
| 1 | 管理员查看所有订单，确认或拒绝订单 | [§5.5.3](../../prd/prd.md) |
| 2 | 订单状态机 | [§6.2](../../prd/prd.md) |
| 3 | 退款规则与原路退回 | [§6.4](../../prd/prd.md) |
| 4 | 争议退款/申诉处理 | [§6.10](../../prd/prd.md) |

---

## 6. 验收标准（业务级 Gherkin）

> 本 US 为 L2（1.5 人天），场景数 = 2 正常 + 3 异常 = 5。

### 6.1 场景 1：管理员查看订单列表与详情

```gherkin
Given 系统中存在 3 笔已支付订单
When  管理员进入订单管理页并选择"已支付"筛选
Then  列表展示 3 笔订单，每行展示订单号、学员、教练、金额、状态
And   管理员点击订单 O-001
Then  详情页展示订单 O-001 的支付流水、套餐信息、退款记录
```

### 6.2 场景 2：管理员批准退款

```gherkin
Given 存在一笔状态为"退款审批中"的订单 R-001，paid_amount=2000.00
When  管理员批准该退款
Then  系统返回 HTTP 200
And   order.status 更新为"已退款"
And   package.status 更新为"refunded"
And   生成退款记录 refund.amount=2000.00
And   向学员发送退款到账通知
```

### 6.3 场景 3：管理员拒绝退款

```gherkin
Given 存在一笔状态为"退款审批中"的订单 R-002
When  管理员拒绝退款并填写原因"未提供有效凭证"
Then  系统返回 HTTP 200
And   order.status 更新为"退款被拒"
And   package.status 保持"active"
And   学员端显示"退款未通过，原因为：未提供有效凭证"
```

### 6.4 场景 4：管理员对非退款审批中订单执行退款

```gherkin
Given 存在一笔状态为"已取消"的订单 C-001
When  管理员尝试批准该订单退款
Then  系统返回 HTTP 400
And   返回错误码 ORDER_STATUS_INVALID
And   订单状态不变
```

### 6.5 场景 5：退款金额超过已支付金额

```gherkin
Given 存在一笔状态为"退款审批中"的订单 R-003，paid_amount=1000.00
When  管理员提交退款金额 1200.00
Then  系统返回 HTTP 400
And   返回错误码 REFUND_AMOUNT_EXCEEDED
And   提示"退款金额不能超过已支付金额"
```

---

## 7. 数据/API/状态机影响

### 7.1 数据表影响

| # | 表名 | 操作 | 说明 |
|---|------|------|------|
| 1 | `order` | 读取/修改 | 查询订单、更新 status |
| 2 | `package` | 读取/修改 | 退款批准后 status → refunded |
| 3 | `refund_record` | 新增 | 记录退款金额、原因、处理人 |
| 4 | `audit_log` | 新增 | 记录管理员操作日志 |

### 7.2 API 影响

| # | API | 方法 | 操作 | 说明 |
|---|-----|------|------|------|
| 1 | `/api/admin/orders` | GET | 新增 | 订单列表查询 |
| 2 | `/api/admin/orders/:id` | GET | 新增 | 订单详情 |
| 3 | `/api/admin/orders/:id/approve-refund` | POST | 新增 | 批准退款 |
| 4 | `/api/admin/orders/:id/reject-refund` | POST | 新增 | 拒绝退款 |

### 7.3 状态机影响

| # | 实体 | 转换 | 触发条件 | 说明 |
|---|------|------|---------|------|
| 1 | `order.status` | 退款审批中 → 已退款 | 管理员批准 | 触发原路退回 |
| 2 | `order.status` | 退款审批中 → 退款被拒 | 管理员拒绝 | 恢复 package 可约课 |
| 3 | `package.status` | active → refunded | 退款完成 | 身份重算异步触发 |

---

## 8. 边界场景

### 8.1 边界场景 1：同一订单重复提交退款审批

- **触发条件**：管理员连续点击"批准退款"
- **预期行为**：幂等处理，第二次返回成功但不重复生成退款记录
- **用户可见反馈**：提示"该退款已处理"

### 8.2 边界场景 2：退款审批期间学员再次约课

- **触发条件**：订单处于退款审批中，学员仍尝试约课
- **预期行为**：系统阻止新预约并提示退款处理中
- **用户可见反馈**："退款处理中，暂不可约课"

### 8.3 边界场景 3：争议退款超过 3 工作日未处理

- **触发条件**：争议退款工单超期
- **预期行为**：系统标记超时并通知高级管理员
- **用户可见反馈**：后台订单详情显示"已超期，请尽快处理"

---

## 9. 依赖关系

### 9.1 前置 US（本故事依赖）

- [x] US-025（学员支付套餐订单）
- [x] US-027（学员申请退款）

### 9.2 后续 US（依赖本故事）

- [ ] 无

---

## 10. INVEST 自检

- [x] **I**ndependent（独立）- 可独立交付，依赖订单与退款数据已存在
- [x] **N**egotiable（可协商）- UI 展示字段可协商
- [x] **V**aluable（有价值）- 完成交易闭环
- [x] **E**stimable（可估算）- 1.5 人天明确
- [x] **S**mall（足够小）- 一个 Sprint 内可完成
- [x] **T**estable（可测试）- 5 个 GWT 场景可验证

---

## 11. 完整性检查

### 11.1 字段完整性

- [x] 15 个章节全部填写
- [x] 无"待定"/"TBD"占位符（除 Figma 链接待设计填写）
- [x] 错误码明确（ORDER_STATUS_INVALID / REFUND_AMOUNT_EXCEEDED / FORBIDDEN）

### 11.2 业务规则

- [x] 引用 §5.5.3 / §6.2 / §6.4 / §6.10
- [x] 与订单状态机一致
- [x] 与退款规则一致

### 11.3 验收标准

- [x] 2 正常 + 3 异常 GWT
- [x] 每个 Then 含具体数值/状态码/错误码/DB 字段值
- [x] 业务规则可被验证

### 11.4 配套文档

- [x] Figma 链接/状态明确
- [x] 技术设计文档链接/状态明确
- [x] 测试计划链接/状态明确

---

## 12. 备注

- **幂等键**：退款审批接口使用 `Idempotency-Key: {admin_id}:{order_id}:approve-refund`
- **事务边界**：订单状态更新、package 状态更新、退款记录写入在同一事务
- **性能要求**：订单列表接口 P99 < 300ms

---

## 13. Figma 链接

> Figma **设计系统规范**见 [docs/spec/figma/README.md](../../spec/figma/README.md)。

| # | 内容 | 链接 / node-id | 状态 |
|---|------|---------------|------|
| 1 | 订单管理页 Figma file URL | 🔲 待设计填写 | 🔲 |
| 2 | 订单详情页 Figma file URL | 🔲 待设计填写 | 🔲 |
| 3 | 退款处理弹窗 frame node-id | 🔲 待设计填写 | 🔲 |

### 13.1 状态截图清单

| 页面 | 空状态 | 加载状态 | 错误状态 | 成功状态 | 备注 |
|------|--------|---------|---------|---------|------|
| **订单管理页** | 🔲 | 🔲 | 🔲 | 🔲 | 空状态提示"暂无订单" |
| **订单详情页** | — | 🔲 | 🔲 | 🔲 | 展示退款审批按钮 |

---

## 14. 页面级设计决策

### 14.1 订单列表默认按创建时间倒序

- 支持按状态、教练、学员、时间范围筛选
- 每页默认 20 条

### 14.2 退款审批需二次确认

- 批准/拒绝前弹出确认框，展示影响范围
- 争议退款需上传处理说明

### 14.3 页面级交互说明

| 交互 | 触发 | 反馈 | 备注 |
|------|------|------|------|
| 点击订单行 | 行点击 | 跳转详情页 | — |
| 批准退款 | 按钮 | 二次确认 → 状态更新 | 触发通知 |
| 拒绝退款 | 按钮 | 弹出原因输入框 | 必填原因 |

---

## 15. 设计评审记录

| 日期 | 评审人 | 反馈 | 处置 |
|------|--------|------|------|
| 🔲 | 🔲 | 🔲 | 🔲 |

---

## 附录：变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-07-30 | PM | 初版 |

---

<p align="right">
<sub>本文档遵循 <a href="../../spec/user-story/SPECIFICATION.md">US 拆分规范</a>　·　复制自 <a href="../../spec/user-story/TEMPLATE.md">TEMPLATE.md</a>　·　填写示例见 <a href="../../spec/user-story/EXAMPLE.md">EXAMPLE.md</a></sub>
</p>
