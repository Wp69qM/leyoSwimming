# US-030 学员取消正价课程

> **状态**：[REVIEW]（评审中）
> **优先级**：[MVP]
> **估时**：1.5 人天
> **作者**：PM　|　**最后更新**：2026-07-30
> **配套文档**：Figma：[待设计填写]　·　技术设计：[./tech-design.md](./tech-design.md)　·　测试计划：[./test-plan.md](./test-plan.md)

---

## 1. 基本信息

| 字段 | 值 |
|------|----|
| **编号** | US-030 |
| **标题** | 学员取消正价课程 |
| **角色（Actor）** | 学员（主）、教练（辅）|
| **业务价值（Why）** | 让学员在不同时间窗内按规则取消正价课程并释放/预占课时，减少纠纷 |
| **优先级** | [MVP] |
| **估时** | 1.5 人天 |
| **配套文档** | Figma / 技术设计 / 测试计划（链接见上方）|

> **范围说明**：本 US 仅覆盖「取消预约」（含 24h 内/外分支与教练审批）。原「改约」能力（取消后再次发起预约）由独立 US-031（教练代约/改约正价课程）承接，避免单 US 承担双重职责、状态机复杂化。

---

## 2. 触发条件

- **触发方**：学员
- **触发动作**：在预约详情页点击「取消预约」
- **触发时机**：课程开始前且 booking 状态为 已预约 / 待上课 时

---

## 3. 前置条件

- [x] 学员已登录（依赖 US-004 / US-005）
- [x] 存在 booking.status ∈ {已预约, 待上课}（依赖 US-029）
- [x] 当前时间早于课程开始时间
- [x] 该 booking 无待处理的取消申请

---

## 4. 业务流程

### 4.1 主路径

1. 学员在「我的预约」中点击目标课程
2. 系统展示取消规则：
   - 开课前 ≥24 小时：可直接取消，课时立即释放
   - 开课前 <24 小时：需提交教练审批
3. 学员确认取消并选择原因
4. 系统判断时间窗：
   - **≥24 小时**：booking.status → 已取消；package.reserved_count -1，available_count +1；通知教练
   - **<24 小时**：创建 booking_cancellation.status = 待审批；通知教练在 24 小时内审批
5. 返回处理结果

### 4.2 异常分支

- **分支 1**：booking 已取消 / 已完成 / 旷课 → 返回 `BOOKING_NOT_CANCELLABLE`
- **分支 2**：课程已开始或已结束 → 返回 `BOOKING_STARTED`
- **分支 3**：已存在待审批的取消申请 → 返回 `CANCEL_REQUEST_IN_PROGRESS`
- **分支 4**：非本人操作 → 返回 403
- **分支 5**：因特殊原因（如场馆闭馆、教练离职等）被取消 → 学员可在 US-049 客服工单中申诉

---

## 5. 业务规则引用

| # | 规则 | 章节 |
|---|------|------|
| 1 | 开课前 24 小时外可随时取消，释放预占课时 | [§5.3.2](../../prd/prd.md) |
| 2 | 开课前 24 小时内取消需教练审批 | [§5.3.2](../../prd/prd.md) |
| 3 | 教练 24 小时内未处理视为拒绝 | [§5.3.2](../../prd/prd.md) |
| 4 | 课时预占回滚规则 | [§6.3.1](../../prd/prd.md) |
| 5 | 预约状态机 | [§6.1](../../prd/prd.md) |

---

## 6. 验收标准（业务级 Gherkin）

### 6.1 场景 1：开课前 24 小时外直接取消

```gherkin
Given 学员已登录
And   存在 booking.status = 已预约，开课时间为明日 10:00
And   当前时间为今日 9:00（距开课 25 小时）
And   package.reserved_count = 1，available_count = 4
When  学员点击「取消预约」
Then  booking.status = 已取消
And   package.reserved_count = 0，available_count = 5
And   系统通知教练
And   返回 HTTP 200
```

### 6.2 场景 2：开课前 24 小时内提交取消申请

```gherkin
Given 学员已登录
And   存在 booking.status = 已预约，开课时间为今日 22:00
And   当前时间为今日 10:00（距开课 12 小时）
When  学员点击「取消预约」并选择原因"临时有事"
Then  系统创建 booking_cancellation.status = 待审批
And   booking.status 保持已预约
And   package 课时暂不变
And   系统通知教练审批
And   返回 HTTP 201
```

### 6.3 场景 3：教练同意 24 小时内取消申请

```gherkin
Given 学员已提交待审批的取消申请
And   booking.status = 已预约
And   package.reserved_count = 1，available_count = 4
When  教练点击「同意取消」
Then  booking.status = 已取消
And   package.reserved_count = 0，available_count = 5
And   booking_cancellation.status = 已通过
And   返回 HTTP 200
```

### 6.4 场景 4：取消申请超时未处理

```gherkin
Given 学员提交的取消申请已等待 24 小时
And   教练未审批
When  系统定时任务执行
Then  booking_cancellation.status = 已拒绝（超时）
And   booking.status 保持已预约
And   package 课时不变
And   学员收到"取消申请已超时"通知
```

### 6.5 场景 5：<2h 特殊原因取消申诉（PRD §5.3.2 第9条 / §6.5.1 / §6.5.2）

```gherkin
Given 学员已登录
And   存在 booking.status = 已预约，开课时间为今日 12:00
And   当前时间为今日 10:30（距开课 1.5 小时，< 2h 阈值）
And   学员此前已提交 24h 内取消申请，但被教练拒绝
And   package.reserved_count = 1，available_count = 4
When  学员在预约详情页点击「特殊原因申诉」并提交原因"突发疾病" + 上传医院证明
Then  系统创建 dispute_refund.status = 争议退款处理中
And   booking.status 保持已预约（不立即释放课时）
And   package 课时暂不变
And   系统通知管理员在 3 工作日内审核
And   返回 HTTP 201
```

### 6.6 场景 6：重复取消

```gherkin
Given booking.status = 已取消
When  学员再次点击「取消预约」
Then  系统返回 HTTP 400，错误码 BOOKING_NOT_CANCELLABLE
And   不创建新的取消申请
```

---

## 7. 数据/API/状态机影响

### 7.1 数据表影响

| # | 表名 | 操作 | 说明 |
|---|------|------|------|
| 1 | `booking` | 修改 | status → 已取消（≥24h 或教练同意）|
| 2 | `package` | 修改 | reserved_count -1，available_count +1 |
| 3 | `booking_cancellation` | 新增/修改 | 记录取消申请与审批结果 |

### 7.2 API 影响

| # | API | 方法 | 操作 | 说明 |
|---|-----|------|------|------|
| 1 | `/api/bookings/{booking_id}/cancel` | POST | 新增 | 学员提交取消 |
| 2 | `/api/coach/cancellation-requests/{id}/approve` | POST | 新增 | 教练同意取消 |
| 3 | `/api/coach/cancellation-requests/{id}/reject` | POST | 新增 | 教练拒绝取消 |

### 7.3 状态机影响

| # | 实体 | 转换 | 触发条件 | 说明 |
|---|------|------|---------|------|
| 1 | `booking` | 已预约 → 已取消 | ≥24h 直接取消 或 教练同意 | 释放课时 |
| 2 | `package` | reserved → available | 取消成功 | 课时回滚 |
| 3 | `booking_cancellation` | 无 → 待审批 → 已通过/已拒绝 | 学员提交 / 教练审批 | 24h 内审批 |

---

## 8. 边界场景

### 8.1 边界场景 1：刚好卡在 24 小时边界

- **触发条件**：当前时间距开课正好 24 小时
- **预期行为**：按 ≥24h 规则处理，可直接取消
- **用户可见反馈**：立即释放课时

### 8.2 边界场景 2：教练审批前课程已开始

- **触发条件**：取消申请待审批，但已到开课时间
- **预期行为**：自动将申请标记为无效，booking 进入待上课/上课中状态
- **用户可见反馈**：提示"课程已开始，取消申请自动关闭"

### 8.3 边界场景 3：并发取消与教练确认

- **触发条件**：学员取消与教练确认上课同时发生
- **预期行为**：通过 booking 状态乐观锁保证只有一种结果
- **用户可见反馈**：以最终状态为准提示用户

### 8.4 边界场景 4：因特殊原因被取消后的申诉

- **触发条件**：因场馆闭馆、教练离职等特殊原因，管理员/系统取消学员 booking
- **预期行为**：学员收到取消通知，并可在 US-049 客服工单中提交申诉
- **用户可见反馈**：预约详情页展示「取消原因」与「申诉」入口

---

## 9. 依赖关系

### 9.1 前置 US（本故事依赖）

- [x] US-004（用户微信授权登录）
- [x] US-005（用户补充注册资料）
- [x] US-029（学员预约正价课程）

### 9.2 后续 US（依赖本故事）

- [ ] US-031（教练代约/改约正价课程）
- [ ] US-033（教练确认上课记录）

---

## 10. INVEST 自检

- [x] **I**ndependent（独立）- 依赖 US-029，但取消逻辑可独立交付
- [x] **N**egotiable（可协商）- 原因选项、审批时长可协商
- [x] **V**aluable（有价值）- 保障学员取消权益，减少客诉
- [x] **E**stimable（可估算）- 1.5 人天明确
- [x] **S**mall（足够小）- 仅覆盖正常取消与教练审批
- [x] **T**estable（可测试）- 6 个 GWT 场景可客观验证

---

## 11. 完整性检查

### 11.1 字段完整性

- [x] 15 个章节全部填写
- [x] 无"待定"/"TBD"占位符
- [x] 错误码明确

### 11.2 业务规则

- [x] 引用 §5.3.2 / §6.5.1 / §6.3.1 / §6.1
- [x] 24 小时边界处理正确

### 11.3 验收标准

- [x] 2 正常 + 4 异常 GWT（含 <2h 特殊原因申诉）
- [x] 每个 Then 含具体数值/状态码/DB 字段值
- [x] 业务规则可被验证

### 11.4 配套文档

- [x] 链接明确

---

## 12. 备注

- **幂等键**：`idempotency_key = {user_id}:{booking_id}:cancel`
- **事务边界**：booking 状态更新 + package 释放 + cancellation 创建在同一事务
- **性能要求**：取消接口 P99 < 200ms，教练审批接口 P99 < 200ms
- **定时任务**：每分钟扫描超 24h 未审批的取消申请，标记为已拒绝（超时）

---

## 13. Figma 链接

> Figma **设计系统规范**见 [docs/spec/figma/README.md](../../spec/figma/README.md)。

| # | 内容 | 链接 / node-id | 状态 |
|---|------|---------------|------|
| 1 | 预约详情页 Figma file URL | 🔲 待设计填写 | 🔲 |
| 2 | 取消原因弹窗 Figma file URL | 🔲 待设计填写 | 🔲 |

### 13.1 状态截图清单

| 页面 | 空状态 | 加载状态 | 错误状态 | 成功状态 | 备注 |
|------|--------|---------|---------|---------|------|
| **预约详情页** | — | ✅ | ✅ | ✅ | |
| **取消原因弹窗** | — | ✅ | ✅ | ✅ | |

---

## 14. 页面级设计决策

### 14.1 取消按钮状态

- 开课前 ≥24h：按钮文案"取消预约"，点击后直接确认
- 开课前 <24h：按钮文案"申请取消"，点击后展示原因选择并提示需教练审批

### 14.2 审批状态展示

- 学员端展示取消申请状态：待审批 / 已通过 / 已拒绝
- 拒绝时展示原因与"联系教练"入口（用户可手动重新预约，改约流程由 US-031 承接）

---

## 15. 设计评审记录

| 日期 | 评审人 | 反馈 | 处置 |
|------|--------|------|------|
| 🔲 | 🔲 | 🔲 | 🔲 |

---

## 附录：变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.0 | 2026-07-30 | PM | 初版 |
| v1.1 | 2026-07-30 | PM | P0 修复：标题移除「/改约」，拆分改约能力至 US-031；§5 移除 §6.5.1 引用；§14.2 移除改约入口跳转 |
| v1.2 | 2026-07-31 | PM | v5 评审修复：§4.2/§8.4 增加特殊原因取消后学员可在 US-049 申诉 |

---

<p align="right">
<sub>本文档遵循 <a href="../../spec/user-story/SPECIFICATION.md">US 拆分规范</a>　·　复制自 <a href="../../spec/user-story/TEMPLATE.md">TEMPLATE.md</a></sub>
</p>
