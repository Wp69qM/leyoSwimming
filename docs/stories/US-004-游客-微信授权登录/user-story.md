# US-004 游客微信授权登录

> **状态**：[REVIEW]（评审中）
> **优先级**：[MVP]
> **估时**：1 人天
> **作者**：PM　|　**最后更新**：2026-07-31

---

## 1. 基本信息

| 字段 | 值 |
|------|----|
| **编号** | US-004 |
| **标题** | 游客微信授权登录 |
| **角色（Actor）** | 游客（主）、系统（辅） |
| **业务价值（Why）** | 微信授权登录是游客进入小程序交易流程的入口，首次登录后通过补充资料完成注册，是后续所有业务（购买套餐、预约、上课）的前置条件 |
| **优先级** | [MVP] |
| **估时** | 1 人天 |
| **配套文档** | [技术设计文档](./tech-design.md)　·　[测试计划](./test-plan.md)　·　[全局设计规范](../../figma/README.md) |

---

## 2. 触发条件

- **触发方**：游客（微信小程序访客）
- **触发动作**：
  1. 游客首次打开小程序并需要使用受登录限制的功能（如购买套餐、预约教练）
  2. 游客在「我的」页面发现为未登录态，看到默认头像和「请登录」按钮，主动点击「请登录」按钮
  3. 游客主动点击登录页「微信一键登录」按钮
- **触发时机**：游客主动操作；或受登录态保护的功能被访问时由系统引导跳转

> **角色命名说明**（v7 评审 P1-4 修复）：本 US 触发方统一为"游客"，与目录名 `US-004-游客-微信授权登录`、PRD 游客浏览主线、§9.2 身份状态机起点（游客 → 注册用户）保持一致。已注册用户登录态过期后重新登录也走本 US 流程，但触发身份仍归为"游客"（未登录态）。GWT 场景中的"用户"为行为主体泛指，不与角色名冲突。

---

## 3. 前置条件

- [x] 游客已安装微信并打开小程序
- [x] 游客已授权小程序获取基本信息（`scope.userInfo`）
- [x] 游客已授权小程序获取手机号（`scope.phoneNumber`）
- [x] 游客每次登录时均已阅读并勾选《用户须知》和《隐私协议》（与首次/非首次无关）
- [x] 后端服务正常可用，微信开放平台接口可达
- [x] 系统已配置微信小程序 AppID / AppSecret
- [x] 游客未处于已登录态（或登录态已过期）

---

## 4. 业务流程

### 4.1 主路径

1. 用户在小程序入口点击「微信一键登录」按钮前，页面展示《用户须知》和《隐私协议》勾选区
2. 用户勾选「我已阅读并同意《用户须知》和《隐私协议》」
3. 小程序调用 `wx.login()` 获取临时登录凭证 `code`
4. 小程序调用 `wx.getPhoneNumber` 获取加密手机号数据 `encryptedData` + `iv`
5. 小程序将 `code`、`encryptedData`、`iv` 以及 `terms_accepted=true`、`privacy_accepted=true`、`app_type=user` 提交到后端 `POST /api/v1/auth/wechat-login`
6. 后端校验 `terms_accepted` 与 `privacy_accepted` 均为 true
7. 后端调用微信 `code2session` 接口，用 `code` 换取 `openid`、`union_id`、`session_key`
8. 后端使用 `session_key` 解密手机号，得到用户手机号 `phone`
9. 后端按 `union_id` 查询 `user` 表：
   - 命中已有用户 → 复用账号
   - 未命中 → 新建用户记录，`identity_status = 注册用户`，`profile_completed = false`，写入 `phone` 与微信头像/昵称
10. 后端签发 JWT `access_token` + `refresh_token`，将 `session_key` 写入 Redis（TTL 7200s）
11. 返回登录结果（含 `access_token`、`refresh_token`、`expires_in`、`is_new_user`、`profile_completed`、`user_id`）
12. 前端将 `access_token` 与 `refresh_token` 存储到本地（如 `Taro.setStorageSync`），并记录 `expires_in`
13. 前端按 `profile_completed` 决定跳转：
    - `false` → 跳转「完善个人资料页」（US-005）
    - `true` → 跳转小程序首页
14. 后续访问受登录态保护的接口时，前端在 HTTP Header `Authorization: Bearer {access_token}` 中携带 token；后端校验 token 有效后方可访问
15. 前端在 app 启动或「我的」等依赖登录态的页面 `onShow` 时检查本地 token：若不存在或已过期，引导用户重新登录；`access_token` 过期但 `refresh_token` 有效时，前端调用刷新接口换发新的 `access_token`

### 4.2 异常分支

- **分支 1**：用户未勾选《用户须知》或《隐私协议》→ 提示「请阅读并同意《用户须知》和《隐私协议》」，不调用微信授权，不创建用户记录
- **分支 2**：用户拒绝微信登录授权或手机号授权 → 提示"需要微信授权和手机号授权才能登录"，停留在登录页
- **分支 3**：微信 `code2session` 接口调用失败 → 提示"微信服务暂时不可用，请稍后重试"
- **分支 4**：`code` 已被使用或已过期（5 分钟内有效） → 提示"登录凭证已失效，请重新点击登录"
- **分支 5**：网络超时 → 提示"网络异常，请重试"

---

## 5. 业务规则引用

> 引用 v11 PRD 章节：[`../../prd/prd.md`](../../prd/prd.md)

| # | 规则 | 章节 |
|---|------|------|
| 1 | 支持微信授权登录，首次登录时获取手机号与头像，首次登录后完善个人资料（头像/姓名/年龄/性别等） | [§5.2.1](../../prd/prd.md) |
| 2 | 用户身份状态机：游客 → 注册用户（通过微信授权登录） | [§9.2 身份体系](../../prd/prd.md) |
| 3 | 用户身份状态机：注册用户 → 学员（购买正价套餐后） | [§9.2 身份体系](../../prd/prd.md) |
| 4 | 页面清单：微信授权页，微信 OAuth，[MVP] | [§13.3 第 4 行](../../prd/prd.md) |
| 5 | 账号注销后软删除，重新注册生成新账号，不绑定原账号数据 | [§5.2.1 第 4 条](../../prd/prd.md) |
| 6 | 登录时必须校验用户已勾选《用户须知》和《隐私协议》 | [§11.2](../../prd/prd.md) |

---

## 6. 验收标准（业务级 Gherkin）

> 业务级 Gherkin：聚焦"用户能观察到的结果"，不绑死实现细节。
> 字段级断言、API 设计、状态机影响 → 见配套**技术设计文档**。
> TDD 任务清单、扩展测试用例 → 见配套**测试计划**。
>
> US 等级：L2（1 人天，含 1 个状态转换）

### 6.1 场景 1：首次微信授权登录成功，跳转完善个人资料页（正常路径）

```gherkin
Given 用户首次使用微信授权登录，user 表中不存在其 union_id
And   用户已同意授权小程序获取手机号
And   用户已勾选《用户须知》和《隐私协议》
When  用户点击"微信一键登录"按钮并同意授权
Then  系统创建新用户记录，identity_status = "注册用户"
And   profile_completed = false
And   user.phone 等于解密后的微信手机号
And   user.avatar_url 等于微信头像 URL
And   返回 access_token 和 refresh_token
And   前端跳转到"完善个人资料"页（US-005）
And   user.union_id 等于微信返回的 union_id
And   user.identity_status 由 "游客" 转换为 "注册用户"
```

### 6.2 场景 2：已注册用户微信授权登录成功，跳转首页（正常路径）

```gherkin
Given 用户已注册且 profile_completed = true，user 表中存在其 union_id
And   用户已勾选《用户须知》和《隐私协议》
When  用户点击"微信一键登录"按钮并同意授权
Then  系统复用已有用户记录，不新建账号
And   返回 access_token 和 refresh_token
And   is_new_user = false
And   前端跳转到小程序首页
And   user.identity_status 保持为 "注册用户"（或更高：学员）
```

### 6.3 场景 3：用户未勾选《用户须知》或《隐私协议》（异常路径）

```gherkin
Given 用户未勾选《用户须知》或《隐私协议》
When  用户点击"微信一键登录"按钮
Then  前端拦截登录请求并展示文案"请阅读并同意《用户须知》和《隐私协议》"
And   不调用 wx.login() 或 wx.getPhoneNumber
And   不发起后端登录请求
And   若用户绕过前端直接调用后端接口，后端返回错误码 TERMS_NOT_ACCEPTED
And   不创建任何用户记录
And   不签发 token
```

### 6.4 场景 4：用户拒绝授权（异常路径）

```gherkin
Given 用户已勾选《用户须知》和《隐私协议》
And   用户在微信授权弹窗中点击"拒绝"
When  系统尝试调用 wx.login() 或获取手机号信息
Then  登录流程终止
And   停留在登录页并展示文案"需要微信授权和手机号授权才能登录"
And   不创建任何用户记录
And   不签发 token
```

### 6.5 场景 5：微信接口调用失败（异常路径）

```gherkin
Given 后端调用微信 code2session 接口时返回错误码或超时
When  后端处理登录请求
Then  返回 HTTP 502 + 错误码 "WECHAT_API_ERROR"
And   前端展示文案"微信服务暂时不可用，请稍后重试"
And   不创建任何用户记录
And   不签发 token
```

### 6.6 场景 6：登录凭证已失效（异常路径）

```gherkin
Given 用户提交的 code 已被使用或已超过 5 分钟有效期
When  后端调用 code2session 接口
Then  微信返回 errcode = 40029（invalid code）
And   后端返回 HTTP 401 + 错误码 "WECHAT_CODE_INVALID"
And   前端展示文案"登录凭证已失效，请重新点击登录"
And   不创建任何用户记录
```

### 6.7 场景 7：登录成功后存储 token 并用其维持登录态（正常路径）

```gherkin
Given 用户已完成微信授权登录
And   后端返回 access_token、refresh_token 和 expires_in = 7200
When  前端收到登录响应
Then  前端将 access_token 和 refresh_token 写入本地存储
And   前端记录 access_token 过期时间
When  用户访问受登录态保护的接口（如「我的」页面）
Then  前端在 Authorization Header 中携带 Bearer {access_token}
And   后端校验 token 有效后返回用户数据
When  access_token 过期但 refresh_token 未过期
Then  前端调用刷新接口换取新的 access_token
And   后续请求使用新的 access_token
When  本地 token 不存在或 refresh_token 已过期
Then  前端引导用户重新进入登录页
```

---

## 7. 数据/API/状态机影响

> 本节为摘要级概述，完整技术设计见 [./tech-design.md](./tech-design.md)。

### 7.1 数据表影响

| # | 表名 | 操作 | 说明 |
|---|------|------|------|
| 1 | `user` | 新增 | 首次登录时插入新记录：`openid`、`union_id`、`phone`（解密后的微信手机号）、`avatar_url`（微信头像）、`name`（微信昵称，可选）、`identity_status='注册用户'`、`profile_completed=false`、`status=0`（TINYINT，0=正常/1=软删除/2=封禁，与 PRD §9.2.1 一致） |
| 2 | `user_session` | 新增 | 写入会话记录：`user_id`、`session_key`（加密）、`expires_at`、`refresh_token_hash` |

### 7.2 API 影响

| # | API | 方法 | 操作 | 说明 |
|---|-----|------|------|------|
| 1 | `/api/v1/auth/wechat-login` | POST | 新增 | 接收微信 `code`，调用 `code2session`，查询/创建用户，签发 JWT |

### 7.3 状态机影响

| # | 实体 | 转换 | 触发条件 | 说明 |
|---|------|------|---------|------|
| 1 | `user.identity_status` | 游客 → 注册用户 | 首次微信授权登录成功且新建用户记录 | **本 US 触发的核心状态转换**；后续 注册用户→学员 由 US-020 触发 |
| 2 | `user.profile_completed` | false → true | 用户在 US-005 中补充资料完成 | 本 US 仅置为 `false`，转换由 US-005 完成 |

> **配套文档**：[技术设计](./tech-design.md)　·　[测试计划](./test-plan.md)　·　Figma 见 §13-15

---

## 8. 边界场景

| # | 场景 | 处理方式 |
|---|------|---------|
| 1 | 微信 `union_id` 已绑定其他已注销账号（软删除账号） | 按业务规则 [§5.2.1 第 4 条](../../prd/prd.md) 处理：重新注册生成新账号，不绑定原账号数据；新用户 `identity_status='注册用户'`，与原账号数据完全隔离 |
| 2 | 网络超时（code2session 调用 > 3s） | 后端设置 3s 超时，超时返回 HTTP 504 + `WECHAT_API_TIMEOUT`；前端提示"网络异常，请重试" |
| 3 | 微信 `session_key` 过期（TTL 7200s 到期） | 后续需要 `session_key` 解密手机号等数据时若已过期，前端重新调用 `wx.login()` 获取新 code 并走刷新流程 |
| 4 | 用户快速重复点击登录按钮 | 前端按钮点击后立即 disable + loading；后端以 `code` 为幂等键，相同 code 5 分钟内重复提交只返回首次结果 |
| 5 | 同一 `openid` 但 `union_id` 缺失（用户未绑定微信开放平台） | 以 `openid` 作为兜底唯一键，记录告警日志；不阻断登录流程 |
| 6 | JWT `access_token` 过期 | 前端用 `refresh_token` 调用刷新接口换发新 `access_token`，对用户无感 |

---

## 9. 依赖关系

### 9.1 前置 US（本故事依赖）

- [x] 无（这是注册登录模块的第 1 个 US，是用户身份流程的起点）

### 9.2 后续 US（依赖本故事）

- [ ] US-005（用户完善个人资料 — 依赖本 US 创建的用户记录、`phone`、`avatar_url` 与 `profile_completed=false` 标志）
- [ ] US-006（用户手机号验证码登录 — 共享 user 表与 JWT 签发逻辑）
- [ ] US-007（用户账号注销 — 软删除本 US 创建的用户记录）
- [ ] US-009（用户/教练隐私协议与用户须知授权 — 登录页勾选协议，登录成功后记录同意版本）
- [ ] US-052（用户退出登录 — 依赖已登录态）
- [ ] US-017（游客购买体验课套餐 — 需先完成本 US 登录）
- [ ] US-020（学员购买正价套餐 — 需先完成本 US 登录，且本 US 是身份状态机的起点）
- [ ] US-042（管理员管理用户账号 — 管理员在后台查看、筛选、管理用户账号及详情）
- [ ] US-051（教练微信授权登录并进入教练端 — 扩展本 US 的 `POST /api/v1/auth/wechat-login` 接口，新增 `app_type=coach` 分支）

---

## 10. INVEST 自检

- [x] **I**ndependent（独立）- 无前置 US 依赖，是注册登录流程的起点
- [x] **N**egotiable（可协商）- 聚焦"用户能用微信登录"这一业务结果，不绑死技术实现
- [x] **V**aluable（有价值）- 是用户进入交易流程的入口，所有受保护功能的前置条件
- [x] **E**stimable（可估算）- 1 人天明确，微信 OAuth 标准流程
- [x] **S**mall（足够小）- 1 个 API + 1 个小程序页面，单 Sprint 可完成
- [x] **T**estable（可测试）- 业务级 Gherkin 7 个场景可客观验证

---

## 11. 完整性检查

### 11.1 字段完整性

- [x] 15 个章节全部填写
- [x] 无"待定"/"TBD"占位符（除 Figma 链接占位）
- [x] 业务规则引用明确

### 11.2 业务规则

- [x] 引用 v11 PRD 具体章节
- [x] 跨章节规则无冲突
- [x] 与状态机/数据模型一致

### 11.3 验收标准

- [x] 场景数量符合 US 等级 L2（7 个场景 = 3 正常 + 4 异常）
- [x] 业务级 Gherkin，不绑死实现
- [x] 用户可观察的结果可被验证

### 11.4 配套文档

- [x] Figma 链接/状态明确（待设计填写）
- [x] 技术设计文档链接/状态明确
- [x] 测试计划链接/状态明确

---

## 12. 备注

- **幂等键**：以微信 `code` 作为幂等键（5 分钟内有效，且只能使用一次），重复提交返回首次结果
- **事务边界**：查询用户 + 创建用户 + 签发 token + 写 session 必须在同一事务内，任一失败全部回滚
- **性能要求**：登录接口 P99 < 1500ms（含微信 code2session 调用时间）
- **安全要求**：
  - `session_key` 不可返回给前端
  - `access_token` JWT 有效期 2h，`refresh_token` 有效期 7d
  - 密码相关字段（即使本 US 不创建）未来需使用 bcrypt 存储
- **设计相关**：见 §13-15 Figma 相关章节
- **技术相关**：见 [./tech-design.md](./tech-design.md)
- **测试相关**：见 [./test-plan.md](./test-plan.md)

---

## 13. Figma 链接

> 本节提供 Figma file URL 与关键 frame 引用。Figma **设计系统规范**（token / 组件 / 状态徽标 / 4 态模板 / 文案）见 [docs/figma/README.md](../../figma/README.md)。

| # | 内容 | 链接 / node-id | 状态 |
|---|------|---------------|------|
| 1 | 微信授权登录页 Figma file URL | 🔲 待设计填写 | 🔲 |
| 2 | 微信授权登录页关键 frame node-id | 🔲 待设计填写 | 🔲 |
| 3 | 补充资料引导跳转 frame node-id | 🔲 待设计填写 | 🔲 |

### 13.1 状态截图清单

> **四态要求**：每个页面必须设计空/加载/错误/成功四种状态。具体样式由设计师根据 [Figma 设计规范](../../figma/README.md) §0.5 主题与 §4 四态模板决定，不在本节指定。

| 页面 | 空状态 | 加载状态 | 错误状态 | 成功状态 | 备注 |
|------|--------|---------|---------|---------|------|
| **微信授权登录页** | 🔲 | 🔲 | 🔲 | 🔲 | 错误态含"拒绝授权"、"微信服务不可用"、"凭证失效"3 种文案 |
| **登录后跳转引导** | 🔲 | 🔲 | 🔲 | 🔲 | 成功态分"新用户跳补充资料"和"老用户跳首页"两种 |

---

## 14. 页面级设计决策

> 本节是**本故事特殊**的设计选择，**全局规范**中没有定义的设计决策记录在这里。

### 14.1 登录按钮样式与位置

- **背景**：登录页是用户进入小程序的第一个交互点，需要明确的视觉引导
- **选项**：
  - A. 顶部居中大按钮
  - B. 底部固定按钮（fixed bottom）
  - C. 全屏授权引导卡片 + 底部按钮
- **结论**：选 B（底部固定按钮），符合微信小程序常见登录页习惯，按钮样式使用微信品牌绿（#07C160），高度 48px，全宽；按钮上方放置协议勾选区
- **影响范围**：微信授权登录页

### 14.2 协议勾选校验

- **背景**：登录时必须校验用户已勾选《用户须知》和《隐私协议》
- **选项**：A. 前端校验即可；B. 前端 + 后端双重校验
- **结论**：选 B。未勾选时前端直接提示并阻止调用 `wx.login`；后端 `POST /api/v1/auth/wechat-login` 同时校验 `terms_accepted=true` 且 `privacy_accepted=true`
- **影响范围**：微信授权登录页交互、后端登录接口

### 14.3 拒绝授权后的二次引导

- **背景**：用户首次拒绝授权后，再次调用 `wx.login` 不会弹窗，需要引导用户主动开启权限
- **选项**：
  - A. 直接展示错误文案，要求用户重启小程序
  - B. 展示「重新授权」按钮 + 引导文案「点击右上角...→设置→开启权限」
- **结论**：选 B，提供二次引导按钮，点击后调用 `wx.openSetting()` 引导用户开启权限
- **影响范围**：微信授权登录页错误态

### 14.4 新用户 vs 老用户的跳转差异

- **背景**：首次登录用户需要补充资料（US-005），老用户应直接进入首页
- **结论**：后端返回 `is_new_user` 和 `profile_completed` 两个标志，前端按以下逻辑跳转：
  - `is_new_user=true` 或 `profile_completed=false` → 跳「补充资料页」
  - `profile_completed=true` → 跳「小程序首页」
- **影响范围**：登录成功后的路由逻辑

### 14.5 页面级交互说明

| 交互 | 触发 | 反馈 | 备注 |
|------|------|------|------|
| 未勾选协议时点击「微信一键登录」 | tap | 前端提示「请阅读并同意《用户须知》和《隐私协议》」，不调用 wx.login() | 见 §6.3 |
| 已勾选协议后点击「微信一键登录」 | tap | 按钮 loading + 调用 wx.login() | 防重复点击：点击后立即 disable |
| wx.login 成功 | 微信回调 | 提交 code 到后端，按钮保持 loading | — |
| wx.login 失败/拒绝 | 微信回调 | 展示错误文案 + 恢复按钮可点击 | 文案见 §6.4 |
| 后端返回成功 | HTTP 200 | 按 is_new_user 跳转 | 见 §14.4 |
| 后端返回错误 | HTTP 4xx/5xx | 展示对应错误文案 + 恢复按钮 | 文案见 §6.5-6.6 |
| 点击「重新授权」按钮 | tap | 调用 wx.openSetting() | 见 §14.3 |

---

## 15. 设计评审记录

| 日期 | 评审人 | 反馈 | 处置 |
|------|--------|------|------|
| 2026-08-04 | PM | 微信授权登录需校验《用户须知》和《隐私协议》勾选 | 在 §3 前置条件、§4.1 主路径、§6.3 异常场景、§14.2 设计决策中补充协议勾选与双重校验 |

---

## 附录：变更日志

| 版本 | 日期 | 作者 | 变更 |
|------|------|------|------|
| v1.4 | 2026-08-05 | PM | 用户端登录流程显式提交 `app_type=user`，与 US-051 的 `app_type=coach` 保持一致，便于后端按字段路由到 `user` 表 |
| v1.3 | 2026-08-04 | PM | 新增登录时必须勾选并校验《用户须知》和《隐私协议》；新增异常场景与错误码 TERMS_NOT_ACCEPTED；GWT 场景从 5 个扩展为 6 个 |
| v1.2 | 2026-07-31 | PM | v7 评审 P1-4 修复：§1 标题与角色、§2 触发方、§3 前置条件统一为"游客"（与目录名 `US-004-游客-微信授权登录`、PRD 游客浏览主线、§9.2 身份状态机起点一致）；新增 §2 角色命名说明 |
| v1.1 | 2026-07-31 | PM | v3 评审 P0 修复：§1 状态从 [DRAFT] 改为 [REVIEW]；§7.1 user.status 字段从 VARCHAR 'active' 改为 TINYINT 0（与 tech-design.md v1.1 / PRD §9.2.1 一致） |
| v1.0 | 2026-07-30 | PM | 初版：15 章完整，含 5 个 GWT 场景（2 正常 + 3 异常）、6 个边界场景、状态机影响（游客→注册用户） |

---

<p align="right">
<sub>本文档遵循 <a href="../../../spec/user-story/SPECIFICATION.md">US 拆分规范</a>　·　复制自 <a href="../../../spec/user-story/TEMPLATE.md">TEMPLATE.md</a>　·　填写示例见 <a href="../../../spec/user-story/EXAMPLE.md">EXAMPLE.md</a></sub>
</p>
