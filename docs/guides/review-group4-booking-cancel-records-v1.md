# 业务评审报告 — 组 4：预约、取消与上课记录组

> **版本**：v1  
> **日期**：2026-07-31  
> **范围**：US-029 ~ US-036（预约、取消与上课记录组）  
> **依据 PRD**：`docs/prd/prd.md` v11  
> **评审人**：us-business-reviewer  
> **变更摘要**：首次对 US-029 ~ US-036 进行业务评审  
> **上版本**：无

---

## 预检摘要

| US | 目录 | user-story | tech-design | test-plan | INDEX 一致 | PRD 引用 |
|----|------|-----------|-------------|-----------|-----------|---------|
| US-029 | ✅ | ✅（298 行） | ✅（107 行） | ✅（50 行） | ✅ | ✅ |
| US-030 | ✅ | ✅（313 行） | ✅（107 行） | ✅（59 行） | ✅ | ✅ |
| US-031 | ✅ | ✅（331 行） | ✅（127 行） | ✅（59 行） | ✅ | ✅ |
| US-032 | ✅ | ✅（320 行） | ✅（103 行） | ✅（52 行） | ✅ | ✅ |
| US-033 | ✅ | ✅（362 行） | ✅（129 行） | ✅（83 行） | ✅ | ✅ |
| US-034 | ✅ | ✅（285 行） | ✅（104 行） | ✅（54 行） | ✅ | ✅ |
| US-035 | ✅ | ✅（302 行） | ✅（99 行） | ✅（42 行） | ✅ | ✅ |
| US-036 | ✅ | ✅（307 行） | ✅（124 行） | ✅（64 行） | ✅ | ✅ |

所有 US 目录与三件套均存在且非空；INDEX 中的编号、标题、状态（均为 `[REVIEW]`）与 `user-story.md §1` 一致；§5 引用的 PRD 章节均存在。

---

## 逐 US 评分

### US-029 学员预约正价课程

#### 综合评分：8.4 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.5 | `§4.1` 主路径清晰：进入预约页默认展示已绑定教练未来 7 天可约时段，步骤最小化；与 `US-022` 换教练入口衔接自然。 |
| 业务逻辑 | 8.5 | `§4.1` 按 FIFO 选择最早 active 套餐并预占 1 课时，`§5` 引用 `PRD §5.3.2/§6.3.1`，与“先到先得、教练无需确认”的硬约束一致。 |
| Figma 可设计性 | 8.0 | `§13.1` 为预约页、预约成功页列出空/加载/错误/成功四态；但 `§13` Figma 链接均为“待设计填写”。 |
| 泳道/角色完整性 | 8.5 | `§2` 明确学员（主）、系统（辅）；系统负责校验、锁单、通知，角色边界清晰。 |
| 异常场景 | 8.5 | `§8` 覆盖并发预约、教练请假导致时段关闭、网络抖动重复提交 3 个边界，且均给出具体错误提示。 |
| 商业合规 | 8.5 | 符合单课程先到先得、不强制教练确认的硬约束；`§6.3` 对课时不足/未绑定教练等错误码明确。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §6.5`（第 129 行） / `tech-design.md §2.3`
   **问题**：user-story 对重复提交的预期是 `Then 系统返回 HTTP 200 … 不重复创建 booking`，而 tech-design 中 `POST /api/bookings` 的异常响应却定义为 `409 DUPLICATE_BOOKING`，两者对同一幂等冲突的 HTTP 语义不一致。
   **建议**：统一为 200 + 已有 booking（符合 user-story），或调整 user-story 为 409；test-plan 中同步明确断言。

##### 🟢 轻微
1. **位置**：`user-story.md §13`
   **问题**：Figma file URL 与 node-id 均为“待设计填写”，当前无法验证页面四态。
   **建议**：设计稿产出后回填链接。

#### 是否推荐进入 [APPROVED]
- [x] 是，修复 HTTP 状态码一致性后可 APPROVED
- [ ] 否，需修改后重审

---

### US-030 学员取消正价课程

#### 综合评分：5.8 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 5.5 | `§4.1` 将 24h 外直接取消与 24h 内教练审批分路，但项目硬约束要求 `<24h` 走管理员审核的特殊原因申诉，两者存在层级冲突。 |
| 业务逻辑 | 5.5 | `§4.1` 的 `<24h` 教练审批流程与 `PRD §6.5.1` 一致，却与项目硬约束“`<24h` 需特殊原因申诉（由管理员审核）”不一致；`§1` 与顶部元数据估时冲突（1 人天 vs 1.5 人天）。 |
| Figma 可设计性 | 7.5 | `§13.1` 预约详情页、取消原因弹窗四态完整，可设计。 |
| 泳道/角色完整性 | 6.5 | `§2` 学员（主）、教练（辅）定义清楚，但教练审批权限范围（是否仅限本课程教练）未细化。 |
| 异常场景 | 6.0 | `§8` 覆盖 24h 边界、审批期间课程开始、并发；但未覆盖“特殊原因申诉直接提交入口”这一硬约束场景。 |
| 商业合规 | 4.0 | 项目硬约束明确 `<24h` 取消由管理员审核特殊原因申诉；本 US 改为教练审批前置，可能影响医疗/突发情况下的课时返还时效，存在合规风险。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 顶部第 5 行 / `§1` 第 20 行 / `INDEX.md`
   **问题**：文档顶部元数据写“估时：1 人天”，`§1` 表格写“1.5 人天”，INDEX 登记为 1.5d，同一 US 出现两种估时。
   **建议**：统一为 1.5 人天并修正顶部元数据。

2. **位置**：`user-story.md §4.1` 第 51–55 行 / 项目硬约束
   **问题**：`§4.1` 规定“开课前 <24 小时：需提交教练审批”，而项目硬约束要求“开课时间 `< 24h` 需特殊原因申诉（由管理员审核）”。虽然 `PRD §6.5.1` 也采用教练审批，但当前 US 未在项目硬约束与 PRD 之间做出一致性处理。
   **建议**：由 PM/业务方明确以 PRD v11 还是项目硬约束为准；若按硬约束执行，需将 `<24h` 取消直接转为管理员特殊原因审批流程，并移除教练审批相关 API/状态机。

##### 🟡 中等
1. **位置**：`tech-design.md` 标题 / `test-plan.md` 标题
   **问题**：两份配套文档标题仍为“学员取消/改约正价课程”，与 user-story 标题“学员取消正价课程”不一致（改约已拆至 US-031）。
   **建议**：同步重命名配套文档标题。

2. **位置**：`user-story.md §6.4` 超时场景 / `§8.2` 审批期间课程开始场景
   **问题**：两个场景叠加时，未明确超时任务与“课程已开始自动关闭申请”之间的竞态处理顺序。
   **建议**：在 `§4.2` 或 `§8` 中补充规则：课程一旦开始，待审批取消申请立即关闭，超时任务不再执行。

#### 是否推荐进入 [APPROVED]
- [ ] 是
- [x] 否，需按上述建议修改后再评审

---

### US-031 教练代约/改约正价课程

#### 综合评分：7.9 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.0 | `§4.1` 代约与 `§4.2` 改约职责分离；改约 `<24h` 需学员确认，降低纠纷风险。 |
| 业务逻辑 | 8.0 | `§4.1` 按 FIFO 预占课时，`§5` 引用 `PRD §5.4.2/§6.3.1`，与代约规则一致；`operator = coach` 留痕。 |
| Figma 可设计性 | 7.5 | `§13.1` 列出学员列表、代约时段选择页、改约页三页四态，可设计。 |
| 泳道/角色完整性 | 8.0 | `§2` 教练（主）、学员（辅）；`§3` 要求学员系统内授权并写入 `coach_authorization`，符合硬约束。 |
| 异常场景 | 8.0 | `§8` 覆盖并发预约、改约时课时刚好用完、代约后学员自行取消。 |
| 商业合规 | 8.0 | 授权留痕、操作来源审计、`coach_authorization` 可审计，符合硬约束。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §4.2` 第 63–64 行
   **问题**：改约在课前 `<24h` 需学员确认，但 `§6` 验收标准与 `§7.2` API 中均未覆盖该分支。
   **建议**：补充 GWT 场景“教练在课前 24h 内改约需学员确认”及对应错误码/状态。

2. **位置**：`user-story.md §4.1` 第 50 行 / `§3` 第 40 行
   **问题**：主路径仅写“系统校验学员身份、套餐状态及可用课时”，未明确是否校验 `coach_authorization` 授权记录；首次授权入口也未定义。
   **建议**：在 `§4.1` 增加授权检查步骤，或引用 US-037/US-005 中授权入口，确保“学员系统内授权”不是一句空话。

##### 🟢 轻微
1. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 是否推荐进入 [APPROVED]
- [x] 是，补充 `<24h` 改约确认场景与授权检查描述后可 APPROVED
- [ ] 否，需修改后重审

---

### US-032 学员签到课程

#### 综合评分：6.9 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 6.5 | `§4.1` 将“一键签到”与“课后总结”合并到一个标题为“签到课程”的 US 中，范围略宽；且允许 `status = 已预约` 时签到，与“到馆后”场景存在时间窗口差异。 |
| 业务逻辑 | 6.5 | 签到不扣课时与 `PRD §5.3.2 #7` 一致；但 `§6.2.1` 允许教练确认前预填总结，引入 `student_summary_draft` 合并时序，超出 PRD 明确范围。 |
| Figma 可设计性 | 7.5 | `§13.1` 预约详情页、上课记录页、课后总结弹窗四态完整。 |
| 泳道/角色完整性 | 7.0 | `§2` 学员（主）、教练/系统（辅）；教练记录由 US-033 创建，时序在 `§6.2.1` 中已说明。 |
| 异常场景 | 7.0 | `§8` 覆盖网络抖动重复签到、取消后签到、上课中才签到。 |
| 商业合规 | 7.0 | 签到窗口“到馆后至课程结束”与 PRD 一致；但预填总结可能让“课后总结”数据语义前置。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §3` 第 36 行 / `§4.1` 第 48 行
   **问题**：签到允许 `booking.status ∈ {已预约, 待上课, 上课中}`，而“到馆后”与状态机“上课前 10 分钟自动切为待上课”之间存在时间差，可能导致学员在教练尚未到场时签到并通知教练。
   **建议**：将签到可用状态限定为 `{待上课, 上课中}`，或在文档中明确允许提前到馆签到的业务规则与教练通知策略。

2. **位置**：`user-story.md §4.1` 第 52–54 行 / `§6.2.1`
   **问题**：允许学员在 `booking.status ∈ {待上课, 上课中}` 且 course_record 尚未创建时预填课后总结。`PRD §5.3.3 #4` 明确为“每节课后填写自我总结”，预填功能增加了跨 US 合并时序与临时表，超出 PRD 当前范围。
   **建议**：将课后总结限制在 `booking.status = 已完成` 后填写；若业务上需要预填，应在 PRD 中补充该设计决策。

##### 🟢 轻微
1. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 是否推荐进入 [APPROVED]
- [x] 是，限定签到可用状态并明确课后总结填写时机后可 APPROVED
- [ ] 否，需修改后重审

---

### US-033 教练确认上课记录（含旷课标记）

#### 综合评分：8.9 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 9.0 | `§4.1` 确认上课、`§4.2` 标记旷课职责单一；将管理员返还课时拆至 US-035，符合单一职责。 |
| 业务逻辑 | 9.0 | `§4.1` 课程结束后 24h 内教练确认并扣课时，`§4.2` 旷课自动扣课时，与 `PRD §5.3.2 line 457/§5.4.3 #2` 一致；状态机覆盖完整。 |
| Figma 可设计性 | 8.5 | `§13.1` 课程确认页、上课记录填写弹窗、旷课标记弹窗均列出四态。 |
| 泳道/角色完整性 | 8.5 | `§2` 教练（主）、系统（辅）；`§4.1` 步骤 7 补充未成年人监护人短信通知，与 US-037 协同。 |
| 异常场景 | 9.0 | `§8` 覆盖最后一节触发 exhausted、误标旷课走 US-035、超时未确认自动扣课时、课程未结束拒绝确认。 |
| 商业合规 | 9.0 | 符合“已预约 → 待上课 → 已完成/旷课”状态机；旷课扣课时、未成年保护短信均落地。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §2` 第 31 行 / `§4.1` 第 58 行 / `§14.1` 第 311 行
   **问题**：`§2` 与 `§4.1` 规定教练确认需在课程结束后 24h 内完成，超时由系统兜底；`§14.1` 却写“「标记学员未到课」仍可见直到 24h 窗口结束”，暗示旷课标记也受 24h 限制。但 `PRD §5.3.2 #10` 仅要求课程结束后标记旷课，未提 24h；若系统已自动确认，教练仍可见旷课按钮会造成状态冲突。
   **建议**：明确旷课标记必须在系统超时自动确认前完成，或在自动确认后禁用旷课标记并提示“已由系统自动确认”。

##### 🟢 轻微
1. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 是否推荐进入 [APPROVED]
- [x] 是，明确旷课标记与 24h 自动确认时序后可 APPROVED
- [ ] 否，需修改后重审

---

### US-034 管理员查看上课记录

#### 综合评分：8.3 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.5 | `§4.1` 列表/筛选/详情/导出路径完整，符合管理后台对账与客诉场景。 |
| 业务逻辑 | 8.5 | `§4.2` 覆盖无权限、空列表、时间范围过大；`§5` 引用 `PRD §5.4.3/§5.5/§6.3.1`，只读逻辑清晰。 |
| Figma 可设计性 | 8.0 | `§13.1` 列表页、详情页四态完整。 |
| 泳道/角色完整性 | 8.0 | `§2` 管理员角色；`§12` 权限键 `permission:admin:course_record:read` 明确。 |
| 异常场景 | 8.5 | `§8` 覆盖大数据量分页、学员已注销、并发导出与数据更新。 |
| 商业合规 | 8.0 | `§4.1` 导出用于对账，`tech-design.md §6` 提到导出审计与字段脱敏，符合财务合规要求。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §4.1` 第 49 行 / `tech-design.md §2.3`
   **问题**：业务主路径写明“管理员可导出当前筛选结果（CSV/Excel）用于对账”，但 tech-design 将导出 API 标记为“可选”。
   **建议**：明确导出是否为 MVP 必须；若是，移除“可选”标记并补充导出审计、脱敏与最大条数限制。

##### 🟢 轻微
1. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 是否推荐进入 [APPROVED]
- [x] 是，明确导出是否为 MVP 必须后可 APPROVED
- [ ] 否，需修改后重审

---

### US-035 管理员返还课时

#### 综合评分：8.6 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.5 | `§4.1` 返还课时需填写原因、校验累计返还上限，流程闭环；从 US-033 拆分后职责清晰。 |
| 业务逻辑 | 9.0 | `§4.1` 累计返还 `>=` 已扣课时返回 `RETURN_QUOTA_EXCEEDED`，`expired` 套餐复活并延长 `expire_at`，`refunded` 不可返还，与 `PRD §5.3.2 #10/§4.6` 一致。 |
| Figma 可设计性 | 8.0 | `§13.1` 返还课时弹窗四态完整。 |
| 泳道/角色完整性 | 8.5 | `§2` 管理员（主）、系统（辅）；`MANAGE_BOOKING` 权限与审计日志留痕。 |
| 异常场景 | 8.5 | `§8` 覆盖 exhausted 复活、重复返还、并发确认、expired 复活并延长有效期。 |
| 商业合规 | 9.0 | 明确“同一 booking 累计返还课时不能超过已扣课时数”，符合项目硬约束。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §6` / `§4.2` 第 69 行 / `§8.2`
   **问题**：`RETURN_QUOTA_EXCEEDED` 是防止超扣课时的关键边界，但 `§6` 验收标准中未包含重复返还达到上限的 GWT 场景。
   **建议**：补充场景“管理员对同一 booking 再次返还达到上限”到 `§6`，明确 HTTP 400 + `RETURN_QUOTA_EXCEEDED`。

2. **位置**：`user-story.md §4.1` 第 59 行
   **问题**：expired 套餐复活时“按套餐原有效期时长从返还操作时间重新计算 `expire_at`”，而 `PRD §4.6` 管理员手动延期仅写“更新 `expire_at`”，未明确从操作时间起算。
   **建议**：在 PRD §4.6 或本 US 中统一“从返还/延期操作时间起按原有效期时长顺延”的口径，避免运营与财务理解冲突。

##### 🟢 轻微
1. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 是否推荐进入 [APPROVED]
- [x] 是，补充重复返还 GWT 场景后可 APPROVED
- [ ] 否，需修改后重审

---

### US-036 教练提交请假申请

#### 综合评分：7.4 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 7.5 | `§4.1` 提交请假路径清晰；但 `§8.3` 引入“>30 天标记管理员重点关注”，PRD 中无对应规则。 |
| 业务逻辑 | 7.5 | `§4.2` 时间冲突、开始时间已过、时间范围非法、教练状态非法等校验与 `PRD §5.4.5/§6.6` 一致。 |
| Figma 可设计性 | 7.0 | `§13.1` 状态截图清单均为 🔲 待设计填写，当前无法验证。 |
| 泳道/角色完整性 | 7.5 | `§2` 教练（主）、管理员（审批方，辅）；审批逻辑由 US-044 承接，职责边界清晰。 |
| 异常场景 | 7.5 | `§8` 覆盖快速重复提交、结束时间早于开始时间、超长时间请假、教练状态非已通过。 |
| 商业合规 | 7.5 | 请假需管理员审批、与实时状态同步，符合 `PRD §5.4.5/§5.4.1`。 |

#### 主要问题

##### 🟡 中等
1. **位置**：`user-story.md §4.2` / `tech-design.md §2.1`
   **问题**：user-story 写“教练状态不允许：`coach.status ≠ 1` → 返回错误码 `COACH_STATUS_INVALID`”，未给出 HTTP 状态码；tech-design 将其映射为 `403`。
   **建议**：在 `§4.2` 中明确“返回 HTTP 403 + 错误码 `COACH_STATUS_INVALID`”。

2. **位置**：`user-story.md §8.3` 第 161–165 行
   **问题**：“请假跨度 > 30 天，允许提交，但标记为「管理员重点关注」”在 PRD 中无对应规则，也缺少后续处理动作。
   **建议**：要么在 PRD 中补充长请假的运营规则与系统行为，要么将其降级为 P2/移除，避免开发实现无依据功能。

##### 🟢 轻微
1. **位置**：`user-story.md §13.1`
   **问题**：状态截图清单均为 🔲 待设计填写。
   **建议**：设计稿产出后回填。

#### 是否推荐进入 [APPROVED]
- [x] 是，明确 HTTP 状态码并处理“超长时间请假”规则来源后可 APPROVED
- [ ] 否，需修改后重审

---

## 组内依赖与 MVP 逻辑通顺性总结

```
US-029（学员预约）
    ├── US-030（学员取消） ──→ US-031（教练代约/改约）
    └── US-032（学员签到） ──→ US-033（教练确认/旷课）
                                      ├── US-035（管理员返还课时）
                                      └── US-034（管理员查看上课记录）
US-036（教练请假） ──→ US-044（管理员排班与请假审批）
```

- **依赖链**：无循环依赖；US-033 拆分出 US-035 后职责清晰，US-034 依赖 US-033/US-035 数据，顺序合理。
- **状态机覆盖**：预约状态 `已预约 → 待上课 → 已完成/已取消/旷课` 在本组 US-029/030/032/033 中全部触达；套餐 `reserved → consumed → available/exhausted/expired/active` 在本组及 US-035 中覆盖完整。
- **主要阻塞**：US-030 的 `<24h` 取消流程与项目硬约束存在冲突，需 PM/业务方拍板；否则本组可进入开发阶段。

---

## P0 / P1 / P2 修改清单

| 优先级 | US | 问题 | 建议修改位置 | 责任人 |
|--------|-----|------|--------------|--------|
| P0 | US-030 | `<24h` 取消流程与项目硬约束冲突 | `user-story.md §4.1/§4.2/§6/§7.2` | PM/业务方 |
| P0 | US-030 | 估时 1 人天 / 1.5 人天不一致 | 文档顶部元数据、`§1` | PM |
| P1 | US-030 | tech/test 标题仍含“改约” | `tech-design.md`、`test-plan.md` 标题 | Dev |
| P1 | US-029 | 重复提交返回码 user-story 与 tech-design 不一致 | `§6.5` / `tech-design.md §2.3` | Dev |
| P1 | US-031 | 缺少 `<24h` 改约需学员确认的 GWT 场景 | `§6` | PM |
| P1 | US-031 | 授权检查步骤未在主路径明确 | `§4.1` | PM |
| P1 | US-032 | 签到可用状态包含 `已预约`，与“到馆后”场景存在时间差 | `§3`、`§4.1` | PM |
| P1 | US-032 | 允许预填课后总结，超出 PRD 范围 | `§4.1`、`§6.2.1` | PM |
| P1 | US-033 | 旷课标记与 24h 自动确认时序未明确 | `§2`、`§4.2`、`§14.1` | PM |
| P1 | US-034 | 导出功能在 tech-design 中标记为“可选” | `tech-design.md §2.3` | PM |
| P1 | US-035 | 缺少 `RETURN_QUOTA_EXCEEDED` 的 GWT 场景 | `§6` | PM |
| P1 | US-035 | expired 复活有效期计算口径需与 PRD 统一 | `§4.1`、`PRD §4.6` | PM |
| P1 | US-036 | `COACH_STATUS_INVALID` HTTP 状态码未明确 | `§4.2` | PM |
| P2 | 全组 | Figma 链接待填充 | `§13` | 设计师 |

---

## 最终推荐结论

| US | 标题 | 综合评分 | 是否推荐 [APPROVED] |
|------|------|----------|---------------------|
| US-029 | 学员预约正价课程 | 8.4 | ✅ 是（修复状态码一致性后） |
| US-030 | 学员取消正价课程 | 5.8 | ❌ 否，需重写/澄清后重审 |
| US-031 | 教练代约/改约正价课程 | 7.9 | ✅ 是（补充改约确认场景与授权检查） |
| US-032 | 学员签到课程 | 6.9 | ✅ 是（限定签到状态与总结时机） |
| US-033 | 教练确认上课记录（含旷课标记） | 8.9 | ✅ 是（明确旷课标记时序） |
| US-034 | 管理员查看上课记录 | 8.3 | ✅ 是（明确导出范围） |
| US-035 | 管理员返还课时 | 8.6 | ✅ 是（补充重复返还场景） |
| US-036 | 教练提交请假申请 | 7.4 | ✅ 是（明确状态码与长请假规则） |

---

## 评审质量自检

- [x] §0 预检报告已输出，所有 US 文件存在性已验证
- [x] 每个 US 的 user-story.md 已完整读取（非猜测）
- [x] 每个 US 的 PRD 引用已打开 prd.md 对照验证
- [x] INDEX 一致性已校验（标题/状态/编号）
- [x] 每个评分说明都引用了 US 文档具体位置（§X.Y）
- [x] 没有超过 3 个 US 的问题描述相同（反模板化 §4.2 规则 1）
- [x] 没有某维度超过 5 个 US 评分完全相同且无差异化理由（反模板化 §4.2 规则 2）
- [x] 综合评分标准差 > 0.5（实际 0.92，反模板化 §4.2 规则 5）
- [x] 三件套不完整的 US 已标注结构分 ≤ 5.0（无）
- [x] 目录缺失的 US 未进入评分阶段（无）
- [x] §7 评审结果已归档到项目文档 `docs/guides/review-group4-booking-cancel-records-v1.md`
- [x] 归档文件包含版本头（日期/范围/依据 PRD/变更摘要）
- [x] INDEX.md 已同步更新（推荐 APPROVED 的 US 状态改为 `[APPROVED]`，US-030 保持 `[REVIEW]`）
