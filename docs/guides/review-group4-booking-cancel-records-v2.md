# 业务评审报告 — 组 4：预约、取消、签到、上课记录

> **版本**：v2  
> **日期**：2026-07-31  
> **范围**：US-029 ~ US-036（预约、取消、签到、上课记录组）  
> **依据 PRD**：`docs/prd/prd.md` v11  
> **评审人**：us-business-reviewer  
> **变更摘要**：复核 INDEX 将本组 US 状态更新为 `[APPROVED]` 的合理性；重点审查 US-030 24h 取消、US-031 <24h 改约确认、US-032 签到窗口、US-033/US-035 旷课与返还边界  
> **上版本**：v1（见 `docs/guides/review-group4-booking-cancel-records-v1.md`）

---

## 预检摘要

| US | 目录 | user-story | tech-design | test-plan | INDEX 一致 | PRD 引用 |
|----|------|-----------|-------------|-----------|-----------|---------|
| US-029 | ✅ | ✅（298 行） | ✅（107 行） | ✅（50 行） | ❌ 状态不一致 | ✅ |
| US-030 | ✅ | ✅（313 行） | ✅（107 行） | ✅（59 行） | ❌ 状态不一致 | ✅ |
| US-031 | ✅ | ✅（359 行） | ✅（127 行） | ✅（59 行） | ❌ 状态不一致 | ✅ |
| US-032 | ✅ | ✅（330 行） | ✅（103 行） | ✅（52 行） | ❌ 状态不一致 | ✅ |
| US-033 | ✅ | ✅（363 行） | ✅（129 行） | ✅（83 行） | ❌ 状态不一致 | ✅ |
| US-034 | ✅ | ✅（286 行） | ✅（104 行） | ✅（54 行） | ❌ 状态不一致 | ✅ |
| US-035 | ✅ | ✅（303 行） | ✅（99 行） | ✅（42 行） | ❌ 状态不一致 | ✅ |
| US-036 | ✅ | ✅（308 行） | ✅（124 行） | ✅（64 行） | ❌ 状态不一致 | ✅ |

**预检说明**：
- 所有 US 目录与三件套均存在且非空（user-story > 50 行，tech-design > 20 行，test-plan > 20 行）。
- `INDEX.md` 将本组 8 个 US 均登记为 `[APPROVED]`，但每个 `user-story.md §1` 的元数据状态仍显示为 `[REVIEW]`，存在系统性状态不同步。
- §5 引用的 PRD 章节均存在，但部分引用精确性、规则匹配度需要进一步校验（详见逐 US 评审）。

---

## 逐 US 评审

### US-029 学员预约正价课程

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：7.9 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.0 | `§4.1` 进入预约页默认展示已绑定教练未来 7 天可约时段，步骤最小化；与 `US-022` 换教练入口衔接自然。 |
| 业务逻辑 | 7.5 | `§4.1` 按 FIFO 选择最早 active 套餐并预占 1 课时，与 `PRD §5.3.2 #6 / §6.3.1` 一致；但 `§6.5` 与 `tech-design.md §2.3` 对重复提交的 HTTP 语义不一致（200 vs 409）。 |
| Figma 可设计性 | 8.0 | `§13.1` 预约页、预约成功页四态完整；但 `§13` 链接仍为“待设计填写”。 |
| 泳道/角色完整性 | 8.0 | `§2` 学员（主）、系统（辅）；系统负责校验、锁单、通知，边界清晰。 |
| 异常场景 | 8.0 | `§8` 覆盖并发预约、教练请假导致时段关闭、网络抖动重复提交 3 个边界。 |
| 商业合规 | 8.5 | 符合单课程先到先得、不强制教练确认的硬约束；错误码明确。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 45 行
   **问题**：`user-story.md §1` 元数据状态为 `[REVIEW]`，`INDEX.md` 却登记为 `[APPROVED]`，状态不同步。
   **建议**：若本次评审结论为降级，同步将 `INDEX.md` 改回 `[REVIEW]`；若确认可 approve，先将 `user-story.md §1` 状态改为 `[APPROVED]`。

##### 🟡 中等
2. **位置**：`user-story.md §6.5` 第 129 行 / `tech-design.md §2.3` 第 64 行
   **问题**：user-story 对重复提交的预期是 `Then 系统返回 HTTP 200 … 不重复创建 booking`，而 tech-design 中 `POST /api/bookings` 的异常响应定义为 `409 DUPLICATE_BOOKING`。
   **建议**：统一为 200 + 已有 booking，或调整 user-story 为 409；test-plan 同步明确断言。

3. **位置**：`user-story.md §5` 第 74 行
   **问题**：将“多套餐课时消耗顺序：FIFO”引用为 `PRD §5.3.3`，但 `PRD §5.3.3 #2` 原文为“多套餐课时消耗顺序：FIFO（见 §4.1）”，真正定义在 §4.1。
   **建议**：将引用改为 `PRD §4.1`，保持规则溯源准确。

##### 🟢 轻微
4. **位置**：`user-story.md §13`
   **问题**：Figma file URL 与 node-id 均为“待设计填写”。
   **建议**：设计稿产出后回填链接。

#### 推荐状态
[REVIEW]（因状态不同步 + HTTP 语义不一致，暂不建议维持 APPROVED）

#### Figma 准备度
⚠️ 需补充 §13 链接与 §15 设计评审记录后方可进入高保真设计；当前页面结构已可开始低保真原型。

---

### US-030 学员取消正价课程

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：7.3 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.0 | `§4.1` 将 ≥24h 直接取消与 <24h 教练审批分路，流程清晰。 |
| 业务逻辑 | 7.0 | 24h 外/内规则与 `PRD §5.3.2 #9 / §6.5.1` 一致；但缺少 `<2h` 特殊原因申诉场景，且估时、配套文档标题存在不一致。 |
| Figma 可设计性 | 8.0 | `§13.1` 预约详情页、取消原因弹窗四态完整。 |
| 泳道/角色完整性 | 8.0 | `§2` 学员（主）、教练（辅）；教练只能审批自己课程的取消申请，权限边界在 §4/API 中已体现。 |
| 异常场景 | 7.0 | `§8` 覆盖 24h 边界、审批期间课程开始、并发；但未覆盖 `PRD §6.5.1` 中 `<2h` 特殊原因申诉入口。 |
| 商业合规 | 7.5 | 特殊原因取消的客诉兜底在 `US-049` 申诉中提及，但未在 GWT 中落地，存在合规验证缺口。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 46 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

2. **位置**：`user-story.md` 顶部第 5 行 / `§1` 第 20 行 / `INDEX.md` 第 46 行
   **问题**：顶部元数据写“估时：1 人天”，`§1` 表格写“1.5 人天”，INDEX 登记为 1.5d，同一 US 出现两种估时。v1 评审已标记为 P0，当前仍未修复。
   **建议**：统一为 1.5 人天并修正顶部元数据。

##### 🟡 中等
3. **位置**：`tech-design.md` 标题 / `test-plan.md` 标题
   **问题**：两份配套文档标题仍为“学员取消/改约正价课程”，与 user-story 标题“学员取消正价课程”不一致（改约已拆至 US-031）。v1 评审已标记，当前仍未修复。
   **建议**：同步重命名配套文档标题。

4. **位置**：`user-story.md §6` 验收标准 / `PRD §5.3.2 #9 / §6.5.1`
   **问题**：PRD 明确“开课前 `<2h`（特殊原因）用户可提交特殊原因申诉”，但 `§6` 的 5 个 GWT 场景未覆盖该分支；`§4.2` 分支 5 仅提到被动取消后向 US-049 申诉，未覆盖主动特殊原因取消。
   **建议**：补充 GWT 场景“开课前 1 小时学员提交特殊原因取消并上传证明材料”，明确进入 US-049 争议退款流程的触发条件与状态码。

##### 🟢 轻微
5. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（P0 估时不一致 + 缺少特殊原因取消场景，不满足 APPROVED 闭合标准）

#### Figma 准备度
⚠️ 需先补充特殊原因取消的页面/弹窗分支，并回填 §13 链接。

---

### US-031 教练代约/改约正价课程

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：7.5 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 7.5 | `§4.1/§4.2` 代约与改约职责分离；改约 `<24h` 需学员确认，降低纠纷风险。 |
| 业务逻辑 | 7.5 | FIFO 预占、`operator = coach` 留痕与 PRD 一致；但 `<24h` 需学员确认属于 US 自增规则，PRD §5.4.2 未明确要求。 |
| Figma 可设计性 | 7.5 | `§13.1` 学员列表、代约时段选择页、改约页三页四态完整，设计工作量较大。 |
| 泳道/角色完整性 | 8.0 | `§2` 教练（主）、学员（辅）；`§3` 要求学员系统内授权并写入 `coach_authorization`，符合审计要求。 |
| 异常场景 | 8.5 | `§6` 6 个 GWT 覆盖代约成功、改约成功、slot 被占、未绑定、已取消改约、<24h 未确认；`§8` 边界完整。 |
| 商业合规 | 8.0 | 授权留痕、操作来源审计，符合可审计要求。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 47 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

##### 🟡 中等
2. **位置**：`user-story.md §3` 第 40 行 / `§4.1` 第 50–57 行 / `PRD §5.4.2 #4`
   **问题**：`§3` 要求“学员在系统内明确授权教练代为预约/改约，并留痕（`coach_authorization` 记录）”；`§4.1` 规定“开课时间 <24h 需向学员发送代约确认请求”。`PRD §5.4.2 #4` 仅写“教练可帮学员代约，系统按正常预约规则从学员课时包中预占 1 节课时”，未提及系统内授权或 <24h 学员确认。
   **建议**：由 PM/业务方确认该设计决策是否升级为 PRD 规则；若保留，需在 PRD 中补充对应条款，避免开发实现无依据。

3. **位置**：`user-story.md §4.1` 第 50 行 / `§3` 第 40 行
   **问题**：首次授权入口（学员在系统内明确授权教练）未在相关 US（US-005/US-037）中定义，可能导致 `coach_authorization` 记录来源悬空。
   **建议**：明确授权入口归属 US，或在 §9 依赖关系中增加对应前置 US。

##### 🟢 轻微
4. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（P0 状态不同步 + <24h 确认/授权规则需 PM 确认）

#### Figma 准备度
⚠️ 需明确 <24h 确认流程的页面分支（学员确认页/通知卡片）后再进入高保真设计。

---

### US-032 学员签到课程

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：6.7 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 6.5 | 一键签到与课后总结合并到一个 US 中，范围略宽；签到窗口定义与 PRD 状态机存在差异。 |
| 业务逻辑 | 6.5 | 签到不扣课时与 `PRD §5.3.2 #7` 一致；但 `§4.1/§12` 将“待上课”定义为课前 15 分钟，与 `PRD §6.1.2` 的“距开课 ≤ 10 分钟”不一致，且变更日志声称已删除 15 分钟限制但正文仍保留。 |
| Figma 可设计性 | 8.0 | `§13.1` 预约详情页、上课记录页、课后总结弹窗四态完整。 |
| 泳道/角色完整性 | 7.5 | `§2` 学员（主）、教练/系统（辅）；教练记录由 US-033 创建，时序在 `§6.2.1` 中已说明。 |
| 异常场景 | 8.0 | `§8` 覆盖网络抖动重复签到、取消后签到、上课中才签到、课前 30 分钟提前签到。 |
| 商业合规 | 7.5 | 签到窗口“到馆后至课程结束”与 PRD 一致，但具体时间口径需统一。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 48 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

##### 🟡 中等
2. **位置**：`user-story.md §4.1` 第 47 行 / `§12` 第 258 行 / `PRD §6.1.2`
   **问题**：文档写“待上课定义为开课时间前 15 分钟至课程结束时间”，但 `PRD §6.1.2` 定义“待上课：距开课 ≤ 10 分钟”。两者时间口径不一致，会导致签到窗口、状态切换、教练通知时序冲突。
   **建议**：统一为 PRD 的 10 分钟口径；同步修正 `§4.1/§12` 及 `tech-design.md §2.1` 的签到窗口描述。

3. **位置**：`user-story.md` 变更日志 v1.2 / `§4.1` 第 47 行 / `§12` 第 258 行
   **问题**：变更日志 v1.2 称“删除「15 分钟」签到窗口限制，改为学员到馆后签到”，但正文仍保留 15 分钟描述，文档内部自相矛盾。
   **建议**：按最新业务决策统一口径，并删除矛盾描述。

4. **位置**：`test-plan.md` 标题
   **问题**：标题为“Test Plan: US-032 学员签到/签退课程”，与 user-story 标题“学员签到课程”不一致。
   **建议**：同步重命名。

##### 🟢 轻微
5. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（P0 状态不同步 + 签到窗口口径与 PRD 不一致，影响状态机正确性）

#### Figma 准备度
❌ 不可进入高保真设计——签到按钮出现时机（课前 10 分钟 vs 15 分钟）直接影响页面状态机，需先统一业务口径。

---

### US-033 教练确认上课记录（含旷课标记）

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：8.6 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 9.0 | `§4.1` 确认上课、`§4.2` 标记旷课职责单一；将管理员返还课时拆至 US-035，符合单一职责。 |
| 业务逻辑 | 9.0 | `§4.1` 课程结束后 24h 内教练确认并扣课时，`§4.2` 旷课自动扣课时，与 `PRD §5.3.2 #10 / §5.4.3 #2` 一致；状态机覆盖完整。 |
| Figma 可设计性 | 8.5 | `§13.1` 课程确认页、上课记录填写弹窗、旷课标记弹窗均列出四态。 |
| 泳道/角色完整性 | 8.5 | `§2` 教练（主）、系统（辅）；`§4.1` 步骤 7 补充未成年人监护人短信通知，与 US-037 协同。 |
| 异常场景 | 8.5 | `§8` 覆盖最后一节触发 exhausted、误标旷课走 US-035、超时未确认自动扣课时、课程未结束拒绝确认。 |
| 商业合规 | 9.0 | 旷课扣课时、未成年保护短信均落地；audit_log 留痕完整。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 49 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

##### 🟡 中等
2. **位置**：`user-story.md §2` 第 31 行 / `§4.1` 第 58 行 / `§14.1` 第 312 行
   **问题**：`§2` 与 `§4.1` 规定教练确认需在课程结束后 24h 内完成，超时由系统兜底；`§14.1` 却写“「标记学员未到课」仍可见直到 24h 窗口结束”，暗示旷课标记也受 24h 限制。`PRD §5.3.2 #10` 仅要求课程结束后标记旷课，未提 24h；若系统已自动确认，教练仍可见旷课按钮会造成状态冲突。
   **建议**：明确旷课标记必须在系统超时自动确认前完成，或在自动确认后禁用旷课标记并提示“已由系统自动确认”。

##### 🟢 轻微
3. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（仅因 P0 状态不同步；业务逻辑本身基本闭合，修复状态字段并明确旷课 24h 时序后可 APPROVED）

#### Figma 准备度
⚠️ 需明确旷课按钮与 24h 自动确认的时序后再进入高保真设计；当前页面结构已可开始低保真原型。

---

### US-034 管理员查看上课记录

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：7.1 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.0 | `§4.1` 列表/筛选/详情/导出路径完整，符合管理后台对账与客诉场景。 |
| 业务逻辑 | 7.0 | `§4.2` 覆盖无权限、空列表、时间范围过大；`§5` 引用 `PRD §5.5` 过于宽泛；`INDEX` 与 `user-story §9.1` 依赖不一致。 |
| Figma 可设计性 | 8.0 | `§13.1` 列表页、详情页四态完整。 |
| 泳道/角色完整性 | 7.0 | `§2` 管理员角色；`§12` 权限键明确，但管理员权限配置来源未在依赖中体现。 |
| 异常场景 | 7.5 | `§8` 覆盖大数据量分页、学员已注销、并发导出与数据更新。 |
| 商业合规 | 8.0 | `§4.1` 导出用于对账，`tech-design.md §6` 提到导出审计与字段脱敏，符合财务合规要求。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 50 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

##### 🟡 中等
2. **位置**：`INDEX.md` 第 50 行 / `user-story.md §9.1` 第 177–180 行
   **问题**：INDEX 中 US-034 前置 US 为“US-033, US-035”，但 `user-story.md §9.1` 仅列出 US-004 / US-005 / US-029 / US-033，未列出 US-035。US-034 是查看上课记录，查看返还记录并非其必要前置。
   **建议**：统一以 user-story 为准，将 INDEX 中 US-034 前置 US 修正为 US-033；若需展示返还记录，在 §4.1/§7.1 中说明为可选关联数据。

3. **位置**：`user-story.md §5` 第 63–66 行
   **问题**：引用 `PRD §5.5`（管理员后台总章节）过于宽泛，未精确到具体条款；权限配置规则未明确。
   **建议**：将 §5.5 替换为 `§5.5.2`（教练排班与预约释放管理）或 `§5.5.3`（套餐与订单），并明确管理员权限由 US-042 还是其他 US 提供。

4. **位置**：`user-story.md §4.1` 第 49 行 / `tech-design.md §2.3`
   **问题**：业务主路径写明“管理员可导出当前筛选结果（CSV/Excel）用于对账”，但 tech-design 将导出 API 标记为“可选”。
   **建议**：明确导出是否为 MVP 必须；若是，移除“可选”标记并补充导出审计、脱敏与最大条数限制。

##### 🟢 轻微
5. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（P0 状态不同步 + 依赖关系不一致 + 权限来源需明确）

#### Figma 准备度
⚠️ 需明确导出是否为 MVP 必须、详情页是否展示返还记录后再进入高保真设计。

---

### US-035 管理员返还课时

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：7.0 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.0 | `§4.1` 返还课时需填写原因、校验累计返还上限，流程闭环；从 US-033 拆分后职责清晰。 |
| 业务逻辑 | 6.5 | `§4.1` 累计返还 `>=` 已扣课时返回 `RETURN_QUOTA_EXCEEDED`，`expired` 套餐复活并延长 `expire_at`，`refunded` 不可返还；但 expired 复活逻辑与 `PRD §4.6` 手动延期规则的引用关系需澄清。 |
| Figma 可设计性 | 8.0 | `§13.1` 返还课时弹窗四态完整。 |
| 泳道/角色完整性 | 8.5 | `§2` 管理员（主）、系统（辅）；`MANAGE_BOOKING` 权限与审计日志留痕。 |
| 异常场景 | 8.5 | `§8` 覆盖 exhausted 复活、重复返还、并发确认、expired 复活并延长有效期。 |
| 商业合规 | 8.5 | 明确“同一 booking 累计返还课时不能超过已扣课时数”，审计留痕完整。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 51 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

##### 🟡 中等
2. **位置**：`user-story.md §4.1` 第 59 行 / `§6.4` 第 124–135 行 / `PRD §4.6`
   **问题**：`§6.4` 场景设定 `package.status = expired`，`total_hours = 3`，`consumed_count = 3`，`available_count = 0`，返还 1 课时后 `consumed = 2`，`available = 1`，`status` 恢复为 `active`。`PRD §4.6` 管理员手动延期仅允许对 `status = expired 且 available + reserved > 0` 的套餐执行延期；本场景在返还前 `available + reserved = 0`，与 PRD 原文存在口径差异。
   **建议**：在 `§5` 中补充引用 `PRD §4.6` 并明确说明：返还课时是“先增加课时再恢复 active”，与“不增加课时的手动延期”属于不同操作；或在 §6.4 场景前提中说明该场景已通过返还创造了 `available > 0`。

3. **位置**：`user-story.md §5` 第 77–79 行
   **问题**：业务规则引用未包含 `PRD §4.6`，而 `§4.1/§7.3/§8.4` 的 expired 复活逻辑直接对标该章节。
   **建议**：在 `§5` 中增加规则 4“套餐过期后恢复 active 的有效期计算规则”并引用 `PRD §4.6`。

4. **位置**：`user-story.md §6` / `§4.2` 第 69 行 / `§8.2`
   **问题**：`RETURN_QUOTA_EXCEEDED` 是防止超扣课时的关键边界，但 `§6` 验收标准中未包含重复返还达到上限的 GWT 场景。v1 评审已标记，当前仍未修复。
   **建议**：补充场景“管理员对同一 booking 再次返还达到上限”到 `§6`，明确 HTTP 400 + `RETURN_QUOTA_EXCEEDED`。

##### 🟢 轻微
5. **位置**：`user-story.md §13`
   **问题**：Figma 链接待填充。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（P0 状态不同步 + expired 复活规则引用不清 + 缺少重复返还上限 GWT）

#### Figma 准备度
⚠️ 需明确返还弹窗中“原因分类”与“有效期重新计算”的文案提示后再进入高保真设计。

---

### US-036 教练提交请假申请

- **当前 INDEX 状态**：[APPROVED]
- **综合评分**：7.7 / 10

| 维度 | 评分 | 简要说明（含具体证据） |
|------|------|----------------------|
| 产品设计合理性 | 8.0 | `§4.1` 提交请假路径清晰；与 US-044 审批及自动取消预约的边界明确。 |
| 业务逻辑 | 7.5 | `§4.2` 时间冲突、开始时间已过、时间范围非法、教练状态非法等校验与 `PRD §5.4.5/§6.6` 一致；但 >30 天“管理员重点关注”规则 PRD 未明确。 |
| Figma 可设计性 | 8.0 | `§13.1` 请假申请页、提交成功弹窗四态完整。 |
| 泳道/角色完整性 | 7.5 | `§2` 教练（主）、管理员（审批方，辅）；审批逻辑由 US-044 承接，职责边界清晰。 |
| 异常场景 | 8.0 | `§8` 覆盖快速重复提交、结束时间早于开始时间、超长时间请假、教练状态非已通过。 |
| 商业合规 | 8.0 | 请假需管理员审批、与实时状态同步，符合 `PRD §5.4.5/§5.4.1`。 |

#### 主要问题

##### 🔴 严重
1. **位置**：`user-story.md` 第 3 行 / `INDEX.md` 第 52 行
   **问题**：`user-story.md §1` 状态为 `[REVIEW]`，`INDEX.md` 登记为 `[APPROVED]`。
   **建议**：同步状态字段。

##### 🟡 中等
2. **位置**：`user-story.md §4.2` / `tech-design.md §2.1`
   **问题**：user-story 写“教练状态不允许：`coach.status ≠ 1` → 返回错误码 `COACH_STATUS_INVALID`”，未给出 HTTP 状态码；tech-design 将其映射为 `403`。v1 评审已标记，当前仍未修复。
   **建议**：在 `§4.2` 中明确“返回 HTTP 403 + 错误码 `COACH_STATUS_INVALID`”。

3. **位置**：`user-story.md §8.3` 第 161–165 行
   **问题**：“请假跨度 > 30 天，允许提交，但标记为「管理员重点关注」”在 PRD 中无对应规则，也缺少后续处理动作。v1 评审已标记，当前仍未修复。
   **建议**：要么在 PRD 中补充长请假的运营规则与系统行为，要么将其降级为 P2/移除，避免开发实现无依据功能。

##### 🟢 轻微
4. **位置**：`user-story.md §13.1`
   **问题**：状态截图清单均为 🔲 待设计填写。
   **建议**：设计稿产出后回填。

#### 推荐状态
[REVIEW]（P0 状态不同步 + HTTP 状态码未明确 + 超长时间请假规则来源需确认）

#### Figma 准备度
⚠️ 需明确超长时间请假的提示文案与“管理员重点关注”标记样式后再进入高保真设计。

---

## 组 4 结论

### 状态结论

| US | 当前 INDEX 状态 | 推荐状态 | 关键降级原因 |
|----|----------------|----------|--------------|
| US-029 | [APPROVED] | [REVIEW] | 1. user-story §1 仍为 [REVIEW]；2. 重复提交 HTTP 200 vs 409 不一致 |
| US-030 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. 估时 1d vs 1.5d 不一致；3. tech-design/test-plan 标题仍含“改约”；4. 缺少 <2h 特殊原因取消 GWT |
| US-031 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. <24h 代约/改约需学员确认属于 US 自增规则，需 PM/PRD 确认 |
| US-032 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. 签到窗口“15 分钟”与 PRD §6.1.2“≤10 分钟”不一致；3. test-plan 标题含“签退” |
| US-033 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. 旷课标记与 24h 自动确认时序需明确 |
| US-034 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. INDEX 依赖 US-035 与 user-story 不一致；3. 导出 MVP 范围需明确 |
| US-035 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. expired 复活与 PRD §4.6 引用关系需澄清；3. 缺少重复返还上限 GWT |
| US-036 | [APPROVED] | [REVIEW] | 1. 状态不同步；2. COACH_STATUS_INVALID HTTP 状态码未明确；3. >30 天请假规则 PRD 无依据 |

### 综合判断

- **维持 APPROVED**：无。
- **建议降级**：US-029 ~ US-036 全部建议降级为 `[REVIEW]`。

### 核心依据

1. **系统性状态不同步**：本组 8 个 US 的 `user-story.md §1` 状态仍为 `[REVIEW]`，而 `INDEX.md` 已登记为 `[APPROVED]`。按 `AGENTS.md` 与 `SPECIFICATION.md` 要求，US 状态应以 `user-story.md §1` 与 `INDEX.md` 一致为前提；当前状态不同步本身即构成 P0 问题。
2. **未修复的 v1 P0/P1 问题**：US-030 估时不一致、US-030 配套文档标题含“改约”、US-032 签到窗口口径、US-036 HTTP 状态码与 >30 天请假规则等 v1 评审问题在当前版本中仍未修复。
3. **新增 P1 业务缺口**：US-030 缺少 `PRD §6.5.1` 中 `<2h` 特殊原因取消场景；US-031 `<24h` 代约/改约学员确认规则在 PRD 中无直接依据；US-035 expired 复活规则与 `PRD §4.6` 引用关系不清。

### 修复优先级清单

| 优先级 | US | 问题 | 建议修复位置 |
|--------|----|------|-------------|
| P0 | US-029~US-036 | INDEX [APPROVED] 与 user-story [REVIEW] 不同步 | `INDEX.md` 或各 `user-story.md §1` |
| P0 | US-030 | 估时 1d vs 1.5d 不一致 | `user-story.md` 顶部第 5 行 |
| P1 | US-030 | tech-design/test-plan 标题仍含“改约” | 配套文档标题 |
| P1 | US-030 | 缺少 <2h 特殊原因取消 GWT | `user-story.md §6` |
| P1 | US-032 | 签到窗口 15 分钟与 PRD ≤10 分钟不一致 | `user-story.md §4.1/§12`、`tech-design.md §2.1` |
| P1 | US-032 | test-plan 标题含“签退” | `test-plan.md` 标题 |
| P1 | US-031 | <24h 代约/改约学员确认规则需 PM/PRD 确认 | `user-story.md §3/§4`、`PRD §5.4.2` |
| P1 | US-034 | INDEX 依赖 US-035 与 user-story 不一致 | `INDEX.md` 第 50 行 |
| P1 | US-035 | expired 复活与 PRD §4.6 引用关系不清 | `user-story.md §5/§6.4` |
| P1 | US-035 | 缺少重复返还上限 GWT | `user-story.md §6` |
| P1 | US-033 | 旷课标记与 24h 自动确认时序需明确 | `user-story.md §2/§4.2/§14.1` |
| P1 | US-036 | COACH_STATUS_INVALID HTTP 状态码未明确 | `user-story.md §4.2` |
| P1 | US-036 | >30 天请假“管理员重点关注”规则 PRD 无依据 | `user-story.md §8.3` 或 `PRD` |
| P2 | 全部 | Figma 链接与设计评审记录为空 | 各 `user-story.md §13/§15` |

### Figma 设计准备度总评

- **可进入低保真原型**：US-029、US-030、US-031、US-033、US-034、US-035、US-036（页面结构与四态已定义）。
- **需先统一业务口径**：US-032（签到窗口 10 分钟 vs 15 分钟）。
- **不可进入高保真设计**：全部 US——因 §13 Figma 链接与 §15 设计评审记录均未完成，且存在未闭合的 P0/P1 业务问题，高保真设计可能因规则变更而返工。

---

## MVP 逻辑通顺性补充检查

| 检查项 | 结论 | 说明 |
|--------|------|------|
| 预约 → 取消流程 | ⚠️ 基本通顺 | US-029 → US-030，但 US-030 缺少 <2h 特殊原因取消分支，与 PRD 不完全对齐。 |
| 预约 → 签到 → 确认流程 | ⚠️ 存在口径差异 | US-029 → US-032 → US-033，但 US-032 签到窗口 15 分钟与 PRD 状态机 10 分钟不一致。 |
| 确认 → 返还课时流程 | ✅ 职责清晰 | US-033 → US-035，拆分合理，返还边界明确。 |
| 教练请假 → 排班管理流程 | ✅ 边界清晰 | US-036 → US-044，提交与审批取消职责分离。 |
| 状态机覆盖 | ⚠️ 需校验 | 预约状态机主要转换已覆盖，但 US-032 签到窗口口径差异可能导致“已预约 → 待上课”切换时序不一致。 |

---

## 评审质量自检

- [x] §0 预检报告已输出，所有 US 文件存在性已验证
- [x] 每个 US 的 user-story.md 已完整读取
- [x] 每个 US 的 PRD 引用已打开 prd.md 对照验证
- [x] INDEX 一致性已校验（标题/状态/编号）
- [x] 每个评分说明都引用了 US 文档具体位置（§X.Y）
- [x] 没有超过 3 个 US 的问题描述相同（每个 US 的问题均差异化）
- [x] 综合评分标准差 > 0.5（6.7 ~ 8.6，std ≈ 0.66）
- [x] 评审结果已归档到 `docs/guides/review-group4-booking-cancel-records-v2.md`
- [x] 归档文件包含版本头（日期/范围/依据 PRD/变更摘要）
