# 用户故事拆分规范

> **本文档性质**：约束性规范，所有 US 拆分必须严格遵守本文档。
> **本文档位置**：作为 §9 SDD 工作流 [1] 拆分用户故事 的输入约束。
> **适用范围**：MVP 范围内所有用户故事（预计 30-50 个）。
> **失效条件**：随 v11 PRD 演进，必要时修订；修订需 PM 评审通过。

---

## 1. 命名规范

### 1.1 故事编号
- 格式：`US-XXX`，三位数字连续编号（US-001、US-002...）
- 编号一经分配**不重用**
- 删除的故事保留编号（不回收），仅标记 `[DEPRECATED]`

### 1.2 文件命名
- 格式：`US-XXX-[角色]-[动作].md`（kebab-case）
- 示例：`US-001-学员-单次预约.md`、`US-015-教练-申请离职.md`

### 1.3 标题规范
- 格式：`[角色] + [动作] + [目标]`
- 角色用主语（学员 / 教练 / 管理员 / 系统）
- 动作用动词
- 示例：✅ "学员单次预约教练时段" / ❌ "预约功能开发"

---

## 2. 故事结构（标准模板）

每个用户故事**必须**包含以下 12 个字段：

| # | 字段 | 必填 | 说明 |
|---|------|------|------|
| 1 | 编号 | ✓ | `US-XXX` |
| 2 | 标题 | ✓ | "[角色] + [动作] + [目标]" |
| 3 | 角色（Actor）| ✓ | 主要使用者 + 涉及的其他角色 |
| 4 | 业务价值（Why）| ✓ | 一句话说明为什么需要这个故事 |
| 5 | 触发条件 | ✓ | 什么事件/操作触发该故事 |
| 6 | 前置条件 | ✓ | 故事执行前必须满足的状态 |
| 7 | 业务流程 | ✓ | 步骤列表（带异常分支）|
| 8 | 业务规则引用 | ✓ | 引用 v11 PRD 章节（如 §6.4.5）|
| 9 | 验收标准 | ✓ | Gherkin Given-When-Then |
| 10 | 数据/API/状态机影响 | ✓ | 列出新增/修改 |
| 11 | 边界场景 | ✓ | 至少 3 个异常场景 |
| 12 | 估时 + 优先级 | ✓ | 人天（0.5-3 天）+ [MVP/P2/P3] |

---

## 3. INVEST 原则（自检）

每个故事必须满足 INVEST 六项：

| 字母 | 含义 | 自检问题 |
|------|------|----------|
| **I**ndependent | 独立 | 是否能独立交付而不依赖其他 US？ |
| **N**egotiable | 可协商 | 是否聚焦"做什么"而非"怎么做"？ |
| **V**aluable | 有价值 | 是否对用户/业务有明确价值？ |
| **E**stimable | 可估算 | 能否在 0.5-3 天内完成？ |
| **S**mall | 足够小 | 能否在一个 Sprint 内完成？ |
| **T**estable | 可测试 | 验收标准是否可客观验证？ |

**至少 5/6 项通过**才能进入下一阶段。

---

## 4. 验收标准（Gherkin）

### 4.1 格式要求
- **统一**使用 Gherkin（Given-When-Then）格式
- 每个故事至少 3 个场景：1 个正常 + 2 个异常
- 场景命名清晰，描述业务含义

### 4.2 标准模板

```gherkin
场景：[场景描述]

Given [前置条件]
And  [更多前置条件]
When [触发动作]
And  [更多动作]
Then [预期结果]
And  [更多预期]
```

### 4.3 反模式（不允许）

| ❌ 反模式 | ✅ 正确 |
|----------|--------|
| "用户点击按钮，系统保存数据" | "用户点击提交，订单写入 DB 且 status='已支付'" |
| "测试通过" | "Given 余额=100，当购买 50 时，Then 余额=50" |
| "系统正常工作" | "Then 页面在 2s 内显示订单详情" |
| 单步 When | 拆分为多步 When + And |

---

## 5. 拆分粒度

### 5.1 大小控制

| 维度 | 范围 |
|------|------|
| 估时 | 0.5-3 天（人天）|
| 代码行数 | 100-500 行（含测试）|
| 涉及页面 | 1-3 个 |
| 状态机影响 | 1 个状态转换，或不改 |
| 跨服务调用 | 0-2 个 |

### 5.2 拆分原则

| 原则 | 说明 |
|------|------|
| **单一职责** | 一个故事只做一件事 |
| **跨页面拆分** | 涉及多页面时拆为多个 US |
| **跨状态机拆分** | 涉及多状态转换时拆为多个 US |
| **CRUD 拆分** | 创建/读取/更新/删除 可拆为独立 US |
| **场景拆分** | 不同用户场景（正常/异常）可拆为独立 US |

### 5.3 反例

| ❌ 不应作为单个 US | 拆分为 |
|------------------|--------|
| "学员管理所有功能" | US-学员注册、US-学员登录、US-学员信息修改... |
| "订单全流程" | US-下单、US-支付、US-退款、US-查看订单 |
| "套餐系统" | US-购买套餐、US-查看套餐、US-套餐退款、US-套餐续费... |

---

## 6. 覆盖性检查（红线）

### 6.1 11 项 MVP 能力必须全覆盖

US 拆分完成后必须能用一张表映射：

| MVP 能力（v11 §13.1）| 涉及 US |
|---------------------|---------|
| 1 游客浏览 | US-001, US-002, US-003... |
| 2 注册登录 | US-005, US-006... |
| ... | ... |

**任何能力必须有至少 1 个 US 覆盖**。无覆盖 = 漏拆。

### 6.2 状态机触达

| 状态机 | 必须触达的所有转换 |
|--------|---------------------|
| 用户身份 | 游客→注册用户、注册用户→学员、学员→注册用户 |
| 套餐状态 | active→exhausted、active→expired、active→frozen、active→refunded、frozen→active、frozen→refunded |
| 教练状态 | 0→1、1→4、4→3、4→1、3→0 |
| 预约状态 | 见 §6.1 状态机定义 |
| 订单状态 | 见 §6.2 状态机定义 |

**任何状态转换必须有至少 1 个 US 触发**。无触达 = 漏拆。

### 6.3 边界场景不遗漏

每个 US 的边界场景至少覆盖：
- 1 个**空状态**（无数据时）
- 1 个**错误状态**（输入非法/网络异常）
- 1 个**并发/重入**（同一操作多次执行）

---

## 7. 拆分检查清单（合并前必查）

每完成一批 US 拆分后，按此清单逐项检查：

### 7.1 完整性检查
- [ ] 11 项 MVP 能力 100% 覆盖（用映射表验证）
- [ ] 5 大状态机的所有转换都触达
- [ ] 总 US 数 30-50 个（在合理范围）
- [ ] 每个 US 估时 0.5-3 天
- [ ] 至少 80% US 标记为 [MVP] 优先级

### 7.2 质量检查
- [ ] 每个 US 都满足 INVEST 5/6 项
- [ ] 每个 US 都有 Given-When-Then 验收标准
- [ ] 每个 US 都有 ≥ 3 个边界场景
- [ ] 每个 US 都引用 v11 PRD 章节
- [ ] 无技术任务伪装成用户故事（如"搭建 CI"不算 US）

### 7.3 一致性检查
- [ ] 命名规范统一（US-XXX + kebab-case 文件名）
- [ ] 角色使用统一术语（学员/教练/管理员/系统）
- [ ] 业务规则引用规范（§x.y.z 格式）
- [ ] 估时使用统一单位（人天）

### 7.4 依赖检查
- [ ] US 间无循环依赖
- [ ] 依赖关系已显式标注（前置 US 列表）
- [ ] 优先实现底层故事（被依赖的先做）

---

## 8. 拆分流程（推荐步骤）

### Step 1: 能力拆解
- 读取 v11 PRD §13.1（11 项 MVP 能力）
- 读取 v11 PRD §5.x（各角色功能模块）
- 输出：能力清单

### Step 2: 故事初稿
- 每个能力先列 3-5 个候选故事
- 不要求完美，先穷举
- 输出：候选 US 列表（60-80 个）

### Step 3: 合并拆分
- 合并相似的故事
- 拆分过大的故事
- 标注依赖关系
- 输出：US 列表（30-50 个）

### Step 4: 模板填充
- 按 §2 模板逐个填写
- 编写 GWT 验收标准
- 输出：完整 US 文档

### Step 5: 评审
- 走 §7 检查清单
- PM/开发/测试三方评审
- 输出：定稿 US 列表

### Step 6: 编号入册
- 分配 US-XXX 编号
- 创建目录 `prd/US-XXX-*.md`
- 更新覆盖性映射表
- 输出：可入开发流程的 US 库

---

## 9. 依赖关系处理

### 9.1 依赖标注
每个 US 必填字段增加 `依赖 US`：

```markdown
## 依赖
- 前置：US-005（学员必须先注册）
- 后续：US-020 依赖本 US（购买套餐前必须有学员）
```

### 9.2 实施顺序
按依赖关系拓扑排序：
1. 无依赖的基础故事（如 US-005 注册登录）
2. 业务故事（US-010 购买套餐）
3. 复合故事（US-015 套餐退款）

### 9.3 MVP 优先级
- [MVP] 必做，发布前完成
- [P2] 第二阶段
- [P3] 第三阶段

---

## 10. 反模式清单（绝对禁止）

### 10.1 拆分粒度问题

| ❌ 反模式 | 问题 | ✅ 正确做法 |
|----------|------|-----------|
| US 粒度过大（"整个后台系统"）| 无法估时、无法独立交付 | 按模块拆 |
| US 粒度过小（"添加一个按钮"）| 无业务价值 | 合并到所属功能 |
| US 跨多个 MVP 能力 | 范围不清 | 拆为多个 US |
| US 含技术任务 | 不是用户故事 | 拆出技术任务独立跟踪 |

### 10.2 验收标准问题

| ❌ 反模式 | ✅ 正确 |
|----------|--------|
| 验收标准不客观（"界面好看"）| "页面在 2s 内加载完成" |
| 验收标准不完整（仅正常路径）| 至少 1 正常 + 2 异常 |
| 验收标准与技术实现耦合 | 聚焦业务结果，不写代码 |
| 验收标准不引用 PRD 规则 | 显式引用 §x.y.z 章节 |

### 10.3 命名问题

| ❌ 反模式 | ✅ 正确 |
|----------|--------|
| "US-001 预约功能" | "US-001 学员单次预约教练时段" |
| "US-002 退款" | "US-002 学员申请套餐退款" |
| 角色混乱（混用"用户/客户/学员"）| 统一使用"学员" |

---

## 11. 示例

### ✅ 正确示例

```markdown
# US-001 学员单次预约教练时段

## 基本信息
- **编号**：US-001
- **标题**：学员单次预约教练时段
- **角色**：学员（主）、系统（辅）
- **业务价值**：学员能选择教练的可用时段并完成预约，是核心交易流程
- **优先级**：[MVP]

## 触发条件
学员在 [教练详情页] 选择某个可约时段

## 前置条件
- 学员已登录（依赖 US-005）
- 学员有 active 套餐（依赖 US-010）
- 套餐有可用课时 available > 0
- 时段未被其他学员预约

## 业务流程
1. 学员选择时段
2. 系统校验 package 可用
3. 系统锁单（reserved +1, available -1）
4. 系统创建 booking 记录
5. 返回预约成功

## 业务规则引用
- §6.1 预约状态机
- §6.3 课时预占规则
- §5.3.2 时段唯一性

## 验收标准

场景：正常预约
Given 学员有 active 套餐且 available = 5
When 学员预约明日 9:00 时段
Then booking 创建成功
And package reserved = 1, available = 4
And booking.status = '已预约'

场景：时段已被预约
Given 明日 9:00 已被其他学员预约
When 学员尝试预约明日 9:00
Then 返回错误 "该时段已被预约"

场景：套餐无可用课时
Given 学员套餐 available = 0
When 学员尝试预约
Then 返回错误 "请先购买套餐"

## 数据/API/状态机影响
- 数据：新增 booking 记录
- API：POST /api/booking/create
- 状态机：booking.status 0→2（无→已预约），package reserved +1

## 边界场景
1. 学员快速重复点击 → 幂等键防重
2. 网络中断 → 前端重试机制
3. 教练请假 → 自动取消 + 释放预占

## 依赖
- 前置：US-005（注册登录）、US-010（购买套餐）
- 后续：US-003（取消预约）、US-008（教练确认扣课时）

## 估时
1.5 人天
```

### ❌ 错误示例

```markdown
# US-001 预约系统

## 业务
用户可以预约教练

## 验收
预约能成功
```

**问题**：
- 标题不清晰（"预约系统"范围过大）
- 缺少角色/触发/前置条件
- 业务流程缺失
- 无 PRD 引用
- 验收标准不客观（"预约能成功"无验证标准）
- 无边界场景
- 无数据/API/状态机影响
- 无依赖
- 无估时
```

---

## 12. 检查清单（每次拆分后必查）

| # | 检查项 | 通过标准 |
|---|--------|----------|
| 1 | 11 项 MVP 能力全覆盖 | 映射表 100% 命中 |
| 2 | 5 大状态机所有转换触达 | 转换表 100% 命中 |
| 3 | US 总数 30-50 | 范围内 |
| 4 | 每个 US 估时 0.5-3 天 | 范围内 |
| 5 | ≥ 80% US 标 [MVP] | 满足 |
| 6 | 每个 US 满足 INVEST 5/6 | 满足 |
| 7 | 每个 US 至少 1 正常 + 2 异常 GWT | 满足 |
| 8 | 命名规范统一 | US-XXX + kebab-case |
| 9 | 业务规则引用 v11 PRD 章节 | 显式引用 |
| 10 | 无反模式 | 100% 规避 |

**任一项不通过 → 修订后再进入 §9 Step [2] PRD 评审**。

---

## 13. 变更日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-07-29 | 初版：US 拆分规范 |
